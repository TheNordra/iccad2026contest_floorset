# Handoff 2026-08-24 — where the ship stands and what to do next

## SHIPPED AND VERIFIED: `1678680` on `origin/l147-tangent-cut`

`build_submission/cadc1075.tar.gz`, 35.5 MB, 1424 files.
`op_wrapper.py` md5 **`ba9ec1be898b00ddbc41207f70bb2bc1`**.
`bin/constructive_linux` md5 `bc9912072cd97b45b47a03adec7170ce` — **unchanged**;
everything below is pure Python, the ELF was never rebuilt.

Contents beyond the band that ships today:
1. **L147 tangent cuts, by CODE DEFAULT** (`_L147_R/G/TOL/PRICE = 1.5/1.10/0.006/1.0`).
   It had to become a default: the grader strips `ICCAD_*`, so an env-only
   mechanism is inert in the package however green it measures.
   Kill switch `ICCAD_SHAPE_LP_L147=0`.
2. **L165 per-case LP depth map** (`_L157_DEPTH`, 1–3 passes keyed on block
   count), built from the CONTEST-PUBLISHED per-case medians.
3. **Vendored scipy** (`vendor/`, the official cp313 manylinux wheel) used only
   if `import scipy` fails, appended to `sys.path` so a system scipy always wins.

### Gates, all green
- in-set: determinism 100/100 cost AND positions; kill switch == pre-L147 band
  100/100; pass histograms identical across runs `{3:66, 2:24, 1:10}`;
  quality +0.6099% vs the L147 k=1 anchor, 74 moved, **0 worse**, 100/100 feasible.
- Linux (WSL2 32c, scipy 1.18.0), four lanes PASS: shipped default
  **1.1943200373961047** = +5.2313% vs pre-LP, **+2.7167% vs the band**,
  determinism 100/100, passes `1:11 / 2:24 / 3:65` identical across runs.
- ❌ `l113_ship_gate --cores 48` has NEVER RUN — every compiler on this box exits
  1 with empty stdout and stderr, an OS-level block unrelated to any change.
  The grader's path is the bundled ELF with no compile, covered by the Linux lanes.

## WHERE WE STAND

Beta leaderboard (`beta_leaderboard.csv`): **we were rank 6 of 10**.
**The gap is 100% quality** — our cost-weighted RF 0.70004 is the best
obtainable, and rank 1 PAYS 0.82354 (slowest in the top 10) yet wins on raw
1.0027 against our 1.3207.

    OOS NET   s1 +3.0583%   s2 +3.0751%   ⇒ total 0.8962 ⇒ RANK 4
    rank 4 threshold 0.8993   rank 3 threshold 0.8932   ⇒ ~0.33pp still missing

## THE THREE THINGS THAT MAKE OLD LEDGER VERDICTS UNSAFE

1. **The RF pricer is the contest-published per-case medians**
   (`C_median_runtimes_beta_hidden.csv`, read by `l146_rf_price.py`). It
   reproduces the graded total to 2.4e-7. `m67e_rf48.py`'s kappa = 3.1612 is a
   back-solve from one aggregate, wrong per case by up to 8x — CLAUDE.md still
   calls it definitive.
2. **Runtime is not the constraint.** 1.73x headroom, measured, and PER CASE
   (min 0.96x, p50 1.74x, p90 2.63x, max 3.91x). Local LP seconds ÷ f = 3.17.
3. **beta's 52.07s contains no LP** — the graded package (M73, `7f38893`) has
   `_shape_lp` zero times; it landed 11 days later at `d0db1fb`. Any reasoning
   that splits 52.07s into "pool + LP" is circular.

## NEXT: FIND THE LAST 0.33pp

Highest-prior lens, because it is exactly how today's +0.35pp was found:
**hunt for estimates used where a measurement or published value is available.**
`_L157_NSET` used a fitted `M_hat(n)` while `r["med"]` sat in the same rows.
Candidates not yet audited: `_RH = 1.4` and `A_hat = 1.035*sum(area)` in
`_proxy_metrics`; `_LP_UTIL = 0.968`; the M45/M49/M50 band constants.

Also open: spend the free 1.73x by UNDOING the M42/M45/M49/M50 cuts, which all
traded quality for wall; re-check L140's boundary finding on the current tree;
widen the profile pool (the wall is max-setter bound, `c*` p50 19.3, so under
~23 profiles it does not grow at all — M80's cut at K=8 may no longer bind).

⚠️ Rank by **gain ÷ gates that must re-run**, not by gain. Every touch of the
shipped path costs three in-set gates + restage + four Linux lanes, ~1.5 h, and
the deadline is 2026-08-28.

🔑 **arm-mixing is EXACT** — mixing flat k-arms per case reproduces a really-run
gated arm 100/100 on cost AND positions, so candidate gates can be scored
offline from the committed flat arms with no new 100-case runs.

## OPEN QUESTION FOR THE ORGANISERS (worth 5.4%)

Two official documents conflict. Beta guidelines §2: leave `requirements.txt`
empty, the environment provides numpy/torch/**scipy**/numba/tqdm/shapely/
threadpoolctl. Beta evaluation report §2(a): "Do not assume any package beyond
the Python standard library is available", naming scipy. Our beta package cannot
settle it — M73 imports scipy zero times. Blocking scipy and running the real
evaluator costs **5.4171%** (1.191977686767963 → 1.260246745790688) and used to
be completely SILENT. It is no longer: the optimizer prints
`[scipy] source=system|vendored|absent` on every path, `l117_linux_verify` FAILS
if the marker is missing or says absent, and the organisers return our stderr.
Also unresolved: whether a conditionally-used 35 MB `vendor/` counts as an
"unused large binary" under the DQ rule.

---

# Session 2 addendum — 2026-08-24 late

## SHIPPED, unchanged: `1678680` + `8432d15` (the latter is probe/label only)

Nothing in the shipping path moved after `1678680`. Package identity
`op_wrapper.py` md5 **`ba9ec1be898b00ddbc41207f70bb2bc1`**.
**NET s1 +3.0583% / s2 +3.0751% ⇒ projects to rank 4.** Rank 3 needs ~0.33pp more.

## Two corrections to numbers quoted earlier — use these, not the old ones

1. **Free runtime budget is 19.79s, not 38s.** The 52.07s beta baseline predates
   the shape LP by eleven days, so 37.99s was the budget *before* L147 and the
   depth map spent 18.23s of it. Per-case slack p50 1.74x → **1.31x**; the
   heaviest cases are nearly out (n=112 1.00x, n=108 1.03x, n=106 1.04x).
   The 1.73x aggregate multiplier was **never spendable** — applied uniformly it
   prices at −1.9540%, because the aggregate hides that the low-slack cases are
   the heavy-weight ones.
2. **The REFINE band constants are dead on the shipped path.** `_l137_env()`
   injects `ICCAD_HINT_REFINE=4` into every profile at ≥40 cores and
   `constructive.cpp:2142` clamps `REFINE_ITERS` to it — verified on HEAD across
   all n and all 51 profiles, effective `REFINE_ITERS = {4: 51}`, zero
   exceptions. That is why L148 read the band-cut restore as +0.0000%. They are
   NOT dead below 40 cores, so **label, do not delete**. Not done yet — no
   tracked edit this close to the deadline unless the label is the only change.

## Verdicts reached this session

| item | verdict |
|---|---|
| 32-profile probe tier (`ICCAD_LENSD_POOL=1`) | 🔴 **RED** — quality real (OOS +0.3383% / +0.5501%, transfer 75%/122%) but costs 63–75 grader-seconds against 19.79s |
| LP depth k=3 flat | 🔴 superseded — the per-case depth map already takes k=3 wherever it fits |
| soft-violation tolerance artefacts | 🔴 CLOSED — every violation has metres of room; the deficit is placement, not arithmetic (re-confirms L140) |
| L152 LANE D | 🟡 **open**, see below |
| LP hpwl-baseline predictor | 🟡 **open**, OOS running as `l170_oos_s{1,2}_hb.json` |

🔑 **THE REUSABLE RULE FROM THE PROBE TIER:** every added profile costs ~71 ms of
**serial** `_proxy_metrics` on the main thread (M47: 2.9s for 41 profiles at
n=120), so N profiles cost at least `N * 0.071 / f` grader-seconds **regardless of
core count** — M47 also records that parallelising the proxies was 4x WORSE (GIL).
"Adding profiles is nearly free" stopped being true at M47. This also explains
M80's K=8: 8 profiles ≈ 0.57s, which the budget carries; 32 is 2.3s, which it
does not. **Apply this before running anything on that axis.**

## The two still open

**L152 LANE D** (`constructive_l152.cpp`, R2 `ICCAD_BND_FRAME_ITEM` + R3
`ICCAD_BND_SNAP_BEST`, both default-0 and off-path verified 510/510).
OOS s1 +0.2769%, s2 +0.1877%, mean +0.23% — **sub-bar** against the
pre-registered 0.30%. Wall 1.029–1.037x re-priced on the *current* clock is
−0.0402% / −0.0600% ⇒ **NET +0.13% to +0.24%**. (L152_REPORT.md's own
"~1.03x ≈ −0.005%" is the pre-L147 clock and understates it 12x — same
stale-baseline class as the M_hat bug.)
✅ **The toolchain blocker is cleared**: a fresh WSL `-O3 -static-libstdc++
-static-libgcc` build of `constructive.cpp` needs **GLIBC_2.38** against Debian
13's 2.41 (the shipped ELF needs 2.34). PyInstaller died at 2.43 > 2.41; the C++
binary clears it.
⚠️ Cost of shipping it: a `constructive.cpp` change forces an ELF rebuild, which
invalidates `audit_cache*.pkl`, `l125_dt_cache.pkl` and **every bit-equality
gate**, and "new cpp + old ELF" is this project's most-recorded near-miss
(L124, L136, route A). Decide whether +0.13–0.24% is worth that with 4 days left.

**LP hpwl-baseline predictor** (`ICCAD_LP_HB_PRED=0.2994`, ported into the tree,
default OFF). The shape LP scores against OUR layout's hpwl while the evaluator
scores against its own baseline — the same error `_LP_UTIL` already refuses to
make on the area side. Our hpwl is 1.238x the evaluator's baseline, so HPWL is
weighted 1.24x too low against area, and hpwl carries 10.15% of the deficit.
In-set **+0.0772%** (96 moved / 31 worse / 100/100 feasible).
**Cost measured in-process: LP 57.79s → 59.04s = +1.25s (1.02x) = 0.4
grader-seconds** against 19.79s of budget. The lens that proposed it read
"1.93x" from whole-eval wall under contention — **the third time this session
that cross-run differencing produced a wrong LP cost**; always use
`ICCAD_LP_TIMING`.
⏳ OOS running when this was written: `l170_oos_s{1,2}_hb.json`. Control is the
shipped depth map, reconstructible **exactly** by arm-mixing, so only the arm
needs running. If OOS holds near +0.08–0.10% it is cheap enough to ship.

## Unresolved risk, unchanged

The whole banked stack is gated on `os.cpu_count() >= 40`. The organisers' docs
say 48 cores twice (guidelines "up to 48 cores", FAQ A2/A3 "ICELAKE CPU with 48
cores") but a containerised report of <40 would silently ship the pre-LP
configuration and nothing in the package would say so. Cheapest mitigation:
one stderr line reporting detected cores and which tiers fired — the organisers
return our stderr (beta's `eval_op_wrapper.log` carries the tqdm bar, which is
stderr). Not done: it forces a full gate re-run for information that helps the
*next* round, not this one.

`l113_ship_gate --cores 48` has still never run — every compiler on this Windows
box exits 1 with empty output. WSL compiles fine, so the gate could be adapted
to run there, or a teammate with a working g++ can run it as-is.

---

# Session 3 addendum — 2026-08-24, after the 08-23 data landed

## THE CONTEST REPUBLISHED THE MEDIANS AND THE LEADERBOARD. EVERYTHING ABOVE
## THAT PRICES RUNTIME IS STALE.

`beta_2026-08-23/` now holds the three files published 2026-08-23. Both tables
are authoritative on the same evidence the old ones were — the new medians
reproduce our updated graded `total_score` 0.9265861161320369 to 6e-7.

**We are rank 4, at 0.92659.** Not because we improved: two entries were
replaced, and our own score got 0.22% WORSE while we touched nothing.
Rank 3 is 0.89933 and is `raw x 0.7` to the last digit — on the floor everywhere.

**All 100 medians FELL.** p50 x0.742, sum 295.72s -> 216.13s.

### What that invalidates, in order of size

1. **`_L157_DEPTH` had gone NET NEGATIVE.** Built on the old table, priced on
   the new one it is **−0.723%**, not the +0.887%/+0.904% the code comment
   claims. Rebuilt at s_build 0.90: **{1:52, 2:18, 3:30}**, OOS +0.4153% (s1) /
   +0.4452% (s2), and its RF cost is **exactly zero** — it lands on the same
   graded total and the same 21 off-floor cases as tangent-only. It is a strict
   shallowing of the old map (0 deeper, 61 shallower). Full grid: `L172_REPORT.md`.
2. **The free budget is 14.72s, not 19.79s and not 38s**, and 0.97s of it is
   already overspent on 18 cases that are past the floor with no LP at all.
3. **Uniform slowdown is 6.5x harsher.** 1.5x was −0.70% on the old table and is
   **−4.54%** on the new one. A flat +0.10s/case costs −0.322%.
4. **Being faster is worth at most +0.229%** — we are 0.161% above a hard floor
   of 0.7000, and 74% of that sits on three pool-bound cases (n=112/99/92).
   Runtime is a constraint now, not a source of gain.

## SHIPPED THIS SESSION (staged, NOT yet gated)

`op_wrapper.py` md5 `ba9ec1be…` → **`243dcf3897b296769026878e64475b13`**.
ELF unchanged (`bc9912072cd97b45b47a03adec7170ce`). Both changes pure Python.

1. `_L157_DEPTH` rebuilt at s_build 0.90 (above).
2. **`requirements.txt` is no longer 0 bytes.** The final evaluation report §2(a)
   settles the open question against us: it must list every imported package,
   naming scipy. Shipped = `iccad2026contest/requirements.txt` verbatim + scipy,
   torch raised to >=2.5.0, no pins. ⚠️ `make_submission.py` force-wrote the file
   to 0 bytes AND asserted 0 bytes — a staged edit alone would have been silently
   reverted by the next restage; the writer and the gate are both changed.

❌ **No gate has run on either change.** `l172_gates.sh` + `l172_verdict.py` are
written and ready; G3 is new and asserts the map ACTUALLY FIRED (no case may
spend more passes than the map allows, and the histogram must move off
`{3:66, 2:24, 1:10}`). Un-run gates count as FAIL in that script, not as skips.
No Linux lane has run.

## VERDICTS REACHED

| item | verdict |
|---|---|
| quality-aware depth rule (per-n, which k helped) | 🔴 RED — held out +0.3856% vs the affordability rule's +0.4153%. Same shape as L127 tally fitting. |
| depth on cases already past the floor | 🔴 RED — NET −0.929% (k=2) / −2.353% (k=3). The `R^0.3` derivative past the floor is not small. |
| `max_s`, the new CSV column | ⚪ the slowest submission per case (p50 331.6s). Not scoring-relevant. |
| **LP solver speed** | 🟡 **RE-OPENED.** L155 priced it at 0.0000% because the deep map was free on the old table. Now: **1.5x → +0.14pp, 2.0x → +0.32pp, ceiling +0.41pp.** 2.0x is exactly L155's own `f* = 2x` gate. |
| L170 hb-baseline predictor | 🟢 OOS s1 **+0.0928%** (in-set +0.0772%, transfer 120%), 240/240 feasible, 0 SA fallbacks. Cost 0.4 grader-seconds ⇒ ~−0.010% on the new table ⇒ NET ~+0.083%. ⚠️ measured ON TOP OF THE OLD MAP; with the shallower map there are fewer passes for it to act on, so it must be re-measured before shipping. |
| L152 LANE D | 🔴 re-priced on the new table: 1.03x wall ≈ −0.076%, so NET +0.13~+0.16%, still sub-bar — and it forces an ELF rebuild. Do not ship. |

## SILENT-FAILURE NOTES FROM THIS SESSION

- **Two copies of `l170_oos_hb.sh` ran concurrently and the script file was
  edited mid-flight.** 30+ `constructive.exe` on a 48-thread configuration, the
  binary probe timed out, and s1 fell back to python SA on **132 of 240 cases**:
  weighted cost 9.900044 against a healthy 1.4169, feasible 138/240. Quarantined
  in `_quarantine/` with its post-mortem. `l171_oos_hb.sh` and `l172_gates.sh`
  both take a lockfile now.
- **A second agent runs jobs in this same tree.** Nothing here was measured while
  it was busy. `optimizer_constructive.py` was edited at 10:24:26 while that
  agent's s2 arm started at 10:23:29 — see `l172_l170_score.py`, which
  adjudicates which depth map an arm actually ran from its weighted cost (the
  two maps differ by 0.62% on s2, far more than any effect being measured).
- **A gate that reports PASS because nothing ran** is the same silent no-op this
  work exists to prevent. `l172_verdict.py` counts "not run" as a failure.

---

# Session 4 addendum — 2026-08-24, exclusive box

## PACKAGE IDENTITY

`op_wrapper.py` md5 **`815d02dae4639b880c4985ca63827b33`**
(was `ba9ec1be…` at `1678680`, then `243dcf38…` before the L171 merge).
ELF `bc9912072cd97b45b47a03adec7170ce`, **unchanged** — everything is pure Python.
`make_submission.py stage` PASS, 1424 files, 35504292 bytes.
Previous tarball preserved at `_quarantine/cadc1075.tar.gz.pre-L172`.

Contents beyond `1678680`:
1. **L172** `_L157_DEPTH` rebuilt on the 2026-08-23 medians → `{1:52, 2:18, 3:30}`.
2. **L172** `requirements.txt` = the contest's own list + scipy (and
   `make_submission.py` no longer force-writes it to 0 bytes and no longer
   asserts 0 bytes — a staged edit alone would have been silently reverted).
3. **L171** (the other agent's) shape-LP `hpwl_baseline` predictor,
   `_LP_HB_K = "0.2994"`, default ON, kill switch `ICCAD_LP_HB_PRED=0`.
   Their version is preserved at `_quarantine/optimizer_constructive.THEIRS-L171.py`.

## 🚨 A WHOLE LINE OF WORK WAS WITHDRAWN. READ THIS BEFORE TRUSTING L173/L176/L178/L179.

Those reports concluded the tree was **3.95x M73's wall**, projected **graded
1.109**, and said the package could not ship. **All of it is void**, and
`L173_REPORT.md` / `L178_ROBUSTNESS.md` carry VOID headers.

Cause: **route A is ON by default at >= 40 DETECTED cores** (`_route_a_default`,
`_ROUTE_A_CORES_MIN = 40`, `_ROUTE_A_INFLIGHT = 4`), and every arm ran with
`ICCAD_ADAPTIVE_CORES=48`. Route A converts each profile into frame tasks on a
global queue — built for 48 REAL cores. This box has **16 physical** cores:

```
           route A ON   route A OFF     M73      cost
  n= 60      3.589s        1.233s     1.277s   1.210020  (bit-identical)
  n=120      4.011s        2.757s     2.780s   1.267585  (bit-identical)
```

Route A does **not change results**, only wall. With it off the current wrapper
matches M73 to within noise, and `l180_diff.py` shows why: at n=120 and n=60 the
current wrapper selects the **same 35 profile indices with identical effective
env** as M73. There was never a pool regression to explain.

Also withdrawn from that line: a claim that the shipped binaries were pre-L108.
That came from `strings`, which finds **zero** `ICCAD_` variables in either
binary. Use `grep -a` — it finds `ICCAD_FRAME_REPORT` and
`ICCAD_FORCE_FRAME_IDX` in both.

## THE TRUE POOL NUMBERS (l181, route A off, exclusive box, 100 cases, LP off)

```
arm      scored_s   weighted cost   profiles
cur      130.21     1.260247        51      <- the tree
nom80    109.96     1.262895        43
notwin    93.86     1.277445        35
nohint   112.97     1.276361        35
m73      112.77     1.281457        35      <- what beta shipped
```

**The pool is 1.155x M73 and buys +1.66% of quality.** Priced on the new
medians that costs −1.0153%, so the pool trade is NET about **+0.65%**.
Incidentally L137's hint is a wall SAVING here (notwin 93.86s vs nohint
112.97s) because it caps `REFINE_ITERS`, at −0.085% of quality.

## 🚨 THE REAL UN-DE-RISKED ITEM: ROUTE A HAS NEVER RUN ON THE GRADER

Beta was M73, which has no route A. Its **−32.2% wall at 48c is a PROJECTION**,
never a measurement. The ledger separately records it sinking every case to SA
fallback at 48 cores, and hanging 600 s on a pre-L108 binary. On 16 physical
cores it costs 2.9x. So the shipped package's grader runtime is bounded by
`P * t_beta` with **P somewhere in ~[0.78, 1.155] and unpinnable from here** —
this box and WSL are the same CPU, so neither can measure it.

Every mechanism validated at "forced 48 cores" on this box (M80, the L124 twins,
L137, tier-5) had route A live during that validation, so their wall numbers are
contaminated in the direction that penalises bigger pools. Use `ICCAD_ROUTE_A=0`
to measure a pool; use a genuinely 48-core machine to measure the package.

## SHOULD `1678680` / `ba9ec1be…` BE SUBMITTED?

**Not as the final answer, but keep it — it is the only fully gated artifact.**

| | `ba9ec1be…` (1678680) | `815d02da…` (now) |
|---|---|---|
| LP depth map | old, **NET −0.723%** on the new medians | x0.90, RF ~0 at P=1 |
| requirements.txt | **0 bytes** while importing scipy | contest list + scipy |
| gates | in-set + 4 Linux lanes PASS | **none yet** |

The gap is ~1.15pp and rank 3 is ~0.06pp away, so it is decisive rather than
cosmetic. Ship `815d02da…` **if and only if** it clears gates; otherwise
`ba9ec1be…` is the safe fallback that merely under-earns.

## OPEN

- `l177_gates.sh` (det1/det2/k1/l147off/**hboff**) running. `hboff` is new: L171
  was measured on the OLD map, which ran 66 cases at k=3; the new one runs 22, so
  its +0.0772% does not carry over unexamined.
- Linux four lanes not yet re-run for `815d02da…`.
- L182: the map is optimal at P=1.00, worst case −0.19% across the P range.
  Deliberately NOT changed — a 0.42pp refinement in one corner of an unpinnable
  range does not justify voiding a 50-minute gate cycle.

---

# Session 5 — the LP was never priced, and that changes the whole answer

## 🚨 READ FIRST: the staged package is NOT the recommended configuration

`build_submission/` currently holds `op_wrapper.py` md5
**`815d02dae4639b880c4985ca63827b33`** = L172's x0.90 depth map + L172's
requirements.txt + L171's hb predictor. It passed the full in-set gate suite
(L177, ALL PASS). **Its depth map is nevertheless the wrong choice** -- see the
ladder below. Do not upload it without reading this section.

## What was measured, and how

The method that finally worked, after three failed ones (local wall-clock at
>=20% noise; `m67e_rf48.py`'s kappa, wrong per case by up to 8x; and my own
mixing of WSL walls with a WSL-calibrated `f`):

    t_ours_grader(n)  =  t_beta(n) * w_ours(n) / w_m73(n)

Both walls on ONE box, same day, same flags; `t_beta(n)` is the GRADER's own
per-case runtime for M73. Every machine-speed constant cancels -- `f` never
appears. M73's wrapper comes from `git show 7f38893:optimizer_constructive.py`
and lives in `_m73win/`.

Route A OFF in every arm. It is verified result-neutral (single cases
bit-identical on/off; L177 det1 vs det2 matched 100/100 on cost AND positions
with it live) and costs 2.9x on this 16-physical-core box.

## The ladder (l189_ladder.py)

```
configuration                   wall  quality       RF      NET    graded  rank
M73 pool, no LP  (= beta)      52.1s  +0.000%  +0.000%  +0.000%   0.92659    4
+ pool work, no LP             60.4s  +1.655%  -2.699%  -1.044%   0.93585    5
+ LP at k=1                    81.2s  +6.450%  -9.209%  -2.759%   0.94664    5
+ LP, x0.90 depth map          97.9s  +6.788% -15.772%  -8.984%   0.99991    9
thin pool + LP k=1             73.8s  +5.060%  -4.793%  +0.268%   0.92186    4
  route A 0.68x:
+ LP at k=1                    55.2s          -1.551%  +4.899%   0.88026    2
+ LP, x0.90 depth map          66.6s          -3.992%  +2.796%   0.89817    3
```

**The shape LP costs 20.8 grader-seconds against a free budget of 14.72 s.**
CLAUDE.md's "下一步 3" lists LP depth as the largest unrealised lever, "卡在 RF
無法定價". It is now priced, and it is net negative.

**k=1 gets 95% of the LP's quality (+6.450 of +6.788) for 60% of the cost.**
Passes 2-3 buy +0.338% for +16.7s = -6.6pp of RF, so **the depth map -- L172's
and L165's alike -- is net negative in BOTH route-A scenarios.**
`ICCAD_SHAPE_LP_DEPTH2=0` strictly dominates it.

⚠️ Why this was missed for so long: **every LP pricing in this ledger omitted
the FIRST pass's seconds.** L172 flagged them missing and then built a whole
depth-map cycle on top anyway. The first pass runs on every case and is the bulk.

## Verdicts

| item | verdict |
|---|---|
| x0.90 depth map (L172) | 🔴 **superseded** -- correct arithmetic, wrong question. k=1 dominates it in both scenarios. Gates ALL PASS, quality +0.3610% vs k=1 anchor, 38 moved / 0 worse -- all true, all irrelevant. |
| L171 hb predictor | 🔴 **-0.0512% in set on the new map** (was +0.0772% on the old one). The `hboff` arm existed precisely to catch this. Kill switch `ICCAD_LP_HB_PRED=0`; `_LP_HB_K if _on else "0"` correctly ties it to the L147 switch (G2 PASS). |
| L124 MIB twins | 🟢 **KEEP.** OOS +0.9090% / +0.4237%, mean +0.6664%, against 0.5296pp of RF ⇒ NET +0.137%. Worth 0.0000% in set (0/100 cases moved, identical positions) -- acting on that would have cost 0.38%. |
| M80 tier | 🟢 keep. +1.3463% standalone in set. My earlier "remove it" read the substitution backwards: the twins are the redundant half only when M80 is absent. |
| thin pool (M80+twins+L137 off) | 🔴 **RED.** OOS quality cost -2.6588% (s1 -2.7613%, s2 -2.5563%) against in-set's -1.390%. Dominated: worse than beta at neutral, worse than the full pool at 0.68x. Its apparent +0.268% edge was in-set pricing of its own loss. |
| **thin pool + per-case GATED LP** | 🟡 **leading candidate, unconfirmed.** +2.551% vs beta at route-A-neutral, +2.731% at 0.68x. A thin pool costs ~52s so the whole 14.72 s budget stays free for the LP, which then fires on 45 of 100 cases instead of 30. RF side measured; **quality side is in-set and needs its own OOS pair.** |

## The one term nothing here can settle

**Route A has never run on the grader.** Beta was M73, which does not have it.
Its -32.2% at 48 real cores is a PROJECTION; on this box's 16 physical cores it
costs 2.9x; the ledger separately records it sinking every case to SA fallback
at 48 cores and hanging 600 s on a pre-L108 binary. This box and WSL are the
same CPU, so neither can measure it.

Consequence, stated plainly:
* **route A neutral → beta (M73) beats everything we have built**, except
  possibly the gated-thin candidate.
* **route A delivers → full pool + LP k=1 is rank 2 (0.88119).**

## If you have to ship tomorrow

1. `ba9ec1be…` (1678680) -- fully gated but carries the OLD deeper depth map
   (worse on this axis) and a 0-byte requirements.txt the 2026-08-23 report
   forbids.
2. `815d02da…` -- gated, requirements fixed, but still carries a net-negative
   depth map.
3. **Cheapest real improvement on either: set `ICCAD_SHAPE_LP_DEPTH2=0`
   equivalent (i.e. `_L157_DEPTH` all 1s).** +6.6pp of RF for -0.338% of
   quality, one constant, and two of the four gates (G2 kill switch, G3 map
   fired) already cover it.

## Not done

- Linux four lanes for any of this. `l187_wsl_verify.sh` is written with hard
  tar preconditions; invoke it as
  `wsl -d Ubuntu -- bash -lc 'bash /mnt/c/ICCAD_ml/ship_final/l187_wsl_verify.sh'`
  -- the bare form gets MSYS-mangled into `C:/Program Files/Git/mnt/c/...`.
- OOS confirmation of the gated-thin candidate.
- `l166_wsl_verify.sh` line 16 greps the tar for `_L157_NSET`, which has not
  existed since L165. It has printed `0 hits` in every Linux verify since.
