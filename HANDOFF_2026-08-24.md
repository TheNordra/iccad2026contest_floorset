# Handoff 2026-08-24 — where the ship stands and what to do next

## SHIPPED AND VERIFIED: `1678680` on `origin/l147-tangent-cut`

`build_submission/cadc1075.tar.gz`, 35.5 MB, 1424 files.
`op_wrapper.py` md5 **`ba9ec1be898b00ddbc41207f70bb2bc1`**.
`bin/constructive_linux` md5 `bc9912072cd97b45b47a03adec7170ce` — **unchanged**;
everything below is pure Python, the ELF was never rebuilt.

Contents beyond the band that ships today:
1. **L147 tangent cuts, by CODE DEFAULT** (`_L147_R/G/TOL/PRICE = 1.5/1.10/0.006/1.0`).
   It had to become a default: the grader strips `ICCAD_*`, so an env-only
   mechanism is inert in the package however green it measures.
   Kill switch `ICCAD_SHAPE_LP_L147=0`.
2. **L165 per-case LP depth map** (`_L157_DEPTH`, 1–3 passes keyed on block
   count), built from the CONTEST-PUBLISHED per-case medians.
3. **Vendored scipy** (`vendor/`, the official cp313 manylinux wheel) used only
   if `import scipy` fails, appended to `sys.path` so a system scipy always wins.

### Gates, all green
- in-set: determinism 100/100 cost AND positions; kill switch == pre-L147 band
  100/100; pass histograms identical across runs `{3:66, 2:24, 1:10}`;
  quality +0.6099% vs the L147 k=1 anchor, 74 moved, **0 worse**, 100/100 feasible.
- Linux (WSL2 32c, scipy 1.18.0), four lanes PASS: shipped default
  **1.1943200373961047** = +5.2313% vs pre-LP, **+2.7167% vs the band**,
  determinism 100/100, passes `1:11 / 2:24 / 3:65` identical across runs.
- ❌ `l113_ship_gate --cores 48` has NEVER RUN — every compiler on this box exits
  1 with empty stdout and stderr, an OS-level block unrelated to any change.
  The grader's path is the bundled ELF with no compile, covered by the Linux lanes.

## WHERE WE STAND

Beta leaderboard (`beta_leaderboard.csv`): **we were rank 6 of 10**.
**The gap is 100% quality** — our cost-weighted RF 0.70004 is the best
obtainable, and rank 1 PAYS 0.82354 (slowest in the top 10) yet wins on raw
1.0027 against our 1.3207.

    OOS NET   s1 +3.0583%   s2 +3.0751%   ⇒ total 0.8962 ⇒ RANK 4
    rank 4 threshold 0.8993   rank 3 threshold 0.8932   ⇒ ~0.33pp still missing

## THE THREE THINGS THAT MAKE OLD LEDGER VERDICTS UNSAFE

1. **The RF pricer is the contest-published per-case medians**
   (`C_median_runtimes_beta_hidden.csv`, read by `l146_rf_price.py`). It
   reproduces the graded total to 2.4e-7. `m67e_rf48.py`'s kappa = 3.1612 is a
   back-solve from one aggregate, wrong per case by up to 8x — CLAUDE.md still
   calls it definitive.
2. **Runtime is not the constraint.** 1.73x headroom, measured, and PER CASE
   (min 0.96x, p50 1.74x, p90 2.63x, max 3.91x). Local LP seconds ÷ f = 3.17.
3. **beta's 52.07s contains no LP** — the graded package (M73, `7f38893`) has
   `_shape_lp` zero times; it landed 11 days later at `d0db1fb`. Any reasoning
   that splits 52.07s into "pool + LP" is circular.

## NEXT: FIND THE LAST 0.33pp

Highest-prior lens, because it is exactly how today's +0.35pp was found:
**hunt for estimates used where a measurement or published value is available.**
`_L157_NSET` used a fitted `M_hat(n)` while `r["med"]` sat in the same rows.
Candidates not yet audited: `_RH = 1.4` and `A_hat = 1.035*sum(area)` in
`_proxy_metrics`; `_LP_UTIL = 0.968`; the M45/M49/M50 band constants.

Also open: spend the free 1.73x by UNDOING the M42/M45/M49/M50 cuts, which all
traded quality for wall; re-check L140's boundary finding on the current tree;
widen the profile pool (the wall is max-setter bound, `c*` p50 19.3, so under
~23 profiles it does not grow at all — M80's cut at K=8 may no longer bind).

⚠️ Rank by **gain ÷ gates that must re-run**, not by gain. Every touch of the
shipped path costs three in-set gates + restage + four Linux lanes, ~1.5 h, and
the deadline is 2026-08-28.

🔑 **arm-mixing is EXACT** — mixing flat k-arms per case reproduces a really-run
gated arm 100/100 on cost AND positions, so candidate gates can be scored
offline from the committed flat arms with no new 100-case runs.

## OPEN QUESTION FOR THE ORGANISERS (worth 5.4%)

Two official documents conflict. Beta guidelines §2: leave `requirements.txt`
empty, the environment provides numpy/torch/**scipy**/numba/tqdm/shapely/
threadpoolctl. Beta evaluation report §2(a): "Do not assume any package beyond
the Python standard library is available", naming scipy. Our beta package cannot
settle it — M73 imports scipy zero times. Blocking scipy and running the real
evaluator costs **5.4171%** (1.191977686767963 → 1.260246745790688) and used to
be completely SILENT. It is no longer: the optimizer prints
`[scipy] source=system|vendored|absent` on every path, `l117_linux_verify` FAILS
if the marker is missing or says absent, and the organisers return our stderr.
Also unresolved: whether a conditionally-used 35 MB `vendor/` counts as an
"unused large binary" under the DQ rule.

---

# Session 2 addendum — 2026-08-24 late

## SHIPPED, unchanged: `1678680` + `8432d15` (the latter is probe/label only)

Nothing in the shipping path moved after `1678680`. Package identity
`op_wrapper.py` md5 **`ba9ec1be898b00ddbc41207f70bb2bc1`**.
**NET s1 +3.0583% / s2 +3.0751% ⇒ projects to rank 4.** Rank 3 needs ~0.33pp more.

## Two corrections to numbers quoted earlier — use these, not the old ones

1. **Free runtime budget is 19.79s, not 38s.** The 52.07s beta baseline predates
   the shape LP by eleven days, so 37.99s was the budget *before* L147 and the
   depth map spent 18.23s of it. Per-case slack p50 1.74x → **1.31x**; the
   heaviest cases are nearly out (n=112 1.00x, n=108 1.03x, n=106 1.04x).
   The 1.73x aggregate multiplier was **never spendable** — applied uniformly it
   prices at −1.9540%, because the aggregate hides that the low-slack cases are
   the heavy-weight ones.
2. **The REFINE band constants are dead on the shipped path.** `_l137_env()`
   injects `ICCAD_HINT_REFINE=4` into every profile at ≥40 cores and
   `constructive.cpp:2142` clamps `REFINE_ITERS` to it — verified on HEAD across
   all n and all 51 profiles, effective `REFINE_ITERS = {4: 51}`, zero
   exceptions. That is why L148 read the band-cut restore as +0.0000%. They are
   NOT dead below 40 cores, so **label, do not delete**. Not done yet — no
   tracked edit this close to the deadline unless the label is the only change.

## Verdicts reached this session

| item | verdict |
|---|---|
| 32-profile probe tier (`ICCAD_LENSD_POOL=1`) | 🔴 **RED** — quality real (OOS +0.3383% / +0.5501%, transfer 75%/122%) but costs 63–75 grader-seconds against 19.79s |
| LP depth k=3 flat | 🔴 superseded — the per-case depth map already takes k=3 wherever it fits |
| soft-violation tolerance artefacts | 🔴 CLOSED — every violation has metres of room; the deficit is placement, not arithmetic (re-confirms L140) |
| L152 LANE D | 🟡 **open**, see below |
| LP hpwl-baseline predictor | 🟡 **open**, OOS running as `l170_oos_s{1,2}_hb.json` |

🔑 **THE REUSABLE RULE FROM THE PROBE TIER:** every added profile costs ~71 ms of
**serial** `_proxy_metrics` on the main thread (M47: 2.9s for 41 profiles at
n=120), so N profiles cost at least `N * 0.071 / f` grader-seconds **regardless of
core count** — M47 also records that parallelising the proxies was 4x WORSE (GIL).
"Adding profiles is nearly free" stopped being true at M47. This also explains
M80's K=8: 8 profiles ≈ 0.57s, which the budget carries; 32 is 2.3s, which it
does not. **Apply this before running anything on that axis.**

## The two still open

**L152 LANE D** (`constructive_l152.cpp`, R2 `ICCAD_BND_FRAME_ITEM` + R3
`ICCAD_BND_SNAP_BEST`, both default-0 and off-path verified 510/510).
OOS s1 +0.2769%, s2 +0.1877%, mean +0.23% — **sub-bar** against the
pre-registered 0.30%. Wall 1.029–1.037x re-priced on the *current* clock is
−0.0402% / −0.0600% ⇒ **NET +0.13% to +0.24%**. (L152_REPORT.md's own
"~1.03x ≈ −0.005%" is the pre-L147 clock and understates it 12x — same
stale-baseline class as the M_hat bug.)
✅ **The toolchain blocker is cleared**: a fresh WSL `-O3 -static-libstdc++
-static-libgcc` build of `constructive.cpp` needs **GLIBC_2.38** against Debian
13's 2.41 (the shipped ELF needs 2.34). PyInstaller died at 2.43 > 2.41; the C++
binary clears it.
⚠️ Cost of shipping it: a `constructive.cpp` change forces an ELF rebuild, which
invalidates `audit_cache*.pkl`, `l125_dt_cache.pkl` and **every bit-equality
gate**, and "new cpp + old ELF" is this project's most-recorded near-miss
(L124, L136, route A). Decide whether +0.13–0.24% is worth that with 4 days left.

**LP hpwl-baseline predictor** (`ICCAD_LP_HB_PRED=0.2994`, ported into the tree,
default OFF). The shape LP scores against OUR layout's hpwl while the evaluator
scores against its own baseline — the same error `_LP_UTIL` already refuses to
make on the area side. Our hpwl is 1.238x the evaluator's baseline, so HPWL is
weighted 1.24x too low against area, and hpwl carries 10.15% of the deficit.
In-set **+0.0772%** (96 moved / 31 worse / 100/100 feasible).
**Cost measured in-process: LP 57.79s → 59.04s = +1.25s (1.02x) = 0.4
grader-seconds** against 19.79s of budget. The lens that proposed it read
"1.93x" from whole-eval wall under contention — **the third time this session
that cross-run differencing produced a wrong LP cost**; always use
`ICCAD_LP_TIMING`.
⏳ OOS running when this was written: `l170_oos_s{1,2}_hb.json`. Control is the
shipped depth map, reconstructible **exactly** by arm-mixing, so only the arm
needs running. If OOS holds near +0.08–0.10% it is cheap enough to ship.

## Unresolved risk, unchanged

The whole banked stack is gated on `os.cpu_count() >= 40`. The organisers' docs
say 48 cores twice (guidelines "up to 48 cores", FAQ A2/A3 "ICELAKE CPU with 48
cores") but a containerised report of <40 would silently ship the pre-LP
configuration and nothing in the package would say so. Cheapest mitigation:
one stderr line reporting detected cores and which tiers fired — the organisers
return our stderr (beta's `eval_op_wrapper.log` carries the tqdm bar, which is
stderr). Not done: it forces a full gate re-run for information that helps the
*next* round, not this one.

`l113_ship_gate --cores 48` has still never run — every compiler on this Windows
box exits 1 with empty output. WSL compiles fine, so the gate could be adapted
to run there, or a teammate with a working g++ can run it as-is.
