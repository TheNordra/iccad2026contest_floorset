# M83 — 擁塞斜率實測：**wall 模型的 dt-invariance 假設證偽**；tier-5 轉虧、route A 轉強

日期：2026-08-02。🔴 車道（產出是牆鐘時間）。送件形零改動、`_PROFILES` 與池常數未碰。

## 一句話

wall 模型 `W = max(max_i dt_i, Σdt_i/cores, Σpt_i)` 假設「逐隻 dt 在並行下不變」。
**把它量了：即使 35 隻全落在不同實體核上（零 SMT、零超額）也慢 10%，SMT 共用下慢到 1.53×。**
這個假設是 M67-E「+0.00%」、G1 tier-5 判定、G2 route-A 上界三者共同的地基。
重新定價後：**tier-5 由賺 −0.4% 轉為虧 +2.6~+11.2%；route A 由 −4.5~−14.3% 變強到 −8.1~−20.7%。**

## 1. 為什麼要量這個

R1（`m80_rf_remeasure.py --force-cores 48`，20 次取逐案最小值）把池強制成評分機組態：

```
20 個大案 (n>100)：13 隻池 → 35 隻池
  實測 wall 比：中位 1.325x  最壞 1.690x   20/20 全變慢
  模型   預測：中位 1.000x  最壞 1.142x   只 6/20 過 2%
```

模型與實測差這麼多，只可能是模型的某個假設錯了。唯一可疑的就是 dt-invariance。
但 R1 的數字**不能直接當評分機值**：本機 16 實體/32 邏輯，35 個行程是 2.2× 超額訂閱，
評分機 48 核跑 35 隻不超額 —— **不同 regime**。所以改量**可外推的斜率**。

## 2. 方法

- 出貨組態 env：`oc._PROFILES[k]` + `oc._M71_ENV` + n>100 疊 `REFINE_ITERS=4`
- 對象：`audit_cache_m71_kband.pkl` 選出的 48c 池 **max-setter**（87/#17、99/#7、93/#19）
- k 份**相同拷貝**、`threading.Barrier` 同時釋放、每點 3 reps 取中位
- **不釘核**

> ⚠️ **對 `m67f_contention_probe.py` 的方法學更正**：那支釘核到 logical `0..k-1`。
> Windows/AMD 的 logical 編號 SMT 相鄰 ⇒ 釘 0..15 其實只用 ~8 個實體核，
> **會假造出提早的擁塞**。出貨 wrapper 不釘核 ⇒ 讓 OS 依拓撲鋪開才是 production-shaped。
> 兩支在 k≤8 吻合（1.018 vs 1.02），差異只在 k>8。

## 3. 結果（三案一致）

| k | dt(k)/dt(1) | 區間 |
|---:|---:|---|
| 4 | 1.01 | |
| 8 | 1.02 | |
| 13 | 1.06 | 現行本機池 |
| **16** | **1.10** | **全在不同實體核、零 SMT、零超額** |
| 20 | 1.22 | SMT 開始 |
| 24 | 1.33 | |
| 28 | 1.42 | |
| **32** | **1.53** | SMT 全共用、仍未超額 |
| 35 | 1.61 | 超額（本機限定）|

**實體核區間斜率 = 0.71% / 每多一個並行者。**
陡的一段起點正好在 **SMT 邊界**；超額那段（32→35）只多 0.08
⇒ 主因是 **SMT 共用 + 記憶體頻寬**，不是超額訂閱。

## 4. 重新定價

品質側基準 `audit_cache_m71_kband.pkl`（模型鏈已由 R1 逐位驗證，見 §6）：
tier-5 ON 1.295548 / OFF 1.305390 ⇒ tier-5 買到 **−0.754% 品質**。

### 4a. tier-5（**已出貨、評分機會觸發**）

| 評分機拓撲 | wall 倍數 | s=1.5 | s=2.0 | s=2.5 |
|---|---:|---:|---:|---:|
| 48 實體核（外推）| ×1.176 | **+2.57%** | **+3.94%** | **+4.55%** |
| 24 實體/48 邏輯 | ×1.446 | **+7.59%** | **+10.38%** | **+11.25%** |
| 原模型（dt 不變）| ×1.000 | −0.52% | −0.42% | −0.34% |

（正 = 虧）**兩種拓撲假設下都是淨虧。**

### 4b. route A（逐外框平行化）

| 評分機拓撲 | wall 比 | s=1.5 | s=2.0 | s=2.5 |
|---|---:|---:|---:|---:|
| 48 實體核 | 0.469 | **−8.10%** | **−13.84%** | **−17.35%** |
| 24 實體/48 邏輯 | 0.436 | **−12.30%** | **−17.87%** | **−20.71%** |
| 原模型 | 0.453 | −4.45% | −9.47% | −14.25% |

**route A 在三種情境下都贏，而且擁塞讓它更值錢。**
機制：現行 wall 是 max-setter-bound（機器只用 45%），擁塞把這個浪費放大；
route A 是 Σ-bound，基準變差反而放大相對增益。

⇒ **route A 的價值對擁塞不確定性穩健；tier-5 的判定完全押在它上面、符號會翻。**
（一致性檢查：把擁塞同時套在 route A 的 `max dt/K` 與 `Σ/48` 兩項，數字不變
——因為 `Σ/48` 對 94/100 案是 binding 項。）

## 5. 誠實範圍

1. **🚨 本機是行動版 AMD、雙通道 DDR5**；伺服器 Xeon 是 8 通道、L3 大得多
   ⇒ **擁塞斜率在評分機上可能平很多**。若接近「原模型」列，tier-5 回到約略中性。
   **本報告不主張「tier-5 正在虧錢」**，只主張 **`dW=+0.00%` 這個論證已死**
   （它要求擁塞恰好為零，而零 SMT、零超額的 k=16 都有 1.10×）。
2. **48 實體核那一欄是線性外推**（k≤16 的 15 點外推到 34 個並行者，超出本機實體核數）。
   頻寬飽和曲線通常是凹的 ⇒ 1.243 偏樂觀的機率較高。
3. **評分機是 48 實體還是 24×2 未知**（`M67E_REPORT` 只記「48c ICELAKE」）。
   這決定要看哪一列，差 +4% vs +11%。Beta 的 `lscpu` 或逐案 runtime 可收斂。
4. **相同拷貝 vs 異質池**：實測用 k 份相同 profile；實際 pool 是 35 隻**不同** profile。
   資料相同、程式路徑不同，擁塞量級應相近但未驗。
5. route A 的 K=4 假設「4 個外框各佔 1/4」未逐外框拆帳（儀裝版只印 pipeline 總和）
   ⇒ 若某個外框特別重，實際加速小於 4×。

## 6. 模型鏈驗證（為什麼品質數字可信）

`m67e_rf48.py --m71 --kband` + `pool_shipped()` **逐位預測到兩種池組態的實測總分**：

```
強制 48 核池 total = 1.295547821428   模型 1.295548  ✓
預設 32 核池 total = 1.305389893451   模型 1.305390  ✓
```

⇒ 錯的只有 **wall 模型的 dt-invariance**，品質側無誤。
（`--m71 --kband` 是本輪新加的模式，純附加、預設 off、獨立快取；
兩道自我閘門：預設 full-41 = 1.324846 逐位不變、新模式 = 1.292657 = `profile_audit` 自報值。）

## 7. 檔案 / 復現

| 檔 | 說明 |
|---|---|
| `m83_contention_slope.py` | `combos` / `sweep`（永不 ship）|
| `results_M83_contention_slope.json` | 逐案逐 k 的 mean/max/ratio |
| `m80_rf_remeasure.py` | R1 的 `--force-cores 48`（永不 ship）|
| `results_M80_rf_remeasure_c48.json` | 評分機池 20 次逐案 runtime |
| `M83_TIER5_CONTENTION_NOTE.md` | 給組員的通知（tier-5 已出貨、需要他們判斷）|

```bash
PY=".../envs/floorset/python.exe"
"$PY" m83_contention_slope.py sweep --combos 3 --reps 3    # ~3 分
"$PY" m80_rf_remeasure.py run --repeats 20 --force-cores 48  # ~45 分
```

⚠️ Windows：跑出貨包前 `PATH` 要加 `C:\msys64\ucrt64\bin`，否則靜默退回 Python SA。
