# L115 A0 — 形狀 LP 凍住的 25.2% 值多少？（2026-08-10）

依核准計畫的 🅐 主軌第一步：**先量天花板再開工**（M63 紀律）。工具 `l115_shape_ceiling.py`。

`reshapeable()` 用兩個 `continue` 鎖死 25.2% 的面積：
```python
if len(mem) != 1:   continue     # cluster component  22.9%
if cn[i][2] > 0:    continue     # MIB member          2.3%
```

量法：讓被鎖的類別自由變形，回報
`ceiling_i = (1 + 0.5(hpwl_gap + area_gap)) · exp(2·vrel_anchor)`
—— 用**新的 hpwl/area** 配**錨的 vrel**，也就是「若有一個合法機制能達到同樣的形狀」的上界。
raw（含 contiguity 損害）一併印出，不藏。

## 結果

基準：出貨組態 `1.238989336`（M80 錨 `1.266623425`，LP k=1 已賺 +2.18%）。

| arm | ceiling（錨 vrel） | vs shipped | 預註冊判定 |
|---|---|---|---|
| MIB only | 1.238167 | **+0.0664%** | **RED** |
| cluster only（自由） | 1.211803 | **+2.1943%** | GREEN |
| both（自由） | 1.210005 | **+2.3393%** | GREEN |
| **cluster_guarded（合法）** | 1.238995 | **+0.0709%（raw）** | **RED** |

加權 gap：area `0.1483 → 0.1035`、hpwl `0.2414 → 0.2261` ⇒ 獎品幾乎全在 **area/utilisation**
（呼應 L95「我們 82.2% vs label 96.6%」）。

## 🚨 更正：上面那張表是「單步」上界，不是形狀軸的上界

`--iters` 掃描證明 1.2100 是 **rho=6% 單步線性化**的產物，不是軸的極限：

| iters | both（自由變形） | **shipped（什麼都不解凍，合法可達）** |
|---|---|---|
| 1 | 1.210005（+2.34%） | 1.236783（+0.18%） |
| 4 | 1.166167（+5.88%） | 1.207226（+2.56%） |
| **12** | **1.148619（+7.29%）** | **1.194109（+3.62%）** |

⚠️ `arm=shipped` 那欄的 raw **是合法的** —— 該臂用出貨的 decompose，cluster 仍成組、contiguity
守門全程生效。（工具原本一律印 "NOT achievable"，只有解凍臂才對，已修。）

**兩個直接後果：**
1. **k=12 就到 1.194，已在 1.2 以下，而且不需要任何新機制**（`ICCAD_SHAPE_LP_ITERS` 早就是旋鈕）。
   area_gap `0.1483 → 0.0690`。
2. ⇒ **瓶頸不是品質，是時間**：k=12 要付 ~12× 的 LP 時間，RF 付不起（L114 §1 的 k 曲線）。
   問題從「找新的品質機制」變成「用更少的時間拿到 k→∞ 的解」。

形狀軸的真天花板（全解凍、k=12）是 **1.148619**，離 label floor 1.1079 已經不遠。

## 三個結論

**1. MIB 那半不用做。** +0.0664%，遠低於 0.5% 的 AMBER bar。計畫裡的 A1 取消。

**2. cluster 的 +2.19% 是靠破壞 abutment 換來的，既有守門會全數退回。**
`cluster_guarded` = 讓成員各自成 singleton unit，但**保留 group 記帳**，於是 `lp_pass` 既有的
「component 數變多就 `freeze |= group_units[g]` 重解」迴圈仍然生效——這是**合法機制**，不是上界。
它只拿到 **+0.0709%**（97/100 案解出，3 案 3 次嘗試後仍 cluster_break）。
⇒ LP 的最佳解幾乎**總是**破壞貼合；能保住貼合的那些形狀改動幾乎不值錢。

要拿到那 +2.19%，唯一剩下的形態是**在 LP 內顯式維持 abutment**（成員形狀必須 slicing-consistent
地一起變），也就是計畫 A2 的變體 3——工作量最大，而且正對著 M10 精度牆
（ledger「cluster-rigid pack/slide」條：任何移動 cluster 成員的後處理都撞這道牆）。

**3. ~~即使做到滿，形狀軸也到不了 1.2~~ —— 這句錯了，見上面的更正。**
1.2100 是單步上界。走到 k=12，**什麼都不解凍就有 1.194109**，全解凍的天花板是 1.148619。

## 對計畫的修正

- A1（MIB）**取消**。
- A2（LP 內顯式 abutment）保留為第二順位：k=12 下它值 1.1941 → 1.1486，約再 +3.8%。
- **目標改寫**：「< 1.2」在品質上已經有解（k=12 → 1.194，零新機制），
  真正要解的是**如何用 ~1 次 solve 的時間拿到 k→∞ 的答案**。這把主軌從
  「找新品質機制」轉成「把迭代線性化換成精確求解」（見下一輪路線表）。

誠實範圍：所有數字用**離線 oracle baseline**（`c["base"]` 來自 label）量；出貨走 baseline-free
替代，k=1 實測保住 93%。k>1 的保留率**未量**，是下一輪必須先確認的事。


---

## R3 —— baseline 估計（已完成，已接進出貨樹）

出貨版原本用「選中佈局**自身**的 hpwl / bbox area」當 LP 目標的權重基準。問題：我們的 bbox 比
label 大 ~15%（utilisation 82.2% vs 96.6%），所以 **area 項被低估 15%** —— 而形狀 LP 的全部
上檔正好在 area。

改成 **`area_baseline = ΣA / 0.968`**（L95 的結構下限，**label-free 且逐案**）。

| base | k=1 | k=4 | k=12 | 對 oracle 的保留率 |
|---|---|---|---|---|
| oracle（label，不可送） | 1.236783 | 1.207226 | 1.194109 | 100% |
| own（原出貨） | 1.238989 | 1.213512 | 1.204992 | 92.6 / 89.4 / **85.0%** |
| **util（新）** | **1.236792** | **1.206789** | **1.193893** | **100.0 / 100.7 / 100.3%** |

`--util` 從 0.85 掃到 1.05，六個值全部落在 1.23678 ±6e-6 ⇒ **不是刀鋒、不是擬合旋鈕**，
關鍵只是「別再用我們自己那個過大的 bbox」。

**出貨路徑實測**（`ICCAD_ADAPTIVE_CORES=48`、LP 開、全 100 案）：

| | |
|---|---|
| 原（own baseline） | `1.238989335942891`（+2.1817%） |
| **新（structural floor）** | **`1.2367916697724892`（+2.3552%）** |
| feasible / regressions / LP 例外 | **100/100 / 0 / 0** |
| avg cost | 1.27399 → **1.2695** |

離線 `util k=1` 預測 1.236791670，實測 1.2367916697 —— **吻合到小數第 8 位**，
證明 l115 的 `--base` 路徑是出貨路徑的忠實模型。

⇒ **R4（k>1 的保留率）同時被解掉**：用 own 是 92.6→85.0% 衰減，用 util 是全 k ~100%。

## 下一步：R1

`util k=12 = 1.193893`（**< 1.2**）。品質已經有解，剩下的純粹是**時間**：k=12 要 12× 的 LP 時間。
R1 = 用精確 sizing（GP / Lagrangian relaxation on 同一組 constraint graph，
[Young/Chu/Luk TCAD'01](http://www.cse.cuhk.edu.hk/~fyyoung/paper/tcad01_5.pdf)）
在 ~1 次 solve 內拿到 k→∞ 的答案。
