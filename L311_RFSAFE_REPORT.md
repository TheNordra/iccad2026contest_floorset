# L311 — 「免費的那一半」是真的免費，而且它是唯一全區間為正的臂

接 [L310_ALIGN_D_VS_MIX.md](L310_ALIGN_D_VS_MIX.md)。合併了 `ICCAD LP Gate 品質量測`
與其 fork 兩個 session 的內容。**沒有動任何出貨物件。**

## 0. 先回答「我們做過了嗎」

**做過了 —— L298 就是這件事。** 我提議的「把 mix 增益裡等牆鐘的那一部分切出來」，
`l298_selective_ungate.py` 的 **RF-SAFE** 臂已經精確地做了：

> ungate n **iff** 該案增加的 grader 時間塞得進它自己到 RF floor 的 slack。
> 用**零品質資訊**挑，所以沒有東西可以過擬合（L157 的形狀）。

它的 RF 帳單 **+0.0000%**，但那是**構造出來的恆等式，不是證據**——只在「挑它時假設的
那個 f」上成立。而 L298 用的是 **F = 3.17**，**L308 後來把 f 量成 2.38–2.84**。

⇒ 沒做過的是**在修正後的 f 下重驗**，以及「用一個 f 挑、用另一個 f 付帳」的交叉定價。
這份補上了。

## 1. 交叉定價：挑的時候樂觀，付帳的時候悲觀

`l311_rfsafe_robust.py`。列 = 挑子集用的 f，欄 = 實際拿到的 f：

```
 pick at   n | pay at 3.17 | pay at 2.84 | pay at 2.61 | pay at 2.38
    3.17  13 |  RF +0.0000% |  RF +0.0012% |  RF +0.0022% |  RF +0.0649%
    2.84  11 |  RF +0.0000% |  RF +0.0000% |  RF +0.0000% |  RF +0.0616%
    2.38   8 |  RF +0.0000% |  RF +0.0000% |  RF +0.0000% |  RF +0.0000%
```

對角線是恆等式（+0.0000%，不算證據）。**對角線以下才是測試**，而它通過了：
用最樂觀的 3.17 挑、在最悲觀的 2.38 付帳，帳單只有 **+0.0649%**。
對照全開（ALL 29）在同一段 f 上是 **+0.9726% → +1.8494%**（擺幅 0.88pp，**13 倍**）。

## 2. 把 median 軸摺進來 —— 這是判定

L310 證明 f 與 m 只以乘積出現。所以把 f 軸往下延伸就等於「medians 也縮了」：
`m = 0.742`（唯一觀測到的那次漂移）配 `f = 2.83` 落在 **f_eff ≈ 2.10**。

**vs D 的 delta（正 = 比 D 好）**

```
arm                            3.17     2.84     2.61     2.38     2.10     1.90     1.77     1.60
RF-SAFE pick@3.17 (n=13)     +0.968   +0.966   +0.965   +0.903   +0.748   +0.599   +0.488   +0.318
RF-SAFE pick@2.38 (n= 8)     +0.423   +0.423   +0.423   +0.423   +0.402   +0.369   +0.344   +0.306
ALL 29 full gate0            +1.558   +1.274   +1.038   +0.703   +0.147   -0.348   -0.721   -1.285
                                                                          ^^^^^^^^^^^^^^^^^^^^^^^^
                                                                          全開在這裡翻負
```

**RF-SAFE 在整個聯合不確定區間都是正的，而且都在 0.30pp 出貨門檻之上**
（最差 +0.318% / +0.306%）。**全開的 gate0 在 f_eff ≲ 2.15 就翻負**，
而 `mix`（gate + depth）比全開更激進，翻負只會更早。

⚠️ 三條線的**絕對水準**各自不同（本框架 D = 0.878564、mix 線 0.87511、l309 ~0.900），
因為 bridge 與投影方式不同。**能比的是 delta，不是絕對值**——上表全是 delta。

## 3. 建議升級

L310 說「出 D」。現在可以更好：

> **出 D + RF-SAFE ungate（13 個 block count，用 f=3.17 挑的那組）。**

理由：

1. **它是桌上唯一在整個 f·m 區間都為正、且都過 0.30pp 門檻的臂。**
2. **它是 wrapper-only 改動**——`_L196_LPGATE` 就是一個 dict。
   `constructive.cpp` 與 `bin/constructive_linux` 不動 ⇒ **不需要 Linux 重建**。
3. **它不吃我們看不見的那個數字。** 曝險 0.064pp，全開是 0.855pp（13 倍）。
4. 想再保守一點就用 f=2.38 挑的 8 個：**曝險 0.000pp**，全區間 +0.31~+0.42%。

`mix` 仍然留在 `build_submission.MIXD/` 當候選。它的品質是真的，但它把
RF-SAFE 白拿的部分和一大段要賭 f·m 的部分綁在一起賣。**RF-SAFE 是可以單獨買的那一半。**

## 4. 還沒做的（出貨前必要）

RF-SAFE **只在 in-set 與投影上驗過**，還沒有走過 mix 走完的那條鏈：

* [ ] 產 `_L196_LPGATE` 的 RF-SAFE 版本、`make_submission.py stage`
* [ ] Windows 48c 逐案：對 D **0 regressions**、positions 逐位（閘外的案子必須不動）
* [ ] Windows 預設核數：**必須與 D 逐位相同**（≥40 核閘之下惰性）
* [ ] Linux 五車道
* [ ] `l246_compliance.py` 20/20
* [ ] OOS s1 / s2 —— 這一項最重要：**挑那 13 個 block count 用的是 in-set 的 dt**，
      雖然 RF-SAFE 不看品質（所以不會過擬合品質），但**它看 dt**，而 dt 是量測值。
      L308 記過「另一 session 平行跑汙染重帶」——**dt 要用 min-of-N 重量一次**。

## 5. 從 `ICCAD LP Gate 品質量測` session 合併進來的事

那條線在做 mix 的收尾時弄壞又修好了一顆 cache，過程中**意外把一個既有問題暴露出來**：

| | |
|---|---|
| `audit_cache.pkl` | 4200 → 2500（弄壞）→ **4200（已修**，`profile_audit.py base`，369 s） |
| `audit_cache_ship.pkl` | 4200，**沒動過** |

**閘的失敗性質變了**（這才是重點）：修好之前 `m49` 三個閘因為簽章失效**根本沒執行**，
只是印 `cache profile signature != current pool`；修好之後它們**真的跑起來**，
`control EXACT 100%`（對照組完全正確），然後掛在 movers 清單上：

```
big : movers [83, 86, 87, 94]                 vs 釘住的期望 [87, 94]
mid : movers [54, 61, 62, 67, 72, 77]         vs 釘住的期望 [48, 51, 54, 61, 62, 67, 72]
```

⇒ 這正是 CLAUDE.md「下一步 2」記的 **drop 常數過期**（隊友動過三次 `constructive.cpp`
都沒重推）。**不是 mix 造成的、也不擋 mix**，但 `README_MIX.md` 把它記成
「pre-existing, 4 failures, signature」現在**只對了一半**：簽章問題已經修掉，
剩下的是**內容問題**，而且現在看得見了。

`rf_score_model` 仍紅，因為它讀 `audit_cache_ship.pkl`、那顆簽章還是舊的。
補它要 `profile_audit.py ship`（6–11 分、**不改常數**）；真正修好要
`ICCAD_REGEN=1 rf_score_model.py`，**那會改出貨常數、整條送件鏈要重走**。
距 8/31 剩三天 ⇒ **建議不要動常數**。

順帶：那條線把樹上的 `optimizer_constructive.py` 記為 `13c629f2…` —— 我複驗過，
**那顆 md5 已經包含我的絕對路徑 patch**（`grep` 兩處皆 0），兩邊沒有衝突。

## 6. fork session（研究線）的結論，與出貨無關但會影響下一輪

`ICCAD LP Gate 品質量測 (fork)` 已經轉去做生成器/ML 研究，結論值得記：

* **1M 公開資料與 validation 的 netlist 分布不同**：p2b edges/block **6.16×**、
  p2b `u|connected` **2.38×** ⇒ 任何吃 netlist 的模型會過度信任 pin 通道。
  形狀通道不讀 netlist ⇒ **免疫**（1M 學的規則 held-out 50.82%，in-sample 52.40%）。
* 但 **51% 不夠**：學到的形狀只回收「最方 → label 形狀」缺口的 22%，面積幾乎沒動。
* ⇒ 他們把 Tier 1 的 ② netlist 反推**降級**、① 重放生成器（不讀 connectivity ⇒ 免疫）**升為首選**。
* 🔑 這解釋了學長「in-set 好、beta 不好」的機制：**資料的問題，不是模型的問題**。

## 6. 重現

```bash
python l298_selective_ungate.py 2.38     # F 現在可覆寫（預設仍 3.17，舊數字全部照舊）
python l311_rfsafe_robust.py             # 交叉定價
```
