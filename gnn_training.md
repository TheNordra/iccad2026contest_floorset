# ICCAD 2026 FloorSet Challenge - GNN 訓練與維護指南 `gnn_training.md`

## 🎯 檔案定位與交接說明
本文件專為接手的後續維護人員（包含 AI 代理工程師/Agent）編寫。旨在清晰記錄 **ICCAD 2026 FloorSet Challenge（晶片平面佈局優化）** 專案中，基於圖神經網路（GNN）的全域佈局（Global Placement）訓練核心邏輯、現狀、瓶頸及後續演進路線。

---

## 📋 一、目前狀態 (Current Status)

### 1. 模型與演算法架構
* **核心模型**: `FloorplanNet` (基於 PyTorch 實作的圖神經網路)。
* **特徵融合層**: 接收每個 Block 的目標面積（Area Target），並透過引腳連接矩陣（`p2b_conn`）與引腳位置（`pins_pos`）計算出每個 Block 受到外圍 Pins 的平均引力中心座標，融合成初始節點特徵。
* **圖卷積機制 (GCN)**: 建立 Block-to-Block 的連線相鄰矩陣（`b2b_conn`），對相鄰矩陣進行度矩陣歸一化（Row-sum Normalization），經過兩層 `Linear + ReLU` 進行圖特徵傳遞，捕獲電路拓撲的互連關係。
* **物理邊界與硬性鎖定**:
  * **座標鎖定**: 輸出端透過 `Sigmoid` 激活函數強行乘以 `300.0`，確保預測的 $(x, y)$ 座標永遠被鎖定在 $300 	imes 300$ 的畫布邊界之內。
  * **長寬比與面積鎖定**: 模型不直接預測 $W$ 與 $H$，而是預測長寬比的對數值（`log_ratio`），透過 `Tanh` 限制長寬比在 10 倍以內。接著結合大賽給定的絕對面積（Area），利用數學公式反推真實的 $W = \sqrt{\text{Area} \times \text{ratio}}$ 與 $H = \sqrt{\text{Area} / \text{ratio}}$。**此舉實現了晶片物理面積 100% 的精準度**。

### 2. 訓練基礎設施與效能
* **硬體滿載優化**: 採用 `Batch Size = 4` 的批次加速，利用資料解包優化，使 NVIDIA GeForce RTX 3060 Ti 的 3D 計算核心達到 **100% 滿載**。由於矩陣高度精煉，視訊記憶體（VRAM）佔用穩定保持在極低的 **1.5GB / 8.0GB**，GPU 運作溫度保持在 **44°C** 的極佳狀態。
* **防禦型存檔機制**: 引入 Checkpoint 系統，訓練過程中每過 20 個 Batch（約 15 分鐘）會自動落袋為安寫入硬碟（`floorplan_gnn_checkpoint.pth`），完全規避了中斷崩潰的風險。
* **視覺化 Bug 修復**: 已徹底解決因 Batch Size 放大導致的繪圖維度不匹配問題（`RuntimeError: expand...`）。目前繪圖模組在評估模式（`model.eval()`）下，會精準抽取 Batch 內 Index 0 的單筆電路資料進行降維與幾何還原，成功產出佈局圖 `predicted_floorplan.png`。

### 3. 最新訓練成果對比
* **對比基準 (Baseline)**: 早期白噪聲階段 Loss 高達 **21.1794**，方塊在視覺上呈現「千層派狀態」——全部死死堆疊在左下角 $(0,0)$ 附近，僅盲目滿足了連線長度（HPWL），完全無視重疊。
* **當前最佳權重 (Latest Run)**: 歷時 1.5 小時的 500 筆資料集充分訓練，單筆驗證 Loss 成功壓低至 **3.0653**。方塊在視覺上呈現出優良的「滿天星狀態」——**方塊成功感知到重疊懲罰，在畫布上大幅度散開，並開始順應周圍紅叉叉（Pins）的幾何引力進行合理的區域定位。**

---

## ⚠️ 二、這個階段想解決的問題 (Problems Addressed)

1. **可微分性與大賽真實分數的鴻溝**: 
   大賽最終排名使用的 `Total Score` 含有極端殘酷的硬性約束（例如：只要有 1 平方微米的方塊重疊，就會被加上天文數字的懲罰分甚至直接判定 Invalid）。但這種斷崖式規則在數學上是不可微分的。因此本階段專注於利用官方提供的平滑可微分代理函數（`compute_training_loss_differentiable`），透過如 $exp(\beta \cdot \text{Overlap})$ 的平滑警報曲線，讓神經網路能夠計算梯度並進行反向傳播。
2. **全域佈局定位 (Global Placement)**:
   解決高維度複雜電路（單個 Sample 包含 300~400 個 Blocks）在隨機初始化時的混亂擺放。本階段旨在讓模型宏觀地學會：「哪些模組應該靠在一起（HPWL 最小化），且同時不能擠成一團（Overlap 懲罰最小化）。」

---

## 🎯 三、預期目標 (Expected Goals)

1. **Loss 的極致收斂與平滑化**:
   在 500 筆樣本的規模下，預期將全域佈局的可微分 Loss 穩定壓制在 **1.0 以下**。並解決當前在訓練尾端（如 Batch 120+）由於學習率固定而產生的微幅震盪反彈現象（從 3.X 反彈至 5.X）。
2. **拓撲聚合（Clustering）的視覺呈現**:
   預期在 `predicted_floorplan.png` 中，連線極為密集的 Block 簇（例如數據通路、暫存器堆）在宏觀上能自發聚攏成塊，而與邊緣 Pin 有強連接的 Block 能被精準拉向邊緣，且整體重疊面積降低至 15% 以下。

---

## 🚀 四、未來發展的方向 (Future Directions)

為了在 ICCAD 2026 排行榜上衝刺最高分，接班人員應從以下兩個維度進行並行迭代：

### 🛠️ 路線 A：深化神經網路潛力（優化 Global Placement）
1. **導入動態學習率衰減 (LR Scheduler)**:
   在 `optimizer` 後方加入餘弦退火（`CosineAnnealingLR`）或動態高原衰減（`ReduceLROnPlateau`）。當訓練進入中後期時，主動縮小模型的調整步伐，徹底消除尾端 Loss 震盪，逼近全局最優解。
2. **動態調整損失函數權重（Weight Annealing）**:
   在訓練初期，調大 HPWL（連線長度）的權重，讓方塊快速聚攏；在訓練中後期，動態拉高 Overlap Penalty（重疊懲罰）的 $\beta$ 係數，以極大的懲罰力道強迫神經網路在全域定位階段就把方塊「彈開」。

### 📐 路線 B：引入傳統 EDA 演算法進行後處理（Legalization 合法化）
這是衝刺大賽排名 **最核心、最決定性** 的下一步：
1. **承認 AI 的局限性**: 神經網路基於連續空間的優化，極難做到 100% 完美的「微觀零重疊」。
2. **建立 Legalizer（合法化程序）**: 
   接班人員需編寫一個獨立的後處理腳本。該腳本直接讀取由 `floorplan_gnn.pth` 所輸出的全域佈局座標（草圖），將其作為起點，隨後調用傳統 EDA 演算法：
   * **線性格子點彈開法 (Tetris-based Legalization)**: 像俄羅斯方塊一樣，將有微小重疊的方塊沿著固定方向推移到最近的空位上。
   * **序列對法 (Sequence Pair) / 擴散模型後處理**: 在固定方塊相對拓撲位置的前提下，強行撐開邊界，消除重疊。
3. **終極目標**: 只要透過 Legalizer 將這張「滿天星草圖」完全推開、達成零重疊，再送進官方的 `iccad2026_evaluate.py` 中，就能轉化為完美的真實 `Total Score`！
