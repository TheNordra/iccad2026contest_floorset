# L114 — route A + 形狀 LP 的聯合定價（2026-08-10）

接 `HANDOFF_2026-08-09.md` §4/§5.2 與 L112。做三件事：**把兩條路放進同一個模型**、
**用符合紀律的計時重量 tLP**、**跑部署形 100 案鏈**。結論比交接檔更好，但理由跟交接檔寫的不一樣。

---

## 1. 交接檔 §1 的「兩者相加」不成立 —— 但方向是**偏保守**

`HANDOFF_2026-08-09.md` §1 把兩條路的百分比直接相加。這不該成立：RF 是
`max(FLOOR, (s·t/M)^GAMMA)`，是**有地板的**，不是純冪次；route A 把案子往地板推、LP 把它們推回來，
是否被 clip 取決於**合起來的 t**，而兩次單獨量測都沒評估過那個組態。

`l114_combined_price.py` 用同一個 baseline、同一個 scoring function 重算四個變體
（A = wall×0.865；B = wall + t_lp；C = 兩者；A+B = 相加）。
**閘**：route A 那一行只依賴 `tnow`/`qship`，必須逐字重現已發表值（`+0.397/+1.978/+2.884`）——過。

結果：**相加是偏保守**。機制是 route A 壓低 wall ⇒ **更多案掉進 RF floor，LP 加的時間在那裡不被收費**；
這個效應蓋過「wall 變小所以 t_lp 相對變貴」。

---

## 2. 🚨 判決對 tLP 的量測方式極度敏感（單次計時會翻盤）

同一個組態、三種 tLP 輸入，出來三個不同判決：

| pruned tLP 來源 | 加權 tLP | 41 點 grid worst | 判定 |
|---|---|---|---|
| Aug-6 tlp ÷ 逐案實測 f（20 案） | — | **+0.567%** | weak-win |
| **單次**計時、全 100 案 | 0.3342s | **−0.645%** | ❌ 不是 weak-win |
| **min of 3**、全 100 案 | **0.2742s** | **+0.563%** | weak-win |

單次那格是雜訊：專案明載「本機單次計時無法排序方案（抖動 ~4.6%），任何計時一律 ≥3 次」
（`HANDOFF_2026-08-09.md` §6.4），而 `l100_lp_speed.py ab` **每案只計時一次**。
兩條獨立路線（逐案 f、min-of-3）落在 +0.567% / +0.563%，單次那次落單。

⇒ 新增 `l114_tlp_reps.py`：同一條鏈、同一個 guard、同一個 B，但 reps + min，
並檢查 reps 之間品質有沒有動（**0/100 案有動** ⇒ min-of-N 合法）。

---

## 3. 定價結果（min-of-3 輸入，k=1，B=8）

| s | A: route A | B: LP | **C: 兩者** | A+B 相加 |
|---|---|---|---|---|
| 0.5 | +0.000% | +2.355% | **+2.356%** | +2.356% |
| 1.0 | +0.397% | +1.496% | **+2.276%** | +1.892% |
| 1.5 | +1.978% | −0.567% | **+1.643%** | +1.411% |
| 2.0 | +2.884% | −2.250% | **+0.969%** | +0.635% |
| 2.5 | +3.840% | −3.049% | **+0.563%** | +0.791% |

**41 點機速網格：C worst +0.563%、best +2.409% ⇒ 全機速 weak-win。**
route A 單獨的 worst 是 0.000%（s=0.5 全被 floor 吸收）⇒ **疊 LP 把最壞點抬高 +0.563pp**。

### 加速倍率：講清楚是哪個統計量

配對的 min-of-3、全 100 案、官方權重加權：**未剪枝 0.8302s → 剪枝 0.2742s = 加權 f 3.03×**。

⚠️ **不要拿這個去對 L112 的 ≥3.0× GREEN 門檻。** 那個 bar 預註冊的是「**量測案子的中位數**」：
20 個最重案的中位數是 **2.62×**（8 案版是 2.75×），依原 bar 仍是 **AMBER**。
加權平均之所以較高，是因為剪枝在重案贏最多（case 91 6.66×、96 3.91×），而權重正好偏重案。
定價模型**不用任何一個純量**——它直接吃逐案時間，那比任何一個 scalar 都準。
（不 retro 移門檻：M64/M65 紀律。）

---

## 4. 部署形 100 案鏈（`l100_lp_speed.py ab --prune-b 8`）

新增 `--prune-b` CLI（原本 `PRUNE_B` 只有 module-level，100 案鏈沒辦法要求開剪枝）。

| | 剪枝 B=8 | 未剪枝對照 |
|---|---|---|
| k=1 gain vs M80 @48c 錨 | **+2.3559%** | +2.3559% |
| regressions | **0** | 0 |
| infeasible | **0** | 0 |
| objective k1 rel diff | **1.659e-03** | 9.582e-12 |

`l97_shape_lp.py gate0` 亦 **PASS**（6 案 max |d| = 0）。

### 🚨 剪枝在 1/100 案上不是 exact —— case 8

逐案帶號比對（`passes[0].lp_obj`，剪枝 vs 未剪枝）：**只有 case 8** 超過 1e-9，
而且方向是**偏低** `−1.659e-03`。

方向是關鍵。`solve_pruned` 的論證是「把 `t ≥ |z|` 換成 `sgn(dC)·z` 只會讓目標值變小 ⇒ 剪過的是**下界**，
只有當回傳解滿足所有假設符號時才等於真最佳」。**偏低 = 那個下界沒被達成 = 符號驗證放行了不該放行的解**，
不是常數記帳問題。

| case 8（n=29） | |
|---|---|
| q_ship | 1.2698504111869229 |
| 未剪枝 q | 1.2211781920413007 |
| 剪枝 q | **1.218286725938109（好 2.89e-3）** |
| tLP | 0.0299s → **0.1153s（慢 3.9×）** |
| feasible / kept | 兩邊都 True / 1 |

實際後果：**分數上無害**（case 8 是 n=29、權重極小；100 案總分逐字相同、0 regressions、100/100 feasible，
而且它剛好變好），**但「剪枝只改速度不改結果」這句話是錯的**。L112 只量 20 個最重案，
case 8 不在裡面。真正擋住壞佈局的是 `hard_ok` + proxy guard + 官方 feasibility，不是這個符號檢查。

⚠️ 別把 `1e-9 objective 等價` 當成剪枝的既有性質引用；它在 99/100 成立。

---

## 5. 🚨 我弄壞的兩樣東西（這些 probe 寫死路徑、輸出不進 git）

1. **`results_L100_lp_speed.json`**（`l100_lp_speed.py ab` 固定寫入）：我的兩次 `ab` 把
   **2026-08-06 那份逐案 LP 計時覆蓋掉了**。品質面不受影響（`q_dep1` 是確定性的，+2.3559% 逐字重現），
   計時也已用更好的方式重量（§2），但**已發表的 LP 那一行（+1.95/−0.57/−2.40）不再能逐位重現**
   ⇒ `l114_combined_price.py` 的 LP 閘已降為 informational，route A 那行仍是硬閘。
2. **`results_L99_oos_shape.json`**：先用 `--limit 8` 做煙霧測試，把 08-06 的 **100 案 k=4** 結果
   蓋成 8 案。已用 §6 的 100 案 k=1 配對跑重建（k=1 才是部署深度與預註冊判準所在），
   但原本的逐案 k=2..4 明細沒了。

**教訓**：跑這一家 probe 前先備份既有 `results_*.json`。它們全部 `.gitignore` 之外但沒 commit，
而每支工具都寫固定檔名。

---

## 6. OOS：配對重跑（100 個沒見過的謎題，k=1）

`l99_oos_shape.py:128` 呼叫的是 **`l97.dep_case`**（參考鏈），不是 `l100.dep_case`（加速鏈）——
兩個同名函式。既有的轉移結論是在**未剪枝**鏈上量的，而 case 8 證明剪枝**可以**改變輸出。
所以給 l99 加了 `--prune-b`：同一個建好的案子上跑兩條鏈，**配對**比較（建案子是最貴的一步，只做一次）。

**參考臂（品質／轉移）**

| | |
|---|---|
| baseline（M80 池 @48c） | 1.533138970 |
| k=1 後 | 1.495314926 |
| OOS gain | **+2.4671%**（in-set +2.3559%） |
| **transfer** | **104.7% ⇒ GREEN**（預註冊 bar ≥60%） |
| regressions / infeasible | **0 / 0** |

**加速臂（PRUNE_B=8）**

| | |
|---|---|
| 加權總分 | 參考 1.495314926 → 加速 1.495327022（**+0.0008%，變差**） |
| 加權 tLP | 2.8356s → **0.2907s（9.76×）**（對的是 l97 參考鏈，含加速 builder + 向量化 guard，非只有剪枝） |
| **輸出有動的案子** | **1/100** — `worker_2/layouts_8400/L29`，**dq = 1.61e-02**，kept 1→1，仍 feasible |
| 加速臂 infeasible | 0 |

⇒ **兩件事都得到了答案**：
1. **LP 機制在沒見過的謎題上轉移得很好（104.7%）**，比 in-set 還好一點。
2. **剪枝在 OOS 上同樣不是輸出保持的**：1/100 案動了，而且這次是**變差 1.6%**
   （in-set 的 case 8 是變好——方向純屬運氣，正如下界論證失效時該有的樣子）。
   對加權總分的淨影響 **+0.0008%**，可忽略。真正擋住壞佈局的仍是
   `hard_ok` + proxy guard + 官方 feasibility：兩臂都 0 regressions / 0 infeasible。

## 7. 已接進出貨 wrapper（`ship_final`，預設 OFF）

LP 核心用**程式化抽取**搬進 `ship_final/optimizer_constructive.py`（748 行，AST 切片，
不手抄）：`comp_split` 來自 `m53_l3_probe`，其餘全部來自 `l100_lp_speed`（加速 builder +
HPWL 剪枝 + 向量化 guard）。probe 對離線世界的耦合只有三個名字（`l3.CASES`／`l3.comp_split`／
`oc._proxy_metrics`），所以用一個 `_LPShim` 綁住 `l3`，抽出來的程式碼**逐字執行**、不改呼叫點。

旋鈕 `ICCAD_SHAPE_LP`（預設 `0` = 關）、`ICCAD_SHAPE_LP_B`（預設 8）、`ICCAD_SHAPE_LP_ITERS`（預設 1）。
`_shape_lp_maybe` 永不拋例外：旗標關、scipy/shapely 缺、任何例外 → 原封不動回傳選中的佈局。

### 🚨 離線量的 +2.3559% 用了兩個 oracle 常數

`build_and_solve` 用 `c["base"]` 的 `hpwl_baseline` / `area_baseline` 設定目標裡 HPWL 與 area 的
**相對權重**。而 `iccad2026_evaluate._extract_baseline` 的 docstring 明寫
**"Extract baseline metrics from ground truth"** —— 那是 label 推出來的，wrapper 在 solve 時看不到。

⇒ 出貨版改用 **baseline-free 替代**：選中佈局**自身**的 hpwl 與 bbox area
（LP 於是在官方 0.5/0.5 的權重下最小化各項的相對改善）。**這是不同的目標函數，
離線那兩個數字不能直接搬**，必須用官方 eval 重量。

### 官方 eval（`ship_final`、`ICCAD_ADAPTIVE_CORES=48`、`ICCAD_SHAPE_LP=1`、全 100 案）

| | |
|---|---|
| 錨（LP 關） | `1.2666234250706565` |
| **LP 開** | **`1.238989335942891`** |
| **增益（RF=1）** | **+2.1817%**（離線 oracle 版 +2.3559% ⇒ 保住 **93%**） |
| feasible | **100/100** |
| **regressions** | **0** |
| movers (>1e-6) | 100（LP 非逐位，預期內） |
| LP 例外 | **0** |

旗標**關**的情況下對動手前的錨**逐位相同**（total + cost 100/100 + positions 100/100）。

### 路上踩到的兩個（都被安全網接住、但都是靜默 no-op）

1. `NameError: Counter` —— 抽取時漏了 `from collections import Counter`。100 案全丟例外，
   **分數等於原值**（守門把原佈局還回去）——只有那 100 行 stderr 讓它可見。
2. `ValueError: operands could not be broadcast` —— `_HARD_MASKS` 以 case id 為 key，
   而每次 live 呼叫都用同一個 key `"live"` ⇒ 第二案起拿到上一案尺寸的 mask。99/100 案失敗，
   **只有單獨跑一案時會過**（我的單案 smoke 正好過了）。已在 `finally` 清掉。

### 用真實出貨品質重新定價

| s | route A | LP | **兩者** |
|---|---|---|---|
| 1.0 | +0.397% | +1.319% | **+2.102%** |
| 1.5 | +1.978% | −0.748% | **+1.467%** |
| 2.0 | +2.884% | −2.433% | **+0.792%** |

**41 點 grid worst +0.384%、best +2.235% ⇒ 仍是全機速 weak-win。**

## 8. 預設已翻 + 三道閘

`_shape_lp_on()` 改成**與 route A 同一道核數閘**（`_effective_cores_hi() >= _ROUTE_A_CORES_MIN`，=40）。
理由：+2.18% 的 weak-win 是**兩者一起**的性質；LP 單獨從 s=1.5 起是負的（−0.75% / −2.43%），
不該在 route A 不生效的機器上開。偵測失敗（回 0）同樣退回出貨行為。
`ICCAD_SHAPE_LP=0/1` 可強制。

| 閘 | 結果 |
|---|---|
| 本機預設（32 核 ⇒ 三個 tier 全不觸發） | **逐位相同** `1.293461035226291`、cost 100/100、positions 100/100 |
| **`l113_ship_gate.py --cores 48`（封包級，LP 全開）** | **ALL PASS** — `1.238989335942891`、feasible 100/100、cost 100/100、positions 100/100、零 fallback、peak 32/32、包內自行編譯（335s） |
| `regression_suite`（m48 / m47b / tier5 / m80） | **ALL PASS**（407s；m47b 300 comparisons 0 mismatch） |

⚠️ **合併時撞名**：`AREA_TOL` 與併入的 SA 尾巴撞（`make_submission` 的 AST assert 攔下），
已改名 `_LP_AREA_TOL`。那個 assert 就是為這種事存在的——它擋住的原本會是「SA 切片的常數
無聲蓋掉 LP 的面積容差」。

## 9. 還沒做的

1. **Linux 100 案逐位驗證**（L113 §6 就欠著的那條）—— 現在多了一項理由：
   出貨包首次帶 **scipy 依賴**（`requirements.txt` 仍必須留空，評分機已內建 scipy）。
2. `tnow` 用的是 l86 存的 **M71@48c** wall，不是出貨的 M80 43 隻池 wall（所有變體共用同一
   baseline，比較有效；絕對值帶著這個偏差）。
3. `rf` / `m49big` / `m49mid` 三個閘仍卡在缺 `audit_cache.pkl` / `m49_pm_cache.pkl`。

## 10. 一句話

**route A + 形狀 LP 都已在出貨樹上、預設開（≥40 核）、封包級 48 核閘逐位重現
`1.238989335942891`（對 M80 錨 +2.1817%、0 regressions、100/100 feasible）。
唯一沒覆蓋的風險仍是 Linux 端。**
