# M83 給組員 — **tier-5 的「免費」論證已證偽**；擁塞斜率實測

**日期**：2026-08-02 · **發訊方**：Claude 側 · **對象**：組員
**來源**：`m83_contention_slope.py` + `results_M83_contention_slope.json` + `m80_rf_remeasure.py`（R1）

---

## 你們需要知道的一句話

**M67-E 的「M42 那 22 隻在 48c 加回來 `dW = +0.00%`」不成立。**
它要求「並行不影響逐隻 dt」，而我們把那個假設**直接量了**：
即使 35 隻**全部落在不同的實體核上**（零 SMT、零超額訂閱），逐隻也**慢 10%**；
在 SMT 共用下慢到 **1.53×**。

tier-5 是**已出貨**且**在評分機上會觸發**（`_M67F_CORES_MIN = 40`，Beta/Final 48 核）。
代進 RF 後，兩種評分機拓撲假設下**它都是淨虧**。

⚠️ **但先看 §否定-1**：這是**行動版 AMD 雙通道**上量的，伺服器 Xeon 的頻寬高得多。
**我不主張「你們現在正在虧錢」**，我主張的是**那個 `+0.00%` 的論證已經死了**，剩下只是虧多少。

---

## 肯定（實測，有數字可查）

### 1. 擁塞斜率：三個重案完全一致

方法：出貨組態的 env（`_PROFILES[k]` + `_M71_ENV` + n>100 疊 `REFINE_ITERS=4`）、
`threading.Barrier` 同時開跑 k 份**相同拷貝**、每點 3 reps 取中位、**不釘核**。

> ⚠️ 這裡和你們 2026-07-23 的 `m67f_contention_probe.py` 有一個方法差異：
> 那支**釘核到 logical 0..k-1**。在 SMT 相鄰編號的機器上，釘 0..15 其實只用到
> ~8 個實體核，會**假造出提早的擁塞**。出貨 wrapper 本來就不釘核 ⇒ 讓 OS 依拓撲鋪開
> 才是 production-shaped。這可能也是你們當時只看到 `dt(8)/dt(1) = 1.018` 的原因之一
> （k≤8 在我們這條曲線上也只有 1.02，**兩邊在該區間是吻合的**）。

本機 = AMD Ryzen 9 8940HX，**16 實體 / 32 邏輯**。

| k | dt(k)/dt(1) | 區間 |
|---:|---:|---|
| 8 | 1.02 | ← 你們量到 1.018，吻合 |
| 13 | 1.06 | 現行本機池 |
| **16** | **1.10** | **全在不同實體核、零 SMT、零超額** |
| 20 | 1.22 | SMT 開始共用 |
| 24 | 1.33 | |
| **32** | **1.53** | SMT 全共用、仍未超額 |
| 35 | 1.61 | 超額（本機才有，評分機沒有）|

**實體核區間斜率 = 0.71% / 每多一個並行者。**
**陡的那一段起點正好在 SMT 邊界**（k>16），超額那一段（32→35）反而只多 0.08。
⇒ 主因是 **SMT 共用 + 記憶體頻寬**，不是超額訂閱。

### 2. 端到端佐證（不是只有 micro-benchmark）

跑真正上傳的 `cadc1075/op_wrapper.py`（md5 `6f1f31a2…`）官方指令，
用 `ICCAD_ADAPTIVE_CORES=48` 強制成評分機池，**各 20 次取逐案最小值**：

```
20 大案帶 (n>100)：13 隻池 → 35 隻池，逐案 wall 中位 1.325x、最壞 1.690x（20/20 全變慢）
wall 模型預測：                                    中位 1.000x、最壞 1.142x（只 6/20 過 2%）
```

⚠️ 這個端到端數字**混入了本機超額訂閱**（35 隻 > 32 邏輯核），不能直接當評分機值——
所以才另外量 §1 的斜率。**斜率才是可外推的那個。**

### 3. 代進 RF 的淨值

| 評分機拓撲假設 | tier-5 wall | s=1.5 | s=2.0 | s=2.5 |
|---|---:|---:|---:|---:|
| **48 實體核**（用 §1 實體斜率外推到 k=35）| ×1.176 | **+2.57%** | **+3.94%** | **+4.55%** |
| **24 實體 / 48 邏輯**（SMT，用實測 k=32）| ×1.446 | **+7.59%** | **+10.38%** | **+11.25%** |
| 原模型（dt 不變）| ×1.000 | −0.52% | −0.42% | −0.34% |

（**正 = 虧**。品質側用 `audit_cache_m71_kband.pkl`：tier-5 ON 1.295548 / OFF 1.305390，
即 tier-5 買到 −0.754% 品質；上表是品質減掉 wall 代價後的淨值。）

### 4. 模型鏈本身是對的（所以上面的品質數字可信）

`m67e_rf48.py --m71 --kband` + `pool_shipped()` **逐位預測到兩種池組態的實測總分**：

```
強制 48 核池  total = 1.295547821428   模型 1.295548  ✓
預設 32 核池  total = 1.305389893451   模型 1.305390  ✓
```

⇒ 錯的**只有 wall 模型的 dt-invariance 假設**，品質那一側沒問題。

---

## 推測（外推，未實測）

### 1. 48 實體核那一欄是線性外推

`c(35) = 1 + 34 × 0.0071 = 1.243` 是從 k≤16 的 15 個點**線性外推**到 34 個並行者，
**超出本機實體核數**、無法在本機驗證。頻寬飽和曲線通常是凹的（越後面越陡），
所以 1.243 偏樂觀的機率高於偏悲觀。

### 2. 評分機是 48 實體還是 24 實體×2？

`M67E_REPORT` 只記「48c ICELAKE」。Ice Lake-SP 兩種都存在。
**這個未知數決定上表要看哪一列**，差距是 +4% vs +11%。
如果你們有 Beta 的 `lscpu` / 逐案 runtime，這題就能收斂。

---

## 否定（別這樣讀）

### 1. ❌「Claude 說 tier-5 在虧錢，快關掉」

**不要這樣讀。** 本機是**行動版 AMD、雙通道 DDR5**；伺服器 Xeon 是 **8 通道**、L3 大得多。
擁塞斜率在評分機上**可能平很多**——平到接近「原模型」那一列的話，tier-5 回到約略中性。

**我主張的只有一件事**：`dW = +0.00%` 這個**論證**死了。
它要求擁塞恰好為零，而實測連「零 SMT、零超額、全在不同實體核」的 k=16 都有 **1.10×**。

### 2. ❌「那 22 隻的 dt ≤ max-setter，所以加它們免費」

`dt ≤ max-setter` 是**單獨跑**時量的。加了它們之後，**所有人都變慢**——
包括 max-setter 自己。免費論證漏掉的正是這一項。

### 3. ❌ 用 `m67f_contention_probe.py` 的釘核結果當定論

見 §肯定-1 的方法差異。你們的 k≤8 區間與我們吻合（1.018 vs 1.02），
但**釘核到 0..k-1 在 k>8 之後會偏**（SMT 相鄰編號）。建議改用不釘核版重測，
或至少確認你們那台的 logical→physical 映射。

---

## 我這邊沒有動的東西

- **送件形零改動**、`_PROFILES` 未碰、`_M67F_CORES_MIN` 未碰
- 池常數（`_BIG_REDUNDANT_IDX` / `_M45_BAND_DROP` / `_M45_LOWCORE_DROP`）**一個都沒改**
- 這是你們 M74 的領域，而且任何改動要走完整重驗鏈 ⇒ 我只提供量測

## 順帶：三個你們可能還不知道的更正

1. **出貨基準是 1.305390（本機 32 核組態）／約 1.295548（評分機 48 核組態），不是 1.326473。**
   `results_shipped_m51.json` 是 **pre-M71** 錨。文件多處仍寫 1.3265。
2. **`m67e_rf48.py` 原本寫死讀 M71-OFF 的 `audit_cache.pkl`** ⇒ 所有 48c 投影都建立在
   pre-M71 positions 上。已加 `--m71 --kband`（純附加、預設 off、獨立快取），
   兩道自我閘門：預設 full-41 = **1.324846 逐位不變**、`--m71 --kband` = **1.292657**。
3. **`profile_audit.py --m71 --kband` 首次執行完成** → `audit_cache_m71_kband.pkl`
   = 真正的出貨組態（M67-F 更正 B 記著「取樣尚未執行」的那個洞）。
   ⚠️ 檔案 13.8MB **沒有進 git**，要的話跟我說怎麼傳。

## 復現

```bash
cd teammate_m71_screen
PY=".../envs/floorset/python.exe"
"$PY" m83_contention_slope.py combos                    # 列出各重案的 48c max-setter
"$PY" m83_contention_slope.py sweep --combos 3 --reps 3 # 斜率，~3 分
"$PY" m80_rf_remeasure.py setup                         # 從 Downloads/cadc1075 建驗證目錄
"$PY" m80_rf_remeasure.py run --repeats 20 --force-cores 48   # 端到端，~45 分
```

⚠️ **Windows gotcha**：跑出貨包前 subprocess 的 `PATH` 要先加 `C:\msys64\ucrt64\bin`，
否則編譯失敗 → **靜默退回 Python SA → 照樣吐出一個分數**（`m80_rf_remeasure.py` 已內建）。
