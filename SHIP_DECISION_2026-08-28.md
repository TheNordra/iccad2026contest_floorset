# 出貨決策：D / D+RF-SAFE / mix — 2026-08-28

> 🗓️ **截止 2026-08-31 23:59 GMT+8**（Problem C 專屬延期，見
> [DEADLINE_PRIMARY_EVIDENCE.md](DEADLINE_PRIMARY_EVIDENCE.md)）。目前已上傳且驗過的是
> `build_submission.D`。**不做任何事也有分。**

## 建議

> ### 送 **D + RF-SAFE**（`build_submission.RFSAFE/`）
> 次選守 D。**mix 不送**，除非拿到 Final median 變寬的證據。

---

## 零、它插在哪裡（先看這個）

08-23 排行榜，**數字越小越好**。我們的三個位置和別隊排在一起：

```
  0.85863   rank 1（別隊）
  0.86726   D + RF-SAFE（高估端，n=114 沒翻）   <== 我們
  0.86994   D + RF-SAFE（低估端，n=114 翻了）   <== 我們
  0.87511   D（已上傳，不做事）                 <== 我們
  0.88819   rank 2（別隊 — 會被我們擠到 rank 3）
  0.89933   rank 3
  0.92659   ← 我們 beta 實際拿到的分（rank 4）
  0.95071   rank 5
```

### 🔑 **D 和 D+RF-SAFE 插在同一格：rank 2。RF-SAFE 不換名次。**

三個位置**全都落在第一名與第二名之間**。它買到的是另外兩樣東西：

| | D | D + RF-SAFE |
|---|---|---|
| 名次 | rank 2 | rank 2（**不變**） |
| 被擠下 rank 2 的餘裕 | 1.49% | **2.10 – 2.41%** |
| 與第一名的差距 | 1.92% | **1.00 – 1.32%**（砍掉約一半） |

`mix` 是唯一會**移動名次**的臂，而且兩個方向都會動：

```
  f_eff 3.17  0.85675  rank 1     <- 只有這裡贏第一名
  f_eff 2.84  0.86506  rank 2
  f_eff 2.38  0.88430  rank 2
  f_eff 2.10  0.89765  rank 3     <- 掉一名
  f_eff 1.90  0.91011  rank 4     <- 掉兩名
```

⇒ **這不是「哪個分數高」的選擇，是「要不要用已經握有的 rank 2 去換 rank 1 的機會」。**
RF-SAFE 拒絕參加這個賭局，只把餘裕加厚。

---

## 一、三個候選

| | D（已上傳） | **D + RF-SAFE** | mix |
|---|---|---|---|
| 改了什麼 | — | `_L196_LPGATE` 71→**83** | `_L196_LPGATE` →100 且 `_L157_DEPTH` →2 |
| 改動範圍 | — | wrapper 一張表 | wrapper 兩張表 |
| `constructive.cpp` / ELF | — | **逐位不變** | **逐位不變** |
| in-set 48c（Windows） | 1.226325126 | 1.215239132（+0.9040%） | 1.195229398（+2.5357%） |
| **in-set 48c（Linux＝評分平台）** | 1.226406964 | **1.217828992（+0.6994%）** | 1.196905117（+2.4056%） |
| OOS s1 / s2 | — | **+1.1122% / +1.2265%** | +2.89% / +2.68% |
| 逐案迴歸 | — | **12 movers / 0 worse**（雙平台） | 0 regressions |
| 閘下惰性 | — | **逐位，含 positions** | 逐位 |
| l246 | 19/20\* | **20/20** | 19/20\* |
| Linux 五車道 | 綠 | **綠** | 綠 |

\* D 與 MIXD 都帶著 `op_wrapper.py:1709` 的絕對路徑；RF-SAFE 併入了那個修補。

## 二、為什麼是 RF-SAFE 而不是 mix

決定成敗的是 **f·m**（機速 × median 漂移），而**兩者只以乘積出現、截止前不可觀測**。
三條臂在這條軸上的行為完全不同：

```
                    f_eff  3.17    2.84    2.38    2.10    1.90    1.77
RF-SAFE  NET（低估端）    +0.69   +0.69   +0.59   +0.43   +0.27   +0.10
RF-SAFE  NET（高估端）    +0.90   +0.89   +0.80   +0.64   +0.48   +0.31
mix / 全開 gate0 NET      +1.56   +1.27   +0.70   +0.15   -0.35   -0.72
```

* **RF-SAFE 在整個可及範圍都是正的**，L308 量到的 f 帶（2.38–2.84）內**最差 +0.591%**。
* **mix 在 f_eff ≲ 2.15 翻負**，而「medians 再縮一次上次那麼多」正好落在 f_eff ≈ 2.10。
* 名次上：D 全程 rank 2；mix 在 f_eff ≤ 2.26 掉到 **rank 3**、要 f_eff ≥ 3.40 才拿 rank 1
  （**在量測帶之外**）。**mix 用我們已經握有的名次，去換一個我們也可能拿不到的名次。**

方向性證據只有一份，而且對 mix 不利：**唯一觀測到的 median 變動是縮 26%**
（08-16→08-23，逐案 p50 0.742），因為場上變快了（rank 1 從 169.4s 換成 110.9s）。

## 三、為什麼 RF-SAFE 是這輪唯一該買的東西

1. **它的選擇規則不看品質。** 只讀「這一案增加的時間塞不塞得進它自己到 RF floor 的
   slack」。**沒有東西被擬合，所以沒有東西會衰減** —— 這解釋了它的
   **轉移率 123%（s1）/ 136%（s2）**，樣本外比樣本內還好。對照本專案品質導向機制
   慣常的 46–93%。
2. **它是 mix 的嚴格子集。** 兩者不互斥；RF-SAFE 是 mix 那一半可以**單獨買、不用賭**的部分。
3. **成本結構乾淨**：wrapper 一張表、cpp 與 ELF 逐位不變 ⇒ **不需要 Linux 重建**，
   換件鏈最短。

## 四、風險，逐條

| 風險 | 狀態 |
|---|---|
| 逐案迴歸 | **零**。Windows 與 Linux 各 12 movers、0 worse、0 stray、100/100 feasible |
| 低核機器上誤觸發 | **不可能**。default 核數下逐位相同（cost 與 **positions** 各 100/100） |
| Linux 只實現 77.4% | **不是系統性稅，是一個案子**。n=114 一案佔淨損失的 110%。逐層基準率：**頂點有歧異 6/12 → 影響分數 4/12 → 有實質影響 3/12 → 對我們不利 1/12**，而三個有實質影響的翻轉裡**兩個是往我們這邊翻**（n=76 +0.0207、n=38 +0.0002）。⇒ 上表的「低估端」是**單邊保守界**，不是期望值；實際期望更靠近高估端 |
| scipy 版本漂移 | pip 現在解到 **1.18.1**，runbook 驗證用的是 1.18.0。⚠️ **不要 pin**（會撞 l246「no pinned versions」）。下界有界：LP 全關仍 100/100 feasible |
| 選子集用的 dt 是量測值 | 已用 **min-of-N**（10 次 ship + 4 次 gate0）。單次 pair 的子集差 **31%**，所以這一步是必要的 |
| 同機另一 session | 兩條線互相偵測到、都留了字條。`build_submission.D/` 兩邊都沒動過 |

## 五、送之前還要做的

1. **`l238_wsl_final.sh` 已在 RF-SAFE 上跑過五車道並全綠**（L313）—— 不需要重跑。
2. ⬜ **Drive 三件事**：資料夾是 `final_test_submission`（Final 不是 Beta）、
   **裡面只有一顆 tar**、檔名逐字 `cadc1075.tar.gz`（Chrome 重下載會加 ` (n)`）。
3. ⬜ **交件時把新 md5 給驗收方**：`op_wrapper.py` = `62db6ee4569b31ddc8c546ccf3e7cd0b`
   （**不是** D 的 `1c326784…`，也不會等於它——見
   [d-not-reproducible-from-its-commit](HACKMD_REVIEW_RESPONSE_2026-08-28.md) §G）。
4. ⬜ **凍結時點：08-30 23:59**。過了就守 D。

## 五之二、真正在賭的東西有名字：**轉移率**

「如果 0.87511 這個投影樂觀了」講得太模糊。投影裡唯一的軟輸入是
**L285 的假設：M73 以來 in-set 的 −5.3431% 品質改善會原樣出現在隱藏集上**。

⚠️ **只能掃品質那一項。** beta→D 的總改善是 **5.5555%**，但其中
**0.2123 pp 是 RF 改善**（包跑在 beta runtime 向量的 0.8679×，把更多案子推到
RF floor 上，cwRF 0.701606 → 0.700035）。那是一個**量到的 runtime 比值**，
不依賴隱藏語料，**沒有「轉移失敗」這回事**。把 τ 也乘到它上面，等於為了一件
包不可能弄丟的東西罰它。

`τ` = 品質改善有多少比例轉移得過去；RF 項固定：

```
   tau           D  rk    +RFSAFE lo  rk    +RFSAFE hi  rk
  1.00     0.87511   2       0.86994   2       0.86726   2
  0.80     0.88499   2       0.87976   2       0.87705   2
  0.75     0.88746   2       0.88221   2       0.87950   2
  0.70     0.88993   3       0.88467   2       0.88195   2
  0.65     0.89240   3       0.88713   2       0.88439   2
  0.60     0.89487   3       0.88958   3       0.88684   2
```

**守住 rank 2 需要的品質轉移率：**

| | 需要 | 餘裕 |
|---|---|---|
| D | **73.5%** | — |
| D + RF-SAFE（低估端） | **62.8%** | +10.7 pp |
| D + RF-SAFE（高估端） | **57.3%** | +16.2 pp |

（RF-SAFE 那一項同樣當作**不受轉移率影響**——理由見下。本檔先前寫的
74.6 / 64.3 / 59.0% 是**把 RF 項也讓 τ 侵蝕**的版本，偏保守約 1 pp，已更正。）

### 🔑 而這裡有一個很少見的不對稱

**被保護的那一項證據薄，加上去的那一項證據厚。**

* **整包的轉移率從來沒有在隱藏集上量過。** L287 的 **93%** 是在 `floorset_lite` 上量的，
  而 L275 明確警告那不是評分語料（OOS 重帶比評分形狀難 22–24%）。
* **RF-SAFE 自己的轉移率量過，而且是在兩份互斥的 held-out 樣本上**：
  **123%（s1）與 136%（s2）** —— 樣本外比樣本內還好，因為它的選擇規則不看品質。

⇒ **我們不是在「加風險換報酬」，是在「用一個量得很準的項，去保一個量得很不準的項」。**

所以標題可以比「買保險、把跟第一名的差距砍半」更強：

> **RF-SAFE 不移動我們的格子，但它把「守住這個格子所需要的品質轉移率」從 73.5% 降到
> 57–63%，而且用的是這整包裡轉移證據最紮實的一個機制。**

## 六、如果只想要最安全的答案

**守 D。** 它已經上傳、已經驗過、是已知良品，而且在整個 f·m 範圍都是 rank 2。
RF-SAFE 的期望增益是 **+0.6~+1.2%**，代價是一次重傳與重驗鏈。
**這是「要不要為了 0.7% 動一顆已經安全的包」的問題，不是「哪個比較好」的問題**——
後者的答案已經清楚（RF-SAFE 比較好）。

---

### 一句話

**D 是保本，RF-SAFE 是不用賭的加分，mix 是要賭一個看不見的數字。**

更精確地說：三個包都插在同一格（rank 2），差別只在**守住那一格需要多少轉移率** ——
D 要 **73.5%**，D+RF-SAFE 只要 **57–63%**，而 RF-SAFE 自己的轉移率是這整包裡
**唯一在兩份互斥 held-out 樣本上量到、而且超過 100% 的**（123% / 136%）。
mix 則是唯一會讓格子移動的臂，兩個方向都會。
