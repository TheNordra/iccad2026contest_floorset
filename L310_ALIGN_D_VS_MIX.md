# L310 — 對齊兩條線：D vs `mix`

兩個 session 在同一棵樹上各自推進，指向不同的包。這份把兩邊的數字放到**同一條軸**上。
沒有動任何出貨物件；`build_submission.D/` 與 `build_submission.MIXD/` 都原封不動。

## 0. 先講結論

**兩條線沒有矛盾，量的是同一件事的兩個投影。** 合併之後：

| | D | `mix` |
|---|---|---|
| 品質（Linux 48c） | `1.226406964` | `1.196905117`（**+2.4056%**） |
| RF 帳單 | 幾乎不動 | 隨 f·m 大幅擺動 |
| 名次（跨整個不確定範圍） | **rank 2，全程不變** | **rank 3 ↔ rank 2 ↔ rank 1** |
| 對 median 漂移的曝險 | 0.0128 | 0.0554（**4.3 倍**） |

`mix` 是一張**彩券**：贏面是 rank 1，輸面是掉到 rank 3。D 是**確定的 rank 2**。

## 1. 為什麼兩邊的 NET 不一樣：軸不同，不是算錯

* **mix 線**用 `f = local_seconds / grader_seconds` 定價，medians 固定在公布值。
* **L309（這條線）**用 `m` = medians 的縮放，f 固定在**逐案量到的值**。

兩者是**同一條軸**：

```
R_i = t_i^local / (f · m · M_i)
```

只有乘積 `f·m` 可辨識。**沒有任何一方能區分「評分機比我們想的快」與「medians 開得比較寬」。**

換算（L309 的 m=1 對應逐案量到的加權 f）：

```
f = 2.83 · m        m = f / 2.83
  mix          損益兩平 m 0.91  ==  f 2.57
  LP gate 100  損益兩平 m 0.98  ==  f 2.79
  REFINE 還原  任何 m 都不會贏
L308 量到的 f = 2.38–2.84  ==  m = 0.841–1.004
```

⚠️ **我原本猜「差異來自 median 版本」，猜錯了**——mix 線走 `l276_price`，預設就是
**08-23** 那份新的 medians，跟我一樣。（若改用 08-16 那份舊的、寬 26% 的 medians，
mix 在整個 m 範圍都是正的 +1.41%…+2.61%，損益兩平掉到 m 0.61。兩份 vintage 差這麼多，
本身就是這件事有多敏感的證明。）

## 2. 合併後的表：把兩邊各自最可信的一半接起來

* **絕對水準**取 mix 線的投影（`D = 0.87511`）——那一半他們校準得比較好；
  L309 的絕對值刻意保守（bridge 的分母 `l285_betacfg` 帶著 L131/L136 修正）。
* **NET 敏感度**取 L309——arm 之間共用同一分母，偏差在差值裡對消。

```
     m      f |  mix total  rank |   D total  rank
----------------------------------------------------
 0.742   2.10 |    0.89765     3 |   0.87511     2     <- 上次 median 漂移的幅度
 0.800   2.26 |    0.88990     3 |   0.87511     2
 0.841   2.38 |    0.88430     2 |   0.87511     2     <- L308 f 的下緣
 0.900   2.55 |    0.87627     2 |   0.87511     2
 1.000   2.83 |    0.86535     2 |   0.87511     2     <- medians 不動
 1.004   2.84 |    0.86506     2 |   0.87511     2     <- L308 f 的上緣
 1.100   3.11 |    0.85973     2 |   0.87511     2
 1.200   3.40 |    0.85563     1 |   0.87511     2     <- mix 拿到 rank 1
 1.300   3.68 |    0.85387     1 |   0.87511     2
```

門檻：rank 1 = `0.85863`、rank 2 = `0.88819`、rank 3 = `0.89933`。

讀法：

* **在 L308 量到的 f 帶內（2.38–2.84）且 medians 不動 ⇒ 兩者都是 rank 2，選誰都不改名次。**
  f=2.38 時 mix 比 D 差、f=2.84 時 mix 比 D 好，但都在 rank 2 內。
* **名次會變，只發生在 medians 漂移的時候**，而 mix 線的分析裡**完全沒有這一項**：
  * medians 再縮一次（上次是 ×0.742）⇒ **mix 掉到 rank 3**，D 還是 rank 2。
  * medians 寬 ≥20% ⇒ **mix 拿到 rank 1**，D 還是 rank 2。

## 3. 方向性證據：medians 一直在縮

唯一觀測得到的那次 median 變動是**變小 26%**（逐案 `M23/M16` p10 0.600 / p50 0.742 /
p90 0.876，離散度 1.95×）。原因看得到——場上變快了。兩份排行榜的 `total_runtime`：

```
08-16  rank1 169.44s (raw 1.0027)
08-23  rank1 110.91s (raw 1.0845)   <- 換人了，而且快很多
```

**如果這個趨勢延續到 Final，medians 繼續縮 ⇒ mix 掉一個名次。**
反方向的論據（Final 進來更多慢件把 medians 撐大）是有的，但**沒有任何一次觀測支持它**。

## 4. 建議

> **出 D。`mix` 留在 `build_submission.MIXD/` 當候選，不上傳。**

理由不是「mix 不好」——它的品質是真的（Linux +2.4056%、0 regressions、
閘下逐位惰性、五車道全綠，我複驗過他們的成員清單與 md5）。理由是：

1. **它的贏面買不到我們沒有的東西，輸面賠掉我們已經有的東西。**
   在 f 的量測帶內兩者同為 rank 2；mix 的 rank 1 要 f ≥ 3.40（**在量測帶之外**），
   而它的 rank 3 只要 f ≤ 2.26（**貼著量測帶下緣**）。
2. **決定成敗的變數在截止前不可觀測。** Final 的 medians 是跨隊的、評分後才公布。
   這不能「等著看」。
3. **唯一的方向性證據指向對 mix 不利的那一邊。**
4. **D 的曝險小 4.3 倍**——它的名次不依賴我們看不見的數字。

### 會翻掉這個建議的東西

* 拿到 Final 的 median 向量，或任何顯示場上變慢的證據。
* 一個**等牆鐘、純品質**的機制——那不吃 RF 帳單，`f·m` 不確定性完全不咬它。
  `mix` 的品質本身就是這種東西的候選來源：如果能把 `+2.4056%` 裡不花時間的那一部分
  單獨拿出來，它就無條件該送。**這是目前最值得花的下一小時。**

### 如果還是要送 mix

那就要接受這是一個「用 rank 2 換 rank 1 的機會、賠率由看不見的數字決定」的決定，
並且至少把 L309 的 m 軸加進 mix 線的定價鏈——目前那條鏈**沒有任何一項處理 median 漂移**。

## 5. 順帶：mix 線獨立確認了我對 D-patch 的更正

他們的 `README_MIX.md` 寫：

> `C:\msys64\ucrt64\bin` must be on PATH. Without it `cc1plus.exe` cannot load
> its DLLs, `g++` exits 1 with no output, and the package degrades **silently**
> to the Python SA

這正是我在絕對路徑 patch 那條記的更正——patch 註解說「裸 `g++` 解到同一顆 msys binary」
**是假的**。兩條線各自撞到、結論一致。⇒ 拿掉那條絕對路徑在評分機上成本為零，
但**本機必須補 `ICCAD_CXX` 或把 msys bin 放進 PATH**，否則靜默退回 SA。

## 6. 重現

```bash
python l309_median_sensitivity.py                    # 08-23 medians（預設）
python l309_median_sensitivity.py --vintage 2026-08-16
```

`l309` 現在含 `mix` 臂，逐案 min-of-N（mix 5 次重複、D 5 次、betacfg 2 次），
並在載入時斷言各重複之間 cost 逐位相同——那同時是一道免費的確定性閘，本次全過。
