# ICCAD 2026 FloorSet — Session Context

## Claude 對話框規範
- 聊天室語句**盡量精簡**、用**繁體中文**。

## 🚨 範式轉移（最重要，先讀）

**這題是 reconstruction（還原 baseline 原圖），不是 floorplan optimization。**

- Cost = `(1 + 0.5·(HPWL_gap + Area_gap)) · exp(2·V_rel)`：gap=0 ∧ V_rel=0 → **Cost=1.0**（理論最小）。「找最佳解」永遠 HPWL_gap>0 → Cost>1；還原原圖才能 gap≈0。
- 真天花板 ~1.1（`fp_sol` verbatim = **1.1079**，headroom 100% 在 quality、violation 已贏）。組員 **1.0322 是 oracle**（讀本地 validation label，hidden test 退 fallback → 不適用）；legit 上限 ~1.62。
- 訓練 `fp_sol` = ground truth (w,h,x,y)，但無監督 loss 沒用它 → per-block local ML 在這題很弱（組員 v10-v12 全 <1%）。
- **現況一句話**：quality 軸 M51 converged（shipped **1.3265**）；RF 軸七槍 M41-M50 已 ship（與 quality 正交、被本地 RF=1.0 藏住，真增益本地看不到）；送件 hardening M43+M48 已 ship。**M52 ML 軌 RED**（imitation 零容錯 × 零訊號）；**M53 offline 錨 1.2978 達標 <1.3**（L1 ship + L3 LP，非送件）；**M54 in-window LP RED**（RF 全域淨虧）；**M57-M61 codex-gap 五 probe 全 RED**（2026-07-16/17：code 主張全屬實、分數價值全零；錨 1.2978 / 送件形皆不動，見 ledger 五條）；**M62 break-even GREEN**（2026-07-18：M54 的「無 weak-win」限 cap≥0.5s 網格——cap 0.1-0.25s **skip** gate 零加速即全 cell 弱贏，惟依賴 pre-build 時間預測器＋機速校準 → 未動工、送件形不動，見 ledger M54 條補註）；**M63 A3-beam 上界稽核 ABOVE-BAR / RED-leaning**（2026-07-19：T2 上界 5.11% 過 bar 不能免實作判死，但 pool oracle 反證 bits 可達而恆付稅、全 pool realizable 僅 0.0012% → 未開工、決策卡在 LP kill-test，見未來方向 §0d）；**M64 錨相鄰拓撲 flip RED**（2026-07-19：pilot 3 案 529 flips **0 movers**——87% cone 空、可行者 HPWL 稅或 tie、vrel 全不動；M59 stage-2 正式結案，見 ledger M64 條）；**M65 L1 K24-extra 補量 RED**（2026-07-19：兩隻 `_L1_EXTRA` 缺 K24 版屬實，但 pilot 15 重案 **0 mover**——4 tie 全逐位同錨（贏處 no-op）、餘 26 runs 全退步；84 池不動，見 ledger M65 條；**附帶還原 25f3eb0 誤刪的 `proxy_analysis.py`**——27 工具依賴、regression_suite gate 鏈曾因此斷裂）；**M66 M56 CV 等價類補洞 RED**（2026-07-20：M56 CV 不對稱屬實——train 側 LOO/keep-set 用 singleton winner index、OOS 卻收 cost 等值——E 化重跑 J\* 仍 20/20 None、OOS breaks 30→26/200（4 healed 0 new、最壞 case 96 +9.09% 不動）、26 殘破口全 LABEL-ABSENT 且 |E|=1（winner 無等值替身可救）、mean kept pool 23.0 ≈ chain pool 24.4 增益注定零 → per-case pool 軸封卷維持，見 ledger M56 條補註）；**🚨 Alpha 結果已到 + M67 Beta 打包主線（2026-07-20）**：alpha 測資 = 本地 validation set 逐位相同、alpha 只是 pipeline exercise；**RF 真實**——官方分 = raw × 逐案 RF，我們 cadc1075 = Rank 3 官方 **1.0286**（送件版 = M10 raw 1.4528，cost-加權 RF **0.7081 ≈ floor**，M41-M50 全數兌現）；現版 M51 投影 ~0.94；Beta = 真 hidden cases、每隊獨佔 48c ICELAKE + A100、deadline **2026-07-31 17:00 GMT+8**、格式錯誤=DQ（entry 必名 `op_wrapper.py`、禁絕對路徑、禁多餘 optimizer .py）；label/oracle 路線出局、L1/LP 仍不可送（48c 下牆=max_i）；**Beta 前唯一主線 = `M67_PLAN.md` 五 session（**A ✅ 完成 2026-07-20**：兩 wrapper msys 路徑 nt-gate、編譯鏈 POSIX 化（SA 亦 M48 式鏈+smoke）、bundled-binary-first `bin/constructive_linux`/`bin/sa_linux` 鋪好、optimizer_claude lazy import；gate 官方 eval 逐位 1.326473104916827 + regression_suite 六項全 PASS 728s；**B ✅ 完成 2026-07-21**：`make_submission.py`（stage/verify/all）產 `build_submission/cadc1075/`+tar——op_wrapper.py = constructive + 機械切片併入 optimizer_claude 的 B1 SA 區+B2 serialize 區（⚠️ naive 串接會炸：SA 側 module-level `_BIN`/`_ensure_compiled`/`MyOptimizer` shadow；lazy import 整塊移除防 sys.path 遮蔽）、衛生 assert（白名單/絕對路徑/AST 零碰撞/tar 複驗）；gate 三綠 = 官方指令 staging 內全 100 案逐位 1.326473104916827（零 fallback、avg 1.57s）+ `m48_coldstart_dryrun.py opwrapper` 四 phase + regression_suite 六項；**C ✅ 完成 2026-07-21**：`m67c_make_linux_bundle.py` 產 WSL2 自足驗證包 → GPU 機 Ubuntu-22.04 / g++ 11.4.0 **四關 ALL GREEN**——T1 `-O3 -static-libstdc++ -static-libgcc`(無 -march)產 `bin/constructive_linux`+`bin/sa_linux`(ldd 僅 libc/libm、smoke OK、全-static 備胎另存不入包)、T2 m48 opwrapper 四 phase、**T3 官方 100 案 total 1.326473104916827 逐位 |d|=0**(avg 0.92s、唯一 case 84 cost 4.441e-16 ULP、positions 全同;**bundled-first 鐵證 = 無 `constructive.exe` 產物**)、T4 破壞 binary 落編譯鏈 case 50 逐位;入包後 Windows gate 全綠 + 最終 tar 內 op_wrapper.py md5 = WSL 實跑那份。⚠️ WSL gotcha:m48 phase 2 的 `MZ` 假 binary 觸發 binfmt WSLInterop → Windows 彈 16-bit modal(無害,`echo 0 > /proc/sys/fs/binfmt_misc/WSLInterop` 可免)；**D ✅ 完成 2026-07-21**：`m67_oos_probe.py` 訓練集 240 案 OOS 預檢——raw **1.6533** vs in-set 1.3265（+24.6%，預註冊 bar 判 RED）**但屬語料難度非過擬合**（單 profile 參照 gap +26.4% 更大、portfolio 增益 OOS 11.50% > in-set 10.22%、label floor 1.2444 vs 1.1079、boundary 28.8 vs 24.0）；硬旗標全清（100% feasible / 零 fallback / runtime 同分佈）；**主發現 = adaptive 切法 OOS 品質稅 +2.825%（movers 52/80、最壞單案 +11.9%）＝ in-set +0.106% 的 27 倍**（證實並超過 M55 CV 預言）→ 「strict gate ∀median∀cores 弱贏」只在樣本內成立，**惟 RF 側仍淨賺 → 送件形不動**（⚠️ 其「median 3.28× t_shipped / 8.2× 邊際」算術已由 **M67-E 修正作廢**，見下），見 ledger M67-D 條 + `M67D_REPORT.md`；**E ✅ 完成 2026-07-21**：`rf_score_model.py` 加 48c 網格 + 新 `m67e_rf48.py`（cache-only、gate0 五閘 ALL PASS）——alpha 錨改為逐案 `M_i=3.161·t_i^alpha`（alpha 送的是 M10 廉價池、其 t p50 0.673s ≈ 我們現行量級）⇒ 48c 下我們**貼著 floor 邊緣**（加權 RF 0.73、mid band 完全在 floor 上）；**48c wall = max-setter（100/100）**；`POOL=0` **+53~60%**、REFINE 還原 **+15~21%** ⇒ M41+M49/M50 必須留；**🚨 但 M42/M45 的 22 隻 build 砍法在 48c 買不到任何 wall（dW=+0.00%，@24c ≤+0.53%），只剩 +2.8% OOS 稅 → break-even θ\*=0、θ=1 上界 −2.11%**，前置 = M67-F 實測 θ（送件形不動），見 ledger M67-E 條 + `M67E_REPORT.md`；**F Phase 1 ✅ 完成 2026-07-22 = GREEN**：加 offline 旋鈕 `ICCAD_M67F_RESTORE`（預設 off、三閘證逐位不變）+ `m67_oos_probe.py` 新 mode `restore`，同批 80 案 OOS 量到 **θ_pool = 0.7636**（44 better / 1 worse、改善 max 10.65%）、第三臂 **θ_refine = 0.0864 RED**（⇒ M49/M50 在 OOS 幾乎不欠品質債、必須留）；**F mid band 補量 ✅ 同日完成**：**θ_mid = 0.5913 品質綠但 ship RED**（mid 稅只有 heavy 的 37%、回收 +0.620% < wall 代價 +0.695%，且 mid 帶 0/40 觸底＝全額付）⇒ **tier-5 只放 M42、tier-3 不動**；**🚨 同時更正 Phase 1 兩點**：(a) −1.60~−1.62% 是**用 dt-filtered 池投影、θ 卻是 index 池量的** → 正解 **−1.30%(s=1) / −0.55%(1.5) / −0.26%(2~2.5)**（index restore 在 48c 要付 wall +5.7~8.7%）；(b) **M45 tier-3 strict gate 在出貨組態(K=8)下已失效**（in-set case 64 +0.41%、加權 −0.0018%；K=12 下兩側逐位相等 ⇒ 常數推導組態與出貨組態不一致）；**送件形仍不動**，ship 卡在 Phase 2（真多核機 wall 實測，本機 12 核答不了；**2026-07-23 本機 E-core 擁塞前哨 = AMBER/lean-GREEN**：mean dt(8)/dt(1) 中位 1.018、K=12 更平 1.003、slope≈0，max-setter 自己不變慢、k=8 straggler 是飽和 jitter 非擁塞 ⇒ M67-F **不判死**，仍待 Beta 逐案 runtime，見 ledger M67-F Phase 2 條），見 ledger M67-F 兩條 + `M67F_REPORT.md`）**，細節與已驗證事實見該檔 + memory `[[alpha-results-2026-07]]`、`[[m67d-oos-precheck]]`、`[[m67f-theta-pool-cut]]`）。逐版數字見「目前狀態」「戰略結論」「未來發展方向」「死路 ledger」與 memory（`[[reconstruction-paradigm]]`、`[[m41-runtime-factor]]`…`[[m50-midband-refine-tiers]]`）。

## 評分公式（2026-05-23 確認）

- **Cost**（per case）= `(1 + 0.5·(HPWL_gap + Area_gap)) · exp(2·V_rel) · max(0.7, R^0.3)`
  - 不可行 = 10.0；feasible 上限 9.999999；gap 從下方 clamp 到 0（贏過 baseline 無額外獎勵）
  - `V_rel = (V_boundary + V_grouping + V_mib) / N_soft`，`N_soft = boundary blocks + Σ(MIB-1) + Σ(Cluster-1)`
- **Total Score** = `Σ Cost[i]·exp(n_i/12) / Σ weight`
  - 權重 e^(n/12)：n=120→8.0%、n≥110 累計 ~53%（中小型 case 比舊版重很多）；總權重 ≈ 275418
- **RuntimeFactor** = `max(0.7, R^0.3)`（逐案，`evaluate.py:552`），R 分母 = cross-submission median（**alpha 實測校準 2026-07-20 / M67-E 修正 2026-07-21**：cost-加權 RF=0.70802 ⇒ **`M_i ≈ 3.161 × t_i^alpha`**——錨的是 **alpha 那版（M10 十四隻廉價池）的逐案 runtime**（p50 **0.673s**、大案 2.4-4.3s），**不是**現行 shipped 的 runtime；M67-D 據此推的「median ≈ 3.28× t_shipped、8.2× 安全邊際」**作廢**。48c 投影下我們是**貼著 floor 邊緣**（重帶 6/10 觸底、mid band 0/40 觸底、加權 RF ≈0.73）→ runtime 在 48c 仍是活的分數項。組員 ~11s 舊參考作廢，見 `[[alpha-results-2026-07]]`、`[[m67e-rf48-projection]]`）。本地 eval **強制 =1.0**（`:924-940`）→ **M1-M37 每次 A/B 對此項全盲，1.3269 是 RF=1.0 fiction**。**懲罰比 = (t1/t2)^0.3，與 median 無關**（→ 逐案可本地判定）。**M41 反轉：RF 不只是約束、是 lever**——`cost∝t^0.3`，砍大案 wall 是 median-independent 的 real-score 增益（同案 `Q_cap/Q_full < (t_full/t_cap)^0.3` 即贏；大案品質結構卡死 → 幾乎恆贏）。見 `[[m41-runtime-factor]]`、`rf_score_model.py`。

## 目前狀態

### 🏆 最佳：local **1.3265**（M51 2026-07-10；quality add −0.15%）/ RF 鏈 M41-M50 全保持（投影 real @12c M=11 ~0.975 量級、M49 後大案 wall −50%、avg **1.60s**、100/100 feasible）＋ M48 送件 hardening。**offline quality 錨 1.2978**（<1.3 達標、非送件，拆帳見未來方向 §0）；**M54 in-window LP RED → 送件形維持 M51 shipped 不動**

M51 = `fa22_fc_pin_tight_wire`（#40）：`ICCAD_FRAME_ASPECTS=1.0,0.75,1.35,2.2` 疊 fc_pin_tight——**wide-CLAMP 機制**（aspect ≥2.0 的 frame 在 preplaced-heavy 案被 `frame_candidates()` 的 `pre_*+MARGIN`/`max_i*+MARGIN` 下限 clamp 成同一個固定寬 outline，預設集最寬 1.8 從未生成它；2.0/2.2/2.5 逐位同值、REFINE K=4/12 逐位同值）。破最重案 99（n=120、權重 8%）1.3334→1.3084 + case 47 1.2242→1.2179；官方 eval **1.3265**、diff 恰 2 案全改善。驗證鏈全綠：re-audit（4200 combos、proxy 確選 #40）→ rf_score_model regen（`_BIG_REDUNDANT_IDX` +#18=22 隻、tier-4 兩 band +#40、#19/#21 tie 保持、全 asserts OK）→ m49 三 gate（control 100% EXACT、big 唯一 mover 仍 case 85、mid 同 6 movers）。

M41+M42+M45 default-on（`ICCAD_ADAPTIVE_POOL=1`）**四階砍 profiles**（皆在 `_pool_indices()`）：M41 砍 swap（全案）、M42 砍 22 隻 build 冗餘（`block_count>100`；M51 起含 #18）、**M45 tier-3 universal** 砍 9 隻中案 band 冗餘（`60<n≤100`，`ICCAD_ADAPTIVE_BAND=0` 關）、**M45 tier-4 低核**（偵測 cores≤8 才啟用）。gate = **strict selection-preserving**（逐案 cost 相等才砍）→ local 逐位不變、任何 median/cores 下弱贏。`ICCAD_ADAPTIVE_POOL=0` 還原 full 41-prof = quality-best **1.3248**（同時還原 M49 full REFINE）。M49+M50 REFINE band-cut（`_band_env()`）與 M46 C++ hot-path exact 加速詳見 env 旋鈕節 + 里程碑。⚠️ 官方 local eval 顯示 **1.3285**（RF=1.0 fiction）——真增益在 real RF 項，本地永遠看不到（見 RuntimeFactor 節）。

`constructive.cpp`（C++ 建構式定框 placer，B 路線重寫組員架構）+ `optimizer_constructive.py`（portfolio wrapper）。**確定性**（無 randomness/限時 → run-to-run 一致，可精確 A/B）、8.78s/case 單 profile。**proxy 自 M13 起 = oracle ceiling**（完美選擇，加 profile 全額 realize → selection 不再是瓶頸）。

### 單 profile 架構（5 階段，~0.16s/case）
1. **boundary-aspect dims**：LEFT/RIGHT-only aspect **2.50**、TOP/BOTTOM-only **0.40**（拉高 edge capacity 降 vBd，最高 ROI insight）
2. **MIB 形狀統一**（`apply_safe_mib_dims`）：master 相容→用 master；否則 movable ≤1% area→`sqrt(avg)` 方形。保 1% 硬約束 → vMb 145→0
3. **cluster 建構**：純 movable→複合 item（3 ordering×5 layout，key=`(fragments, boundary_bad, area, aspect)` 字典序）；mixed(preplaced+movable)→anchored（first-pass 貼 preplaced「牆」）
4. **定框 greedy packing**：試 4-5 個 outline frame（面積小優先），每 item boundary-aware 候選評分（`bbox_area + 0.10·anchor + ww·WIRE·wire + BP_W·boundary_miss`），ww base **×2000**；layout_score 挑最佳 frame
5. **後處理**：compaction（M10）→ wire refinement（M9）→ HPWL push/slide/swap/jump（M14-16/24）

### Portfolio 層
平行跑 41 deterministic profile（env 旋鈕變體；M51 加 #40 `fa22_fc_pin_tight_wire`），用 **baseline-free proxy** 選最佳：
- proxy = `(area/Â + _RH·hpwl/hmin)·exp(2·vrel)`，Â=1.035·ΣblockArea，hmin=該 case 各 profile 最小 hpwl，**_RH=1.4**（補償 hmin/hbase≈1.3-1.4 對 hpwl 項的低估）
- ⚠️ **vrel 必須用 shapely 算**（wrapper `_proxy_metrics`），不可用 C++ union-find（1e-3 tol，34/100 案不一致）
- 下檔保護：無用 profile 不被選、不傷分（只花 runtime）

### 演進里程碑（deterministic A/B；M4 起累計 −38.5%）
M1 singles 3.62 → M2 cluster 2.35 → M4 +MIB/layout-key/wire×2000 1.82 → M5 anchored 1.7045 → M6-8 portfolio 1.5659 → M9 wire refine 1.5375 → **M10 %.17g + compaction 1.4528** → M12 40-prof 1.4371 → M13 narrow frame + _RH=1.4 1.4349 → M14-16 HPWL push 1.4231 → M17-23 pack-order 1.3983 → **M24 HPWL jump 1.3862** → M25 審計剪枝(38-prof) → **M26 GUIDE_MED 1.3843** → M27 global-packer 死路 → M28 reconstruction ceiling(GREEN) → **M29-M37 free-aspect 六子軸 1.3843→1.3269**（每子軸見 env 旋鈕 + memory）→ M38/M39 殘渣收束 → **M40 reconstruction RED-confirmed**（quality 軸 converged）→ **M41-M50 RuntimeFactor 軸七槍**（M41 砍 swap → M42 砍 n>100 build 冗餘 → M45 band tiers + cores-adaptive → M46 C++ hot-path exact → M47 wrapper overhead → M49 REFINE band-cut → M50 mid-band 兩層 tier；local 1.3269→**1.3285** RF=1.0 fiction、avg 9.89→**1.49s**、投影 real @12c M=11 combined ~0.975；詳見戰略結論 §4 + memory `[[m41-runtime-factor]]`…`[[m50-midband-refine-tiers]]`）→ **M51 wide-CLAMP quality add** 1.3269→**1.3265**（案 99 破，見 §「目前狀態」）。

## 🔑 戰略結論：quality 三面天花板皆探畢；**RF 軸七槍已 ship（M41+M42+M45+M46+M47+M49+M50）**

1. **ordering / ML 永久封卷**（M26 oracle-perm，`oracle_perm_probe.py`）：注入完美 fp_sol 排序，placer 只多拿 +0.002%（類內）/ +0.005%（全域）→ 瓶頸是 **placer**（greedy+compact+push）非 pack order。⇒ refinement pair-relocation / order-LNS / 監督式 ML ranking 全不值得。⚠️ 誠實範圍（A6 2026-07-17 補）：oracle-perm 只測 **yx/xy 兩 scalar sort key**（`oracle_perm_probe.py:39-40`），不覆蓋 anchored first-pass 內部順序（`constructive.cpp:630-638`）——該洞已由 M60 補診：first-pass 根本不產生 boundary 容量衝突（前件空集，見 ledger M60 條）→ 封卷結論維持。
2. **更好的 packer 封死**（M27 global-packer，`dbg_seqpair.py`）：greedy 已在 (area,HPWL) frontier；agap 與 hgap **結構耦合**（wire-driven 花 area 換低 HPWL）+ cluster/preplaced 強迫 void → B*-tree/SP/skyline 重寫不值得。⚠️ 誠實範圍（A6 2026-07-17 補）：`dbg_seqpair.py` 把 cluster 打散、est_cost 非官方 eval、V_rel 固定、預設只跑 5 案——frontier 結論是該近似語意下的證據；M59/M61 以官方 strict eval 攻鄰近軸（refine 種子 / event frames）亦全 RED，方向一致。
3. **reconstruction RED-confirmed**（M28 GREEN→M29 YELLOW→**M40 RED**）：headroom +0.219（oracle 1.1079，100% 在 quality，top-15 大案佔 68.9%）但 X 結構不可從 connectivity 還原（M40 Spearman 0.009）+ Y 序需 label（M40 deterministic +159% vs oracle-Y）→ 重寫 slicing placer 不可行。詳見死路 ledger + `[[m40-reconstruction-red]]`。
4. **RuntimeFactor 軸（M41-M50 七槍已 ship）**：以上三點全是 **quality**；計分式還有 `max(0.7,R^0.3)` 一項，本地 eval 強制 =1.0 而被全程忽略。`cost∝t^0.3` + 大案 quality 卡死 → 砍 wall / 加速 = **median-independent** real-score 增益（同案 `Q_cap/Q_full < (t_full/t_cap)^0.3` 即贏）。七槍：**M41** 砍 swap、**M42** 砍 n>100 build 冗餘（per-big-n）、**M45** band-scoped tiers（tier-3 universal 中案 band + tier-4 cores≤8 深砍；gate = strict selection-preserving，逐案 cost 相等 → ∀median ∀cores 弱贏；cores 偵測 fail-open）、**M46** C++ hot-path exact 加速（每案 t'≤t 無條件弱贏、零 local 代價）、**M47** wrapper overhead、**M49** REFINE band-cut（n>100 K→4，+0.027% local 全來自 case 85）、**M50** mid-band 兩層 tier（universal K=8 + 低核 K=4）。投影 real @12c M=11 combined ~−33%、n>100 median-independent WIN、最壞（官方忽略 RF）僅 +0.09% local。逐槍細節見 memory `[[m41-runtime-factor]]`、`[[m42-runtime-2nd-order]]`、`[[m45-band-tiers]]`、`[[m46-wall-setter-speedup]]`、`[[m47-wrapper-overhead]]`、`[[m49-refine-band-cut]]`、`[[m50-midband-refine-tiers]]` + `rf_score_model.py`。

## 未來發展方向（M51 後；2026-07-17 更新）

> 現況總覽見上方「範式轉移·現況一句話」＋「目前狀態」＋「戰略結論」。以下依 ROI 列進行中／未探明方向：

0. **🎯 M53 三層計劃（目標 local <1.3 已達 offline；使用者方針：分數優先、一切 env-gate、送件檔位不動）**——勿重掃細節一律見死路 ledger「M53 L1/L2/L3」條：
   - **L1 ✅ SHIPPED（`[[m53-l1-quality-mode]]`）**：1.3248→**1.3176**（−0.549%）、100/100 feasible。復現 = `ICCAD_ADAPTIVE_POOL=0` + `ICCAD_L1_POOL=1` + `ICCAD_PROFILE_TIMEOUT=600`（84-pool = 41 base + 2 `_L1_EXTRA` + 41×K24 tier）。回歸閘：default eval 逐位、0 movers → 送件檔位不動。逐案基準 `results_L1_final.json`；**L2/L3 一律 A/B vs 1.3176**（diff 工具 `m53_diff_results.py`）。**M65 補（2026-07-19）**：84 池缺兩隻 `_L1_EXTRA` 的 K24 版（`_L1_BASE` 快照在 extras append 前，`optimizer_constructive.py:377`）——已補量 **RED**（pilot 15 重案 0 mover；K24 於 extras 贏案逐位 no-op、輸案全毒）→ 池維持 84 不動、勿再補，見 ledger M65 條。
   - **L2 ❌ RED（`[[m53-l2-stochastic-red]]`）**：stochastic best-of-N union oracle **+0.2377%** < bar 0.3% → 砍。逐決策噪聲全域毒（greedy 零容錯）、權重 jitter = 連續 profile 空間 8/15 重案恰 0。**但 L2 樣本作 L3 LP 種子已證 GREEN**（cache `m53_l2_cache.pkl`）。
   - **L3 ✅ GREEN 達標（`[[m53-l3-lp-green]]`）：constraint-graph LP 後處理**（固定拓撲 + scipy HiGHS 全域最小化 exact 線性 HPWL；delta 變數、boundary 等式鎖 bbox、cluster 剛體平移 shapely 驗+破則凍結）：winner-only +0.76% → **LP×portfolio 重選**（top-32 飽和）1.3176→**1.30035**（33/100 換 winner、realizable=oracle）→ 疊 L2 種子 → **1.2978 <1.3 達標**。shipped 錨 winner-only 對照 +0.8697%。boundary-repair **0 可修**。工具 `m53_l3_probe.py`（gate0/probe15/full/port15/portfull/repair/l2stack）+ `m53_l3_cache.pkl`。
   - **帳**：L1 1.3176 → L3 1.30035 → +L2-stack **1.2978** ✓（離 oracle 1.1079 尚 +0.19）。**送件路徑 M54 全結案 → in-window RED**（見 ledger「M54」+ `[[m54-lp-inwindow-red]]`）：RF 全域淨虧、β=0 kill test 亦死、proxy-guard deployable 形本身 GREEN（+0.891% on shipped、0 回歸）但死在 RF。復活條件三路（M62 更新 2026-07-18）：官方 median ≥~11s／官方 RF=1.0／**M62 micro-cap skip 路徑**（cap≈0.1s it1 skip 零加速即全 cell 弱贏 +0.06%、cap=0.25 upside +0.36%；前置 = pre-build 時間預測器 + 機速校準兩 probe 過關，見 ledger M54 條補註 + `M62_REPORT.md`）。**L3 = 離線錨專用（in-window 檔位仍不動）。**

0b. **M52 ML 軌 Phase 0：RED**（2026-07-11 訓練前判死，`m52_phase0_probe.py`；`[[m52-phase0-red]]`；機制細節見 ledger「M52 tree-space」）——三量測：(i) **管線 GREEN**（true tree+oracle-Y+dims → decode/legalize → 官方 eval **260 案 100% 逐位重現 fp_sol**、2-3ms/案）；(ii) **容錯帶 ≈ 0**（單一 near-miss token 錯 wR **1.232**、理想 soft 修復仍 **1.072**；2% token 2.71；dims 任何擾動 ≈2.9-3.1 且 ε→0 已滿高；機制 = 27 boundary-touch/案 + cluster abutment 對任何幾何誤差歸零 → `exp(2·vrel)`×2-3）；(iii) **legalizer v0** 硬可行修復 100% 但 quality 不可修（model +213% ≫ interact +42%）。疊 `recon_index_probe` 零訊號（Spearman ~0.01）→ **imitation 家族全滅（含 direct-layout 模型 B）**；倖存 ML 形態只剩 cost-driven sampling = 歸 M27 packer 軸。**勿再嘗試任何以 fp_sol 為監督的生成/回歸。**正面資產：dims = integer divisor pointer（vocab p50=2/p95=6）、decode+legalize 管線、`m52_phase0_cache.pkl`。⚠️ 任何 layout 級評分必用 `m52_phase0_probe._cost_strict`（`tree_decode_probe._cost_of` 不查 preplaced/fixed 硬約束）。

0c. **M57-M61 codex gap 五 probe：全 RED**（2026-07-16/17；spec `M57_PLAN.md`、報告 `M57_REPORT.md`…`M61_REPORT.md`、memory `[[m57-frozen-reanchor-red]]`…`[[m61-event-frames-red]]`）——外部 codex 報告的行號級 code 主張**全部屬實**，但分數價值全零（bar 0.05%）：M57 frozen 重錨 41/41 LP infeasible（+0.0000%）、M58 nsoft 分母 live 但 weighted −0.0001%、M59 refine-rejected 種子 +0.0055%、M60 anchored 牆容量前件空集、M61 event frames +0.0001%。offline 錨維持 **1.2978**、送件形維持 M51 shipped。細節與勿重試邊界見死路 ledger 五條；附帶更正已同步：boundary-repair 三語意封卷（ledger）、M26/M27 誠實範圍加註（戰略結論 §1/§2）。

0d. **M63 codex A3 boundary-regret beam 前置上界稽核：ABOVE-BAR / RED-leaning，未開工**（2026-07-19；`m63_vio_bound.py` + `results_M63_vio_bound.json` + `M63_REPORT.md`；`[[m63-vio-bound-above-bar]]`）——純分析、無 C++、無 beam code。逐案逐 bit 分解 shipped M51 violation numerator（gate0 全綠：cost/vrel/n_soft/總分逐位重現）：**186 vBd = 43 frozen + 113 cluster-member + 30 movable-single**、vCl 19、vMb 0。三層上界（只削 numerator、HPWL/Area/RF 不動）：T1（歸零 singles）**−1.40%**、T2（歸零全部非 preplaced vBd，判定用）**−5.11%**、T3（+vCl+vMb）−5.71% → **T2 ≫ bar 0.3%，依預註冊規則不免實作判 RED**。但 **pool oracle**（`pool` 模式，audit_cache 42 profiles × 100 案逐 combo 分解）反證：31/100 案存在更少 movable-bits 的 profile、15 案可全清（case 89 的 #22 fc-wide 全清 4 bits），**29/31 淨 cost 變差**（中位稅 +5%、極端 +30~42%；case 89 淨 +2.68%）、唯二淨贏 = M41 剪掉的 ORDER_SWAP 家族（已知 POOL=0 trade）→ **全 pool 加權 realizable 僅 0.0012%**。機制與 M61 同構（清 bits 的收益被 outline/HPWL 稅吃掉）。**開工前必過的便宜 kill-test**：對 pool 的 bit-clearing 候選（positions 在 audit_cache）跑 M53 L3 LP 回收 quality 稅——LP 後仍輸 shipped → 補 RED 進 ledger；翻正 → 才評估 beam 工程。top-5 目標案 89（0.559%）/85/94/76/91；71 案有 movable bits、top-10 佔 T2 ~58%。附帶更正：M60 ledger「硬案五案無 movable violator」該句限 anchored 域（case 89 有 4 個純 movable cluster g1 violator，M60 trace 域外，見 ledger M60 條更正）。

0e. **M64 L3 錨相鄰拓撲 flip：RED（pilot gate）**（2026-07-19；`m64_flip_probe.py` + `m64_cache.pkl` + `M64_REPORT.md`；`[[m64-flip-red]]`）——M59 stage-2（relation flip）正式量測結案：從錨 `results_L3_port_top32_area.json` 逐案 winner 出發，unit-pair 級強制替代 separation disjunct（其餘 pair 沿用 max-gap）+ HiGHS 重解 + 官方仲裁。pilot 3 案（85/88/91）× top-24 pairs × 3-4 方向 × 2 variants（strict / bbox +0.5% area cap）= **529 flips、0 movers（>1e-6）** → 依預註冊 gate 停止。死因：**86.8% cone 空**（459 attempt-1 LP infeasible；diag 0/15 拿掉 boundary 等式仍 infeasible → 鎖死者是其餘數千 pair 的 fixed-disjunct 鏈＋bbox，**非** boundary-preplaced 等式）、可行 12% 全 worse/tie（**HPWL 稅** d_hgap +3.2e-4 ≫ area 稅；全 pilot 最佳僅 −8.2e-13 tie 級）、**62 個 feasible 的 vrel 全部恰 0**（相鄰 cell 動不了 violation bits，與 M57/M63 一致）。細節與勿重試邊界見死路 ledger M64 條。

1. **已 ship 里程碑（詳見 memory + 死路 ledger）**：RF 軸 M41-M50（見戰略結論 §4）；送件 hardening M43（`[[m43-submission-hardening]]`）+ M48（`[[m48-submission-hardening]]`）；quality M51 wide-CLAMP（`[[m51-wide-clamp-frame]]`）；BP_WEIGHT 雙向全死（ledger）。**殘餘 RF 已探底、勿再嘗試**：勿在 M≥11 疊更多 wall cut（floor 飽和只付 quality 不收 RF）、**候選生成 sort/TOL 比較器絕不可動**、frame-B&B exact RED（ledger）、高核 pool-prune RED（`[[teammate-prune-v1-zero-gain]]`）；任何新 pool-prune 須過 `rf_score_model.py` M45 strict gate。
2. **Beta/Final 送件前重跑**（回歸閘）：`m48_coldstart_dryrun.py` 四 phase + `rf_score_model.py` asserts + `m49_refine_probe.py variant 4 big`/`variant 8 mid`/`variant 4 mid`（control 逐位 gate）；或一鍵 `regression_suite.py`（`[[regression-suite-tool]]`）。
3. **若要再純-quality 追分**：in-space 殘渣已由 M51 清空、§0b ML 軌 RED → 走 §0 M53 三層（offline 錨已達 1.2978）；其外只剩未知 constraint 結構洞察（codex-gap 五洞已由 M57-M61 全數探畢、全 RED，見 §0c；錨相鄰拓撲 flip 亦由 M64 測 RED，見 §0e），或（唯一條件式重開）取得 rival 1.29 方法細節後走 M27 packer 軸評估。

### 精度 / 數值（持續遵守）
任何新加的、會被 shapely 評分的幾何輸出都要保持精確 abutment + `%.17g`（見 Gotchas）。

## 死路 ledger（勿重試）

- **boundary aspect port 到舊 SA**（2.50/0.40）：3.3258→3.4255 退步（skyline ≠ 組員 shelf，tall block 成 cliff）
- **preplaced-aligned frame**（攻 case 89）：greedy 不下 tighter width，case 89 結構性無解
- **cluster-rigid pack/slide**：cluster 無 slack（100 案僅 1 能動）+ 剛體平移破壞 abutment → shapely 假 fragment（**M10 精度牆，任何移動 cluster 成員的後處理都撞**）
- **violating boundary 修復（M57 後三語意封卷，2026-07-16）**：(1) 單 block 移向邊（`dbg_vio_stats.py`）：202 violating 0 可修（123 cluster+45 preplaced+34 single 全 BLOCKED）；(2) LP 全域同動、block 移向邊（M53 L3 `repair` 2026-07-13）：0 可修——⚠️ **範圍改窄（A6 更正）**：此量測限 **movable violator**、每 pair 單一 max-gap disjunct（**fixed-disjunct**，表達不了 relation flip）、**non-growing bbox**（shrink-only）、**逐 violator greedy 且須立即 official-cost 改善**才 accept →「0 可修」= 該語意下 0 accepted，非全語意證明；(3) **LP 全域同動、邊移向 frozen block（M57 重錨，`m57_reanchor_probe.py`）：41/41 infeasible、0 可修**。M57 死因分解：61%（28/46 bits）frozen-vs-frozen 幾何（更外側 frozen block + 同側 frozen 已滿足 boundary block 釘死舊邊）= **語意無關絕對死**；39%（18/46）固定拓撲 separation 鏈死 = 只在拓撲凍結語意下死，relation-flip 類理論上仍可攻，但 movable 已由 (2) 證死、frozen 61% 絕對死 → 期望值極低（M59 第二階段依 kill gate 一併不做）。residual vBd 只能 packing 階段擺對
- **per-frame compaction + csc 重估 frame**：csc 固定 hw 跨 outline 失準（拿 vCl 換 vBd）
- **reframe**（`ICCAD_REFRAME`）：與 portfolio aspect 多樣性結構性冗餘（code 保留 gated off）
- **env knob 軸**：WIRE_MULT 4/6、ANCHOR 0.30、ultra-narrow frame、WT/BFS/NORM/PIN 組合、CLUSTER_ORD、OM×tight 全 ≤0.063%（⚠️ M32 例外：pure decoupled LR=4.5 +0.186% 已 ship）
- **per-block FREE_BOUNDARY（M32）**：0.000%——greedy 局部 area 項偏好窄塊（與 edge-capacity 要的「統一變扁」反向）⇒ boundary-aspect win 須 **UNIFORM**（profile 級），per-block 死，code 已 revert。uniform 版 = M32 ship
- **cluster aspect uniform 飽和（M33，勿重掃）**：standalone 弱（2.0=+0.14%），win 須 stack tight+FREE+PIN，寬 ratio 共振峰 **3.0**（+1.105%）；已加 ca{2.0,0.6,1.25,0.4,3.0}
- **cluster per-member 飽和（M34，勿重掃）**：共振 **4.0**（PIN +0.466%）、5.0 退步、narrow(max2.0) 僅 +0.143%；已加 fc_pin_tight + fc_gm_pin
- **MIB-member 飽和（M37 ship + M38 收束，勿重掃）**：共振 ungated-wide **5.0**（89/61 ship）；tall 0.25→79 +0.027% below bar；M38 gated complement sweep 全 below bar（唯一 mover 79 gated-6.0 +0.033%）。per-case 選 ratio 不需 arbiter（portfolio 即選擇器）
- **FREE_CLUSTER boundary-ungate（`ICCAD_FREE_CLUSTER_BND`，M39，勿重掃）**：鏡像 M36 anchored-BND（layout-key 仲裁、build-time wall-free）。**LIVE 但 below bar**：92/100 案 / 391 members、layout-key 確實挑更好 boundary aspect（非 M32 純死），但贏案全低權重小/中案、硬案 89/97/85/82/88 任何 ratio 零移動 → 三 stack best +0.034% < bar → **revert**（3 行可復原：static 宣告 + gate `(boundary==0||FREE_CLUSTER_BND>0)` + env parse）
- **reconstruction 遞迴二分 slicing builder（`recon_slice_probe.py`，M40，勿重試）**：reconstruction **RED-confirmed**。M29 只證偽 greedy 插入 builder（8.22）；M40 補做正統 spectral 遞迴二分 slicing（Fiedler-median cut、region-aspect 切向）配 M29 `render_bstar` exact-X + oracle-Y。**雙閘各自獨立證死**（self-check `trueTree_oracleY` quality=1.0000 重現 fp_sol）：**Gate A（P1 X 還原）FAIL**——slicing-X+oracle-Y quality **2.05**（ceiling 1.0、我們 1.274）、movable X-order **Spearman 0.009**（pin-free 零相關）；**Gate B（P2 deterministic Y 序）FAIL**——即便給 **true tree X**，5 個 deterministic Y 序最佳 +**159%** vs oracle-Y（Y 需 label）。任一死即 RED。⚠️ 全集 100% 有 preplaced（mean 2.7、無 pure-movable）→ builder 須 obstacle-aware；但 Spearman 是 movable-only/pin-free，preplaced 救不了此 kill
- **RF 三階 @高核（組員 codex prune V1 #19/26/27、我方候選 #8/12/26/27，2026-07-02 驗證）**：CORES=12 下 10 案 n>110 全部 `tCap==tM42`（被砍隻無一 wall-setter）→ **增益恰 0.00%**；quality 僅靠 #19/#21 case-90 proxy 完全並列（2.766374981）才零損。⚠️ **此 RED 是 cores-conditional**：低核（≤8）wall 翻 sum-bound → 同類砍法 −3.7~−9.3% → **M45 tier-4 已 ship cover**（strict gate、#19 as winner 恆保留、cores 偵測 fail-open）。高核 regime 勿再嘗試 pool-prune 攻大案 wall（max-setter 結構設底、(100,∞] candidates=0）。任何新 pool-prune 須過 `rf_score_model.py` M45 **strict selection-preserving gate**（逐案 cost 相等；WIN 不等式在 RF floor 附近會高估）——winner tally 單獨不足（tie/hmin 耦合會誤判）
- **frame 級 B&B 早棄（2026-07-07 code 驗證，勿投資 LB 證明）**：M46/M47 曾掛 conditional GO（「需先證 layout_score 對 partial layout 有單調下界」）——靜態驗證三事實即死：(1) `constructive.cpp:1568` `guide.swap(c2)` **無條件**執行，12/13 的 refine packs 是 guide 鏈環節（sc 沒改善也 load-bearing），跳過任一即非 bit-identical；(2) 可 exact 早棄的只剩非首 frame 的 initial pack = 3/52 packs ≈ **5.8% 上限**（OS/OM 候選 pack 本可 exact 棄，但 M41 已砍出池）；(3) `run_frame` 在 `layout_score` 前跑三個 nudge（`:1490-1492`），`final_boundary_nudge` 把 miss 的 boundary block 搬到 frame 外——若該 block 原撐 xmax 則 **bbox 反縮**、HPWL 雙向 → partial layout 對最終分數**無單調下界**。殘餘 exact 只剩 refine ≥2-cycle 偵測（opt-D 只證無 fixpoint）→ 已由 M49 證 RED（見下條）。見 `[[frame-bnb-early-abandon-red]]`
- **REFINE exact 截斷 / refine cycle 快轉 / max_trials=3（M49 量測 2026-07-07，勿重試 exact 版）**：RTRACE 全量曲線（20 案×13 profiles×4 frames）證三件事：(1) **無 exact REFINE 截斷點**——17/260 勝出 frame 的最後改善落在 pass 11（refine 改善不飽和、fat tail），K<12 必有案變；(2) **≥2-cycle 僅 2/1040 鏈**（case 94 #17 period 6、case 96 #12 period 2、0 fixpoint）→ 偵測快轉期望 ~0.09% band wall，不值得；(3) **max_trials=3 非 exact**（第 4 frame 勝 15/260）且 M≥11 floor 飽和邊際 ≈0 → 緩辦。quality-trade 版（非 exact）已由 M49 ship（K=4）；勿在 M≥11 再疊 wall cut（floor 飽和只付不收）。數據重現：`m49_refine_probe.py trace`。**M50 mid 補量（2026-07-09，勿重掃）**：`trace mid` 1000 pairs 同樣**無 exact 截斷**（40 勝出 frame 改善於 pass 11、K-coverage 到 REFINE_ITERS=12 才全 exact）；**cycle 在 mid 極多（538 events vs big 的 2）但快轉仍 RED**（要動 shipped C++ + band 大多 RF-floored）；**ungated K=4 mid 勿 ship**（11 movers、+0.069% local、@≥12c M≥11 淨損 +0.06~0.07%——floor 飽和 quality 損直接穿透）；**K=6 不必量**（movers ⊇ K=8 且含 41/42 +3.9~4.3% 大 mover、低核 upside 已被 tier 覆蓋）；max_trials 於 mid 同樣非 exact（992/1000 用滿 4 frames）。ship 版 = universal K=8 + cores≤8 K=4（M50）
- **OS K>16 / OM K 組合**：K=16 飽和；**OS16×free（M36）** +0.630% 但 48s/n=120（4× OS16，增益與 4× runtime 結構耦合，OS16+PIN+tight 無 free=+0.001%）；runtime 候補 om16/os24/os32 懲罰比不划算
- **compaction 方向偏好 / pack 向 connectivity 重心**：compact_layout 已對稱試 4 單向+8 兩步由 csc 仲裁；wire 已是動態重心
- **BP_WEIGHT 30000→1M 無變化**（無可行 bp=0 位置）；**M51 補：向下亦全死**（10000/3000/1000/300 standalone 0 wins；3000 疊 M37 recipe / fc_pin_tight 兩 stack 全 0，M37 stack 甚至倒退）→ **BP_W 軸雙向封卷**；**試更多 frame** all-frames→2.42（4-5 frame 最佳）；**wire×50000**→1.93；**wire_order**→vBd 390
- **FRAME_ASPECTS（M51 量測 2026-07-10，勿重掃）**：tall `0.67-0.33`（pre-M25）與 narrow `0.55-0.28`（M13）**早掃過**（僅 `tall_asp7_anc` 存活）；M51 補 wide/mid——**standalone 全 ~0.000%**（wide 2.2/2.6、wide-only、mid-fill 1.15/0.85、no-square、mild-tall 0.85/0.7 六集合；僅 case 9 n=30 零權重）；win 只在 **stacked**：wide ≥2.0 疊 fc_pin_tight 觸發 **clamp-frame**（`frame_candidates()` `pre_*+MARGIN`/`max_i*+MARGIN` 下限把 2.0/2.2/2.5 clamp 成同一 outline → 逐位同值 = 值不敏感、勿掃更多 wide 值）破案 99；M37 anchored host 較差（1.3129 vs 1.3084）；保留 1.8+2.2 變體亦同值（非「移除 1.8」效應）。**結構洞已封**：n≥60 只試前 4 frames（`max_trials`、等面積 tie-break 近方形優先）→ 預設集第 5 值 0.55 從未被試，no-square 集讓它被試 = 0 增益
- **FREE_RATIOS 加寬（M31）**：整池 oracle 僅 −0.044% + 抬牆 21→29.6s（free 搜尋 ∝ n²，結構耦合）→ 全軸死
- **proxy/_RH（M31 再確認）**：oracle-min==proxy(_RH=1.4)==1.3269 零漏分 → 要降分須降 oracle 本身
- **ML shape / perm ranking**：oracle 上限實驗 = BL packer 是天花板（perm+SA 3.27、shape only 3.42）
- **M52 tree-space 生成軌 / imitation 家族（Phase 0 量測 2026-07-11，勿重試）**：`m52_phase0_probe.py` 三量測：(i) 管線 GREEN（260 案 100% 逐位重現、decode+legalize 2-3ms/案）；(ii) **容錯帶 ≈ 0**——單一 near-miss token 錯 wR **1.232**（理想 soft 修復後仍 **1.072**）、random-slot 單錯 1.319、2% token 2.71、Y-order 2% 1.73、**dims 任何擾動（jitter/量化皆然）≈2.9-3.1 且 ε→0 已滿高**；懸崖機制 = boundary-touch（27/案、1e-6 bbox-edge）+ cluster abutment 對任何幾何誤差歸零 → `exp(2·vrel)`×2-3 疊 X-rule 傳播 gap；(iii) legalizer v0 硬可行修復 100%（構造保證、self-inflation=0）但 quality 不可修（model +213% ≫ interact +42%）。**疊 `recon_index_probe` 零訊號 → imitation 全滅，含 direct-layout 模型 B**（懸崖與表徵無關；v2 MSE + M26 注入 cap 封「模型出座標」變體）。倖存 ML 形態只剩 cost-driven sampling 搜尋 = 歸 M27 packer 條目管轄（勿以 M52 名義重開）。⚠️ 附帶 gotcha：`tree_decode_probe._cost_of` 不查 preplaced/fixed 硬約束（`target_positions=None`），任何 layout 級評分實驗必用 `m52_phase0_probe._cost_strict`
- **M53 L1 量測（2026-07-12，勿重掃）**：(1) **REFINE 向上全域 override 死**——`ICCAD_REFINE_ITERS=24` 全池 +0.105%、`=48` +0.083%（毒案 84 +0.053 / 73 +0.085 cost；layout_score≠true cost 非單調，同 frame-B&B RED 機制）→ 向上只能走 **portfolio tier**（M38 教義；K24 tier oracle −0.053% 已 ship 進 L1 池）；(2) **K=48 tier 邊際 −0.010% < bar**（疊在 K24 上）勿加；(3) **`PUSH_PASSES`/`COMPACT_ITERS`=64 逐位 no-op**（兩迴圈 early-break、8 內全收斂；constructive.cpp `:1097`/`:1307`）→ 該二軸向上封卷；(4) **fa22+OS16 host +0.022% < bar**（贏案 79/64/35 全被 fc_pin_tight+OS16 拿走或並列，殘餘 99→1.3058/47/18）勿加——但 case 99 的 1.3058 證明最重案還有肉，L2 目標名單用
- **M53 L2 stochastic best-of-N（2026-07-12 量測 RED，`m53_l2_probe.py` + `constructive_l2.cpp/.exe` 副本 + `m53_l2_cache.pkl`，勿重試）**：union oracle **+0.2377%** / realizable **+0.2345%** < bar 0.3% → 砍。證據鏈：(a) **σ=0 gate 90/90 byte-identical**（副本 vs shipped）、phase0 15/15 案 proxy 選擇 cost 逐位重現 `results_L1_final.json` + winner positions 逐位比中（85-99 winner = #12/41/19/27/26/42/2/23/17/34/42/53/41/21/40）；(b) **機制 B（per-candidate 乘法 score 噪聲，pack 三站點）全域毒**：σ∈{0.01,0.03,0.1,0.3} × 5 案 best-of-24 全部退步（+0.007~+0.18）且隨 σ 單調惡化 → **greedy packing 決策鏈零容錯，任何逐決策擾動空間全滅**（呼應 M27 frontier + M52 零容錯帶；tie-break/softmax 變體不必再試——σ→0 極限=baseline，無改善區間）；(c) **機制 A（per-run ANCHOR_W/WIRE_MULT/BP_W ×exp(σ(2u−1))）= 連續 profile 空間**：σ∈{0.05..0.7} 掃描 + top-15 × N=500 × σ∈{0.3,0.5}——兩配置收斂**同一離散改善集**（σ 只影響發現速度）：movers 僅 7/15（85 −0.0360、91 −0.0173、90 −0.0107、97 −0.0102、89 −0.0070、92 −0.0041、88 −0.0002），**8/15 案（含最重 99/98/96/95/93）1000+ samples 恰 0**；曲線 N=16-32 飽和（89/91 遲至 256/500 現身、邊際 ~0.02%/oct）。case 97 sample 達 vrel=0（重採樣真能修 violation）但 cost 增益不足。**proxy→sample 選擇成立**（98.7% 捕獲、僅 case 94 誤選 +0.0004）。未掃（誠實範圍）：frame-選擇噪聲（FRAME_ASPECTS M51 已封）、order 噪聲（M26 oracle-perm pre-kill）、compaction/push 噪聲（cascade-RED：post-proc 佔 wall 2-7%）。撿回路徑（若要）：+0.24% = 1.3176→~1.3145 offline 錨，ship 形 = env-gated sample tier + 下檔保護。**2026-07-13 註：standalone RED 不變，但 L2 樣本作為 L3 LP 種子已證 GREEN**（case 91/97 疊 LP 共 +0.19%，見 L3 條）
- **M53 L3 已探畢事項（2026-07-13，勿重掃；`m53_l3_probe.py` + `m53_l3_cache.pkl`）**：(1) **top-k 飽和於 32**——top-84 realizable 逐位=top-32（1.3003478581），rank 33-84 零貢獻、oracle 也僅 +1.3e-5，勿再加深；(2) **area 聯合目標邊際 +0.026%**（一階線性化已 ship 進 probe 預設路徑；placer compaction 已把 bbox 壓得夠緊）；(3) **LP 迭代 2-4 pass 即 fixpoint**（8 pass 上限下大多 rej 收斂，勿加深）；(4) **boundary-repair 0 可修**（見 violating boundary 條）；(5) LP 預測 vs 官方 evaluator HPWL 誤差 ~1e-15（模型 exact）、cluster 精度 ladder 觸發率低（att=2 少數案）且全數收口——M10 精度牆在「LP 等式 + 剛體分量 + shapely 驗證 + official guard」組合下不是障礙
- **M54 in-window LP 後處理（2026-07-13 量測 RED，`m54_lp_rf_model.py` + `m54_cache.pkl` + `results_M54_dep_lp.json`，勿重試 ship 形）**：deployable-shaped 全鏈量測。(1) **proxy-guard GREEN（正面資產）**——決策路徑零 official-eval（keep iff proxy hpwl/area 改善 ∧ 三指標不惡化 ∧ 硬檢查：鏡射 `check_overlap` 1e-6 雙軸 + dims/preplaced 凍結）：shipped 1.3265→**1.31466（+0.891%）**、100/100 feasible、0 回歸、優於 official-guard 版 1.31494（area objective 生效）；iters=1 已拿 +0.80%。(2) **RF 投影全域淨虧**：tLP（scipy）p50 0.18s/p90 1.37s/離群 96=13.8s、91=12.5s（solve 佔 87%）；gate=all @12c M=8 **+15%**、cap=1-2s abort/skip 仍 +2~+8% @M=6-8、**任何 band×cap×iters×skip 組合皆無 strict weak-win**（唯一 weak-win = LP 永不觸發的退化 gate）。(3) **β=0 kill test 決定性**：solver 免費、只付 build+guard ~0.2-0.4s/案 → 最佳 gate 最壞 cell 仍 **+0.48%**（M50 doctrine bar +0.03% 的 16 倍）→ **C++ solver / x-y 解耦 network-flow dual / 向量化組裝全部不必做**。(4) oracle per-case gate 也只 @M=8 −0.26%。機制：M=6-8 時多數案在 floor 懸崖邊（0.305·M≈1.8-2.4s vs walls 1.5-3.6s），加時稅率 `0.3·dt/t` 壓過逐案 <1% 增益；贏區僅 M≥11∧cores≥8（cap 後 −0.4~−0.9%）= 純賭未知 median，與 M41-M50 median-independent doctrine 相反。**復活條件**：官方 per-case median 證實 ≥~11s（flip cap gate 拿 −0.4~−0.9%）或官方 RF 證實 =1.0（純贏 +0.891%）。**M62 break-even 補判（2026-07-18，`m62_break_even.py` + `M62_REPORT.md`，cache-only 純分析）**：本條「任何 band×cap×iters×skip 組合皆無 strict weak-win」**範圍限 cap≥0.5s 網格**——α(build+guard)×β(solve) 全網格掃描證 **α\*=1.0 全 β 欄**（零加速即贏）：cap∈{0.1,0.25}s **skip** gate 在 α=β=1 全 20 cells（M∈{6..20}×cores∈{4..16}）弱贏——cap=0.25 it1：RF=1 品質 +0.356%（76 案、集中 case 88 dt=0.239 離 cap 4%）、worst cell −0.014%、±30% jitter 27/40；**cap=0.1 it1：+0.060%、worst −0.033%、jitter 40/40、機速 s∈[0.5,1.5] 存活**。但 **abort-only（免預測器）wins=0** → 贏依賴 pre-build 時間預測器（size→time 回歸，與 M56 winner-identity 不同類、未建未驗）；**s≥2 全 gate 滅** → 非 machine-speed-independent（M41-M50 doctrine 偏離，需 wrapper 相對 cap 校準）。⇒ C++/向量化 build+guard 與 solver 加速軸皆 **moot**（不是不夠快、是根本不需要；加速只擴大安全 cap 內的戰果 β=0.1 → −0.694%）。四 sanity 全綠（anchor 逐位、M54 原 caps 無-win 重現、β=0 kill +0.480% 逐字、α=β=0 +0.8907%）。未動工；ship 前置見 §0 復活條件三路
- **M56 per-case feature-conditioned pool 砍法（2026-07-14 量測 RED，`m56_percase_oracle.py` + `m56_cache.pkl` + `results_M56_percase.json`，勿重試）**：oracle 天花板巨大但**不可實現**。(1) **winner-only oracle**（selection-preserving per-case 砍法的嚴格上界——proxy 已 oracle-min 故 Q 不可改善、singleton pool trivially 選 winner → Q 逐位不變、wall 降到 t_winner）：@12c M=11 **+3.87%**、@4c M=6 **+24.5%**、全 M×cores 矩陣弱贏；機制 = winner 幾乎從不是 max-setter（4/100 @12c）→ 連高核 max-setter-bound 的 big band 也有肉（wall 4.09→2.63s）。(2) **5-fold OOS 判死**：kNN winner-union（10 個 input-side features：n/fix/pre/bnd/clu/mib/logΣA/nets/pins）J* 在**全部 fold×regime×variant 為 None**——即便 keep = 全部 80 train winners 聯集（最寬鬆可行模型、mean kept pool 22.9），train-LOO index preservation 都到不了 100%；OOS strict-breaks **30/200（15%）**、最壞 case 96 @lo **dq +9.09%**、40/73/41/44 皆 +4~6%；+band variant 逐位同結果。機制：**winner 身分 case-idiosyncratic**——held-out 案的 winner 常不是任何 train 案的 winner（與 M55 band 集 40-48% strict-breaks 同根），input features 對 winner 零預測訊號（呼應 recon_index_probe 零訊號）。任何更緊的模型嚴格更差；加 feature 不改變「winner 不在 train label 集」的破口。誠實範圍：只判 selection-preserving 形（100% preservation gate）；quality-trading 形（容 break 換 wall）未量測——但 break dq 到 +9% 且不可預測，與 M45 strict doctrine 相反，勿以此復活。已知限制同 M55：cache dt = n>100 K=12 counterfactual（兩側同 dt，結論針對 selection 結構）。**M66 等價類補洞（2026-07-20 量測 RED，`m66_equiv_cv.py` + `results_M66_equiv.json`，cost 寄生 `m56_cache.pkl`，勿再開）**：本條 CV 的方法論不對稱屬實（`loo_J`/`keep_set` 用 singleton winner index、OOS gate 收 cost 等值替代）——以 E(case,regime)={k∈chain_pool: cost 與 winner 差 ≤1e-9 rel} 取代單一 label（LOO 接受判準 + keep-set 聯集）、其餘協定不動重跑：gate0（Jstar 20/20 None、breaks 30+30、winner 100/100×2 vs 存檔）+ selfcheck（singleton-E 走新管線 200×2 recs 逐位重現）全綠；E 化後 **J\* 仍 20/20 None**、OOS breaks **26/200**（僅 4 healed = case 37/38 兩 regime、0 new、最壞 case 96 @lo **+9.09% 不動**）、mean kept pool 23.0 ≈ **mean chain pool 24.4**（最寬鬆可行模型已留 94% 池 → 即使 0 breaks RF 增益亦歸零）；`diag` 分解：26 殘破口**全 LABEL-ABSENT 且 |E|=1、0 hmin-flip**——破口案 winner 為 singleton 等價類（連自己池內都無 cost 等值替身）→ 不對稱與失敗根因**正交**，「winner 身分 case-idiosyncratic」精化為「破口集中在 winner 等價類 singleton 的案子」。|E| 分佈：hi mean 1.39、|E|>1 佔 20/100、max 6 = FC×FREE×PIN 疊層家族 #22/#24-27/#40（gated 旋鈕於無對應結構案 = 恆等變換）——等價類真實存在但救不了 CV。封卷維持，勿再以「判準不對稱」重開
- **M57 frozen-violator bbox 重錨 LP（2026-07-16 量測 RED，`m57_reanchor_probe.py` + `results_M57_reanchor.json`，勿重試）**：「邊移向 block」語意（frozen violator 不動、bbox 邊釘到它、其餘 movable extremes 退讓；spec `M57_PLAN.md` §2 細節 (a)-(d) 全實作）全滅——100 案 41 個 frozen violators（31 案）**41/41 LP infeasible**、零 feasible vrel 下降、weighted +0.0000%。diag/diag2 鑑識：28/46 violated bits = frozen-vs-frozen 幾何死（同側 frozen 已滿足 boundary block 釘死舊邊 + 更外側 frozen block——任何語意都救不了）；18/46 = 固定拓撲 separation 鏈死（單 disjunct 表達不了 relation flip，dist 0.095~62.3 皆死）。唯一 FEAS（case 99 blk24 XMIN 單 bit pin）另一 bit 鏈死 → violation 不清、cost 反升、guard 正確拒絕（同時證 pin 等式建構正確）。合併 M53 → violating boundary 修復**三語意封卷**（見該條）
- **M58 compute_nsoft 官方分母修正（2026-07-16 量測 RED，`constructive_m58.cpp` + `m58_nsoft_probe.py` + `m58_cache.pkl` + `results_M58_nsoft.json`，勿重開）**：C++ `compute_nsoft` MIB 項 distinct-shapes−1 vs 官方 group_size−1 的 spec 落差**真實且 live**（影響面稽核：唯一 live 使用點 = `compact_layout` csc；`ICCAD_FRAME_CSC` 僅註解殘留無 code；METRICS cosmetic；Python proxy 已是官方語意）——修正後 53/2700 profile runs positions 變、29/100 案觸及、case 6（n=27）經二階路徑（非 winner profile 變 → proxy 翻 winner）拿 −3.31%——但觸及全集中低權重（n≤60 佔 22、**n>100 恰 0**；機制：官方分母 +2~+6 對小 nsoft 基底是大幅相對鬆綁、對大案是雜訊，而權重 e^(n/12) 恰反向），weighted delta 僅 **−0.0001%**（bar 1/500）；winner-profile diff 0/100（spec 原判準單獨即 RED）。**spec 落差存在但無分數後果**，ship 還要付 M51 級重驗鏈 → 封卷，勿再以「分母不對」重開
- **M59 REFINE rejected states → L3 種子（2026-07-16 量測 RED，`constructive_m59.cpp` + `m59_refine_seed_probe.py` + `m59_cache.pkl` + `results_M59_refine_seed.json` + `m59_rows.json`，勿重試；第二階段 dual-guided relation flip 依 kill gate 一併不做）**：rejected `c2` 確實全是新拓撲（251/251 pair-relation signature 互異、無一與錨/pre-LP 輸出重合）但品質盆地太遠：7 硬案 39 個 c2 種子 LP 後 **0 個**打贏錨（6/7 案 best 高於錨 +0.06%~+13.6%）；唯一 mover = case 88 的**替代 frame pre-refine c1**（f0 initial pack，非假說標的）LP 後 1.325391→1.323154（單案 −0.17%、weighted +0.0055% = bar 1/9）。機制：c1/c2 = pre-compaction/pre-push 中間態，凍結拓撲 LP 只平移、代替不了 compaction 拓撲改寫（L2-seeds GREEN 因種子是全管線輸出）。4/7 案 best 種子是 c1 非 c2 → 殘值在「替代 frame 早期州」= frame 軸（M61 已判 RED）非 refine 軸；postproc-stdin 回灌不啟動（離 bar 9 倍）。**stage-2 relation flip 已由 M64 正式量測 RED**（2026-07-19，見 M64 條）
- **M60 anchored first-pass 牆面容量（2026-07-17 診斷 RED——前件空集，`constructive_m60.cpp` + `m60_anchored_deficit.py` + `m60_forks.json` + `m60_cache.pkl`；beam/保牆排序/lookahead 任何變體勿做）**：anchored first-pass（`constructive.cpp:630-688`）**從未**吃掉 boundary violator 牆段——全 100 案 winner hosts 的「最終落在 anchored cluster 的 movable boundary violator」全集僅 **2 個**（case 1/7 最低權重帶，且 pack 當下 selected bp=0 在牆上 = violation 是後處理/官方 bbox 位移產物，`pack-time-satisfied`）；9 traced 案 26 個 movable violator first-pass 結束時需求牆段全開放（needed ≪ max free gap，如 case 85：12.3-24.2 vs 270.2）、fp-implicated=0、「更保牆替代候選」分叉零觸發；硬案 89/97/61/79/66 連 movable violator 都沒有（全 preplaced = M57 域）——⚠️ **M63 更正（2026-07-19）：此句限 anchored 域**——case 89 另有 **4 個純 movable cluster g1 成員 violator**（blk13/25/63/68，L1 與 shipped 逐位相同），純 movable cluster 走複合 item 路徑、不經 anchored first-pass → 在 M60 trace 域外（M60 診斷本身不受影響；97/61/79/66 無誤）。吃牆的是後面的 items loop → 歸 M27 packer 條目管轄（已封）。誠實範圍：牆=frame 邊（packer 語意）vs violation=官方 bbox 邊（1e-6）兩語意在 RIGHT/TOP 可有 slack（`pack-time-satisfied` 即其產物）；items loop 內部吃牆不在 trace 內
- **M61 blocked exact-edge → event frames（2026-07-17 量測 RED——拓撲真、分數零，`constructive_m61.cpp` + `m61_event_frame_probe.py` + `m61_cache.pkl` + `m61_events.json`；「per-case 動態 frame」「blocker-aware frame 掃描」等變體勿再試）**：`frame_candidates()` obstacle-blind 是真的、event frames（越過 blocker 的最小 ΔW/ΔH）97/97 對 host 全 frame 清單去重全存活、96/97 開出新可行拓撲——但 6 案 97 events 對 L1 live 基準最佳 **+0.0001%**（case 65，官方 cost 第 4 位小數 tie 級）、93 個持平或倒退、40 個崩 shelf-fallback（cost 5.8-8.8）：解鎖單一貼牆候選的收益恆被 outline 放大的 area/HPWL 稅吃掉。**下檔警訊**：case 99 兩 event proxy 判優於全 84 池、官方卻 −0.0008%/−0.0017% → 做成 live profile 會選到微退化。對照 M51：wide-CLAMP 贏在 clamp 把多 aspect 收斂**同一**寬 outline（免 area 稅）；反方向（微調尺寸繞 blocker）無肉 → **frame 軸殘餘自由度兩側皆探畢**。本量測已是上界語意（FORCE_FRAME 單 frame 走全管線；ship 形受 max_trials=4 排擠、event frame 面積必大於 base 多半輪不到試 → 更差）。⚠️ 基準 gotcha：host 自身 pre-LP cost 不可當基準（post-LP 重選常挑 pre-LP 較差 host）——case 85 假訊號 +0.08% 換正確 L1 live 基準後 −0.04%
- **M64 L3 錨相鄰拓撲 flip（2026-07-19 量測 RED——pilot gate 0 movers，`m64_flip_probe.py` + `m64_cache.pkl` + `M64_REPORT.md`；「單/少數 pair relation flip + LP」任何變體勿再試）**：M59 stage-2 正式結案。unit-pair 級 flip（rigid unit / free single / per-block frozen pseudo-unit；全跨 pair 的 separation row 替換成強制方向、鏡射 0↔1/2↔3；selfcheck 證佈線：force 當前方向逐位 = 無 force）從錨 winner positions 出發，3 案 × top-24（tightness×net-gradient 排序）× 3-4 方向 × {strict, bbox×√1.005 area+0.5% cap} = **529 flips 0 movers**：(1) **86.8% cone 空**（459 個 attempt-1 LP infeasible、零 ladder kill）且 **diag 0/15**——拿掉 boundary 等式重解仍全 infeasible → 鎖死者是**其餘數千 pair 的 fixed-disjunct 鏈 + envelope/bbox**，非 boundary-preplaced 等式（M57 「39% separation 鏈死」的拓撲版、鏈鎖率更高）；(2) 可行 62 個**全 worse/tie**，稅在 **HPWL**（mean d_hgap +3.2e-4 ≫ d_agap +8e-5）、約半數解回與錨 cost 逐位同值、全 pilot 最佳 −8.2e-13（tie 級，= mover bar 的 10⁻⁶ 倍）；(3) **vrel delta 0/62 恰 0** → 相鄰 cell 完全動不了 violation bits（呼應 M57/M63「可達恆付稅」）。誠實範圍：pilot 3 案（85/88/91）top-24/案（npk 全集 3157-4941 的 ~0.5-0.8%，按動機排序）；unit-level slide 粗化（強於 pairwise 要求，低估可行域——但主死因是 pair 外的 disjunct 鏈，非此粗化）；component-merge 默許（僅查數量增加）。多 pair 聯動 flip = 組合搜尋 = M27 packer 軸管轄（已封）。工具備 `heavy`/`full`/`l2base`（min(port32,l2stack) 誠實基準）模式，gate 未過未動用
- **M65 L1 K24-extra cells（2026-07-19 量測 RED，`m65_l1_cell.py` + `m65_cache.pkl` + `results_M65_l1cell.json` + `M65_REPORT.md`，勿重試）**：`optimizer_constructive.py:377` `_L1_BASE` 快照在 `_PROFILES.extend(_L1_EXTRA)` 前 → 84 池缺兩隻 OS16×free extras 的 REFINE=24 版（遺漏屬實，gate0 assert 寫死：池=84、兩 K24 combo 不在池）。env 疊加補量（profile + `ICCAD_REFINE_ITERS=24`、剝 ICCAD_* 衛生、官方 strict 仲裁）：pilot 15 重案（85-99）30 runs **0 mover**（bar 1e-6）→ 依預註冊 gate 停（full/l3seed 未動用）。機制：4 tie（86/97←ab、90/95←fc）= K24 輸出與錨 **positions 逐位相同**——錨 winner 正是對應 K12 extra、REFINE 12→24 在勝出案上 exact no-op（pass<12 已收斂）；其餘 26 runs 全退步（最差 case 93 −0.36 cost）= REFINE-up 全域毒逐案顯形（M53 L1 同機制：贏處 no-op、輸處是毒 → 期望值恰 0）。誠實範圍：mid-band extras 贏案（80/73/82/78/75/66、64/35）未量測，但 in-band 贏案已證 no-op、同族收斂期望一致。**附帶修復：commit 25f3eb0 誤刪 `proxy_analysis.py`（27 工具依賴——`rf_score_model`/`analyze_constructive`/`profile_vs_portfolio`/m53 portfull/m55-m61 probes——`regression_suite.py` gate 鏈整條斷裂；M62-M64 全 cache-only 純分析才未踩到）已自 `25f3eb0^` bit-exact 還原**；同 commit 刪的 `proxy_dbg.py` 確為 stale（零依賴）維持刪除
- **M67-D OOS 泛化預檢（2026-07-21，`m67_oos_probe.py` + `m67_oos_cache.pkl` + `results_M67D_oos.json` + `M67D_REPORT.md`；結論已納入，勿重跑求「更好的 raw」）**：訓練集 `floorset_lite`（從未參與調參）240 案（seed 67；validation 每 n∈[21,120] 恰 1 案 → 鏡射，K=2、n>100 K=4；per-n 先平均再官方加權），官方 strict 語意 + RF=1.0。gate0 全 PASS（in-set 3 案 cost+positions 逐位）。**(1) raw 1.6533 vs 1.3265（+24.6%）判 RED，但不是過擬合**——單 profile 參照 1.4775→1.8681（gap **+26.4% 比 portfolio 的 +24.6% 更大**）、portfolio 增益 in-set 10.22% vs OOS **11.50%**；語料本身更硬：label floor 1.2444 vs 1.1079（label 自身 vrel 0.1081 vs 0.0504）、boundary blocks 28.8 vs 24.0（max 46 vs 37）、preplaced 3.08 vs 2.59、b2b 1201 vs 994。訓練集 ≠ 測試集生成器（alpha 已證測資 = validation 逐位）→ **Beta 若沿用測試集生成器，raw 應接近 1.33 而非 1.65；勿用 1.6533 當 Beta 預期值**。**(2) 硬旗標全清**：100/100 feasible、零 SA/row fallback、零例外、runtime p50 1.54s / p90 2.44s（in-set 1.55 / 2.58）→ hidden-case robustness 無警訊。**(3) 🚨 主發現（`pool0`）**：M41/M42/M45+M49/M50 的 adaptive 切法品質稅 in-set **+0.106%（movers 3/20）** vs OOS **+2.825%（movers 52/80、shipped 較差 51、最壞單案 +11.9%）= 27 倍**——證實並超過 M55 CV 預言（break 65% vs 40-48%、最壞 +11.9% vs +9.6%、tax +2.83% vs +2.36%）⇒「strict selection-preserving ⇒ ∀median ∀cores 弱贏」**只在樣本內成立**，hidden set 上是真實品質交易。**(4) 但送件形不動**：pool-cut wall 比 2.50× @48c（`audit_cache` 兩側同 K=12；實測含 REFINE 為 7.28× @12c），以 alpha 校準（RF 0.7081≈floor ⇒ t≈0.30·median）→ `cost_full/cost_ship=(1/1.02825)×(0.921/0.700)=1.279` = 切法淨賺 **~28%**（含 REFINE 取 5× 則 ~58%）；翻盤只在兩側都撞 floor，即 median ≥ **8.2×** t_shipped（alpha 實測 3.28×，安全邊際 2.5 倍）。**(5) 交接**：M67-E 48c 投影**須用 +2.8% OOS 稅**取代 in-sample +0.1%；任何新 pool-prune 的 `rf_score_model.py` strict gate 結論都要自帶 ~3% OOS 折扣，勿再宣稱 hidden set 上 bit-identical。誠實範圍：pool0 只跑 n>100（81% 權重）且未分離 pool cut 與 REFINE band；48c wall 用 `max(max_i, Σ/cores)` 模型

- **M67-E 48c RF 投影（2026-07-21，`m67e_rf48.py` + `m67e_cache.pkl` + `results_M67E_rf48.json` + `M67E_REPORT.md`；cache-only、送件形零改動；勿再重跑求「更好的 s 估計」）**：`rf_score_model.py` cores 網格加 48 + 48c 結構表（asserts 未動、全綠）。gate0 五閘 ALL PASS。**(1) 校準修正**：alpha 送的是 M10 十四隻廉價 knob 池、逐案 grader t p50 0.673s ≈ 我們現行 shipped 量級 → `M_i = 3.161·t_i^alpha`（bracket 模型 B 常數 M=9.43s、97/100 clamped）；**M67-D 的 8.2×/2.5 倍安全邊際作廢**——實際貼著 floor 邊緣（重帶 6/10 觸底、h p50 1.0-1.3、**mid band 0/40 觸底**、加權 RF 0.7325 @s=1）。機速 s bracket [1.5,1.7]（皆上界，含 M10 binary 速差）。**(2) 48c 結構（分析即證）**：所有 shipped 池 ≤41 < 48 ⇒ **wall = max-setter，100/100 案**（`Σ/48` 僅 max 的 3-27%、crossover `c*` ≤27.2 → 24 physical 亦成立）；**48 核對重帶零加速**（n>100 在 12c 就已 max-bound）；M47 後 proxy 鏈 2.5ms/隻（ledger 舊述 2.9s 是 pre-M47）。**(3) wall 模型驗證**：`W=max(max dt, Σdt/cores, ΣPT)`，REFINE-free 40 案 OLS a=0.9997/b=2.45ms per profile/c≈0；γ(n>100)=0.48 = M49 K=4 的 −50%、γ_full=1.088（12 核 41-way 並行只低估 9% ⇒ contention 論據）、實測 full/ship wall **6.27×**。**(4) 投影**（κ∈{2.5,3.16,4,6}×s∈{1,1.5,2,2.5}×模型 B×`--tax-all`×`--cores 24` 符號全同）：**`POOL=0` 全池 +52.8~+60.0%、REFINE 還原 K=12 +15.1~+20.5%** ⇒ **M41 swap 砍 + M49/M50 REFINE = 48c 的 RF 主力，必須留**。**(5) 🚨 主發現**：M42/M45 砍掉的 22 隻 build profile **全部 dt ≤ max-setter** ⇒ 48c 加回來 **dW=+0.00%**（重帶 13→33/34 隻、ΔΣPT ≤0.20s、@24c ≤+0.53%）、in-set dQ≈0（閘本來就是逐案 cost 相等 ⇒ 樣本內盲）⇒ **這兩層是為 12 核 `Σ/cores` 牆設計的，48 核把牆拿掉後只剩 M67-D 的 +2.8% OOS 稅**：break-even **θ\*=0.000**、θ=1 上界 **−2.11%**。**(6) 交接 M67-F（spec 已寫進 `M67_PLAN.md` 獨立小節，下個 session 主線）**：θ（OOS 稅中屬於池砍的比例）cache 無法回答（M67-D 只量 shipped 與 POOL=0 兩點；in-set 分解 REFINE +0.055% / 池砍 ≈+0.05% 各半、不可外推）→ Phase 1 = env 旋鈕（預設 off、逐位不變）+ `pool0` 協定同批 80 OOS 重案，**20 案 pilot 預註冊 gate：θ≤0.10 RED 結案 / ≥0.30 直接補齊**（θ 可能為負：池變大 → proxy 在未見案更易選錯）；Phase 2 = **多核機實測 restore 池 wall**（本機 12 核不足，「48c 免費」目前只有模型+間接證據）；Phase 3 才討論 ship，形態 = **cores-gated tier-5**（≥32 核才鬆綁、偵測失敗→現行行為）+ 全鏈重驗（m49 三 gate → regression_suite → make_submission → WSL 逐位）。⚠️ deadline 2026-07-31、現行 tar 已四關綠 → **先提早上傳現行 tar**；換件前須確認 Drive 可否覆蓋重傳；θ 未量到前不要動 wrapper（**θ 已於 M67-F 量到 = 0.7636，見下條**）

- **M67-F Phase 1：θ 實測 GREEN（2026-07-22，`m67_oos_probe.py restore` + `m67_oos_cache.pkl` 桶 `m67f` + `results_M67F_theta_{pool,refine}.json` + `M67F_REPORT.md`；勿重跑求「更好的 θ」）**——**這是目前唯一還在桌上、方向確定的分數槓桿**。(1) **wrapper 加 offline 旋鈕 `ICCAD_M67F_RESTORE`**（`_pool_indices()` 內，預設 **0 = 逐位不變**，同 `ICCAD_L1_POOL` 慣例）：**只**跳過 `_BIG_REDUNDANT_IDX`（M42）+ `_M45_BAND_DROP`（M45 tier-3）；**M41 swap 濾除、`_band_env()` 的 M49/M50 REFINE band、tier-4 `_M45_LOWCORE_DROP` 全不碰**（後者只在 cores≤8 啟用 = 12c/48c 皆 fail-open）。off-path 三閘全綠：**V1** `_pool_indices`/`_band_env` 對 n=1..130 與 `git show HEAD` 版逐一相等；**V2** 官方 eval total **1.326473104916827 逐位**、`m53_diff_results` **0 movers**、逐案 positions 亦全同、100/100 feasible；**V3** `m48_coldstart_dryrun.py` 四 phase + `opwrapper` 四 phase 全 PASS。(2) **量測**（`restore --arm {pool,refine}`，鏡射 `pool0` 的 80 案 OOS 重案 + per-n 估計量；shipped/full 兩端點直接重用 M67-D cache——旋鈕不動 `_PROFILES` ⇒ `_sig` 不變）：**Gate A** 旋鈕五閘（含 `restore(120) − shipped(120)` **恰等於** `_BIG_REDUNDANT_IDX`、REFINE band 不被連帶還原）；**Gate B** in-set n>100 **20/20 cost-equal（rel 1e-9）** = M42 當年的 strict selection-preserving 閘至今零漂移。(3) **🚨 結果**：**θ_pool = 0.7636**（pilot 20 案 0.6745 同號；shipped 1.659884 → restore 1.625062 → full 1.614282；**movers 45/80 = 44 better / 1 worse（唯一退步 +0.01% 雜訊級）**、改善 mean 3.34%/median 3.07%/**max 10.65%**；**48/80 案 restore 已等於 full**、9 案**比 full 更好**——full 額外還原的 swap 巨獸+K=12 反而帶偏 proxy）→ **GREEN（預註冊 ≥0.30）**。**θ_refine = 0.0864 → RED**（15 better / 6 worse、淨 +0.238%）⇒ **M49/M50 的 REFINE band cut 在 OOS 上幾乎不欠品質債**（呼應其 in-set +0.027/+0.028%），而它在 48c 是 **+15~21% 的 RF 主力 → 絕對必須留**。兩 θ 和 0.85 ⇒ **約 15% 是交互作用**（只有兩層同時還原才回收），故「restore 免費」只適用**池砍那一層**。(4) **分數投影**（代 θ 進 `m67e_rf48.variant_quality`）：**官方分 −1.60~−1.62%**（s∈{1,1.5,2,2.5} 全同號、κ=3.161、48c；`--tax-all` 保守版 −2.06%；θ=1 上界 −2.11%）。(5) **12 核 wall（不可外推 48c）**：shipped 1.59s → restore **3.17s（1.99×）** / norefine 3.22s / full 11.59s——本機是 `Σ/cores`-bound，Beta 48c 依 M67-E 是 max-bound、預期 +0.00%。**⇒ ship 還缺兩件事**：**Phase 2**（真多核機實測 restore 池 wall；判準預註冊 = 逐案 wall 漲幅 >2% 即免費論證破產結案；復現 = 同一支旗標跑 100 案官方 eval 比 `runtime_seconds`，品質不必重跑）＋ **mid band 補量**（本次 θ 只覆蓋 n>100 = **M42 層**；(60,100] 的 tier-3 九隻、18.2% wContr 的 OOS 稅**仍未量**，要補 mid 帶的 pool0+restore 兩批各 ~80 案）。Phase 3 ship 形 = **cores-gated tier-5**（`_effective_cores() >= 32` 才跳過，偵測失敗→現行行為）+ 全鏈重驗（m49 三 gate → regression_suite → make_submission → WSL `verify_final_tar.sh` 逐位）。誠實範圍：~~restore 是 index-based vs 模型 dt-based → 22 vs 22.3 隻等價~~（**此句已由 mid 補量證偽，見下條**）；OOS 語料比測試語料更硬（M67-D），θ 是比值故一階抵銷但未嚴格證明無關
- **M67-F mid band 補量：θ_mid 品質綠、ship RED；附兩項對 Phase 1 的更正（2026-07-22 同日，`m67_oos_probe.py --pool0-hi` + `m67e_rf48.py restoreIdx` + `results_M67F_theta_pool_60_inf.json` + `M67F_REPORT.md` §7；送件形零改動、tar 未動）**——(1) **θ_mid = 0.5913**（(60,100] 80 案 OOS，20 better / 1 worse）；**mid 帶 OOS 稅只有 heavy 的 37%**（分母 +1.053% vs +2.825%）、restore 回收 +0.620%。逐帶分解（同一支 160 案 run）：mid wContr 18.2% θ 0.5913／big wContr 81.1% θ **0.7636 逐位重現 Phase 1**（1.659884/1.625062/1.614282、44/1）＝ 改動未污染舊結果；合併 n>60 θ=0.7504、稅 +2.502%。(2) **🚨 更正 A：Phase 1 的 −1.60~−1.62% 是用錯的池投影的**——θ 是拿 `ICCAD_M67F_RESTORE=1`（**index-based**，drop set 全放回）量的，但 `m67e_rf48.py` 的 `restore` 變體是 **dt-based**（`restore_candidates()` 只撿 `dt ≤ max-setter`）⇒ **wall-free 是它的定義、不是發現**。新變體 `restoreIdx`（池身分對 100/100 案與 knob 完全相同、已 assert）量到 index restore 在 48c **要付 wall**：(100,110] **+8.70%**（5/10 案）、(110,inf] **+5.68%**（7/10）、(60,100] **+2.34%**（9/40）——貴的是 #5/#15/#16/#18（heavy）、#2/#18/#20（mid）這些**本身就是某些案的 max-setter** 的 profile。⇒ **真實可 ship 上檔 = −1.30%（s=1）/ −0.55%（1.5）/ −0.26%（2~2.5）**，不是全網格 −1.6%；符號仍對但量級掉一半以上，且 **Phase 2 真機 wall 實測因此更關鍵**（模型說要付 5.7~8.7%）。(3) **mid band ship = RED**：`--idx-bands mid,big` 在**每個 s 都比 `big` 差 0.01~0.02pp**——品質回收 +0.620% < wall 代價 `(1.0234)^0.3 = +0.695%`，且 M67-E 已證 mid band **0/40 觸底**（全在 floor 之上）⇒ +2.34% wall **全額付、零 floor 吸收**；mid 帶 break-even 約需 θ_mid ≥ 0.65。⇒ **Phase 3 的 tier-5 只放 M42，tier-3 維持現狀、勿一起鬆綁**。(4) **🚨 更正 B（方法論，影響所有未來 pool-prune）：M45 tier-3 的 strict selection-preserving gate 在出貨組態下已失效**——`_M45_BAND_DROP`/`_BIG_REDUNDANT_IDX` 是從 `audit_cache.pkl` = **REFINE K=12** positions 推的，但 M49/M50 之後在**同樣那兩個帶**疊了 K=4/K=8 overlay，**gate 從未在出貨組態重證**。in-set case 64（n=85）單案四格複驗（鄰居 63/65 全恆等）：**K=12 兩側逐位相等 1.3558352796522921 ✅／K=8（出貨）shipped 1.3580617332260372 vs restore 1.3524850027999449 = 砍法付 +0.41% ❌**。heavy 帶仍 20/20 相等。**對送件可忽略**（case 64 權重 0.433% ⇒ 加權 **−0.0018%**，local 會是 1.326448970；方向是「現況略差於可達」非正確性問題），**對方法論不可忽略**：ledger「逐案 cost 相等 ⇒ local 逐位不變」對 tier-3 **已不再嚴格成立**，任何重算 tier-3/M42 常數的動作**必須在 K=4/K=8 overlay 下做**，不可再直接吃 `audit_cache.pkl`。Gate B 已改兩段（restore 變差 = 硬 FAIL＝knob 壞；restore 變好 = DRIFT 印出續跑）
- **M67-F Phase 2 前哨：本機 E-core 擁塞 probe = AMBER（lean-GREEN），M67-F **不判死**、送件形零改動（2026-07-23，`m67f_contention_probe.py` + `results_M67F_contention.json` + `m67f_contention_stdout.txt`，純量測、永不 ship）**——M67-E wall 模型 `W=max(max_i dt_i, Σdt_i/cores, ΣPT)` 的「`dt_i` 並行不變」假設從未實測（audit dt 只在 12 核箱 ~11-way 量過、模型在 41-way 已偏 9%）。48c 下 shipped 重帶 ~13 隻、restore ~34 隻**皆 <48 不超額訂閱** ⇒ 唯一能讓 restore 變貴的機制 = 記憶體擁塞讓 max-setter 自己變慢。本機 12 核不能直測 → 改量**擁塞斜率**。**方法**：ctypes `SetProcessAffinityMask`（無 psutil）把 constructive.exe 釘在 8 顆同質 E-core（logical 8-15；校準 self-validating：P-core 0-7 ≈1.2s / E-core 8-15 ≈2.0s，最慢 8 顆 = {8..15} 完全吻合），編排器釘 P-core 免偷週期；5 重案 restore-池 max-setter（99/#6、95/#18、96/#18、98/#40、86/#7）× k=1..8 各起**相同拷貝**、`threading.Barrier` 同時開跑量 dt(k)/dt(1)（REFINE=4=Beta 形、reps=4 median）+ 最重案 99/#6 加 K=12 保守對照。E-core = 本箱 cache/頻寬最貧（4 核 cluster 共用 2MB L2 ≈512KB/核）→ **保守上界**（平 ⇒ 48c server 更平；比值抵銷 E-core 絕對慢）。**結果**：**mean dt(8)/dt(1) 中位 1.018 / 最壞 1.037（95/#18）/ slope +0.0016 per co-runner / K=12 更平 1.003**——max-setter **自己的時間**全帶平（k1-k7 皆 0.98-1.03 無趨勢、僅 k=8 全 8 核飽和微升）。**straggler（k=8 max-copy）1.13-1.15 但 mean 平 = 一隻拷貝落隊、其餘 7 隻貼基準 = 零 E-core headroom 的排程 jitter，非頻寬擁塞**（擁塞會抬 mean 非只 max；K=12 同型 mean 1.003 / max 1.130 佐證）；此飽和 straggler 在 Beta（34/48、14 核空吸收 OS/背景）**不發生**。**判定 = AMBER（預註冊：worst-combo mean 1.037 > 3% GREEN bar 僅 0.7pp、遠低 10% RED bar）**，屬 **lean-GREEN**——對映使用者決策規則 = 「dt(8)/dt(1)≈1.00 → M67-F 存活、只剩 Beta 逐案 wall 要對」，**非**「明顯上升 → 判死」。⚠️ 不 retro 移門檻求 GREEN（M64/M65 紀律）；AMBER 的 0.7pp miss 建立在單一 combo k=1 基準的 ~1% jitter 上。**對 ship 的意義**：Phase 2 兩必要條件之一（擁塞不抬 max-setter）本機已收斂到 lean-GREEN；另一條件（真 48c 逐案 wall 實測，Phase-1 預註冊「>2% 漲幅即破產」= pool-vs-pool `runtime_seconds` 對比）**仍需 Beta 資料**，本 probe 是它的 local 前哨、非替代。tier-5（只 M42）ship 維持卡 Beta。精確淨值：把實測 f（保守 extrap k34 mean 1.045、更可能 ≈1.0）代進 `m67e_rf48.py restoreIdx`（restore 帶 wall ×f）重投影。**誠實範圍**：8-way 斜率外推 34-way 非直測（GREEN 是外推、RED 才會是保守判死）；量 wall 非 CPU-time（釘核不超額訂閱下等價）；相同拷貝=保守（全重）；REFINE=12 對照更保守。**送件形一律不動**（tar md5 `b9589618d507de0561f79a55a80fd8f3` 未變）
- **M67-G Beta 封包 slim（2026-07-23，`make_submission.py` 改 5 處 docstring/`_WHITELIST`/`_BIN_FILES`/`_README`/stage copyfile；`build_op_wrapper_text()` 未動）**——re-check 官方 Beta 指南（`beta_submission_guidelines_problemC.txt`）發現 `bin/sa_linux`+`optimizer_claude.cpp` = unused assets：SA fallback 是內嵌純 Python `python_sa_solve`（`solve()` 三處 fallback 全呼叫它），兩檔在 op_wrapper.py **零 code 引用**（僅註解/docstring）、舊 README 自證「never invokes it」。依 Beta §1「Large binary files … only if actively used … Remove any unused assets（violations **may lead to disqualification**）」移除 → 封包 **8→6 檔**（留 README/requirements(空)/op_wrapper/op_src/constructive.cpp/`bin/constructive_linux`）、tar 425563B→**276064B**、**新 md5 `06979081e47002a01d705cbfb9fc8f18`**。⚠️ **副作用**：重新 `stage` 從當前 source 產生 op_wrapper.py → 帶進 2026-07-22 M67-F Phase 1 的 default-off 旋鈕 `ICCAD_M67F_RESTORE`（op_wrapper md5 `709a255e`→`6afff2c7`、+1173B；git 確認 07-21 tar 後 `optimizer_constructive.py` 僅此一 commit，line 544 預設 `"0"`、`restore=False`=M67-F 前邏輯、grader 不設此 env + verify 剝 `ICCAD_*` → 出貨逐位不變，與已出貨的 `ICCAD_L1_POOL` 等 offline 旋鈕同類）。**Windows 官方 verify（`make_submission.py verify`）100 案逐位 `1.326473104916827`、100/100 feasible、零 fallback、avg 1.76s**。**✅ Linux gate PASSED（2026-07-23 GPU 機 WSL2）**：`bash verify_final_tar.sh` → `M67C-TIER [final]: PASS`（`bundled-first: OK`、`total |d|=0`、`feasible 100/100`、僅 case 84 ULP cost 4.441e-16、avg 0.89s）⇒ slim tar **Windows+Linux 雙邊皆逐位 `1.326473104916827`、ready-to-upload**。⚠️ gotcha：`m67c_tier3.py` 的 `BIN_NAMES` 寫死要兩 binary（舊 8 檔包）→ slim 後 `final` 誤報 `bin/sa_linux missing`，就地 sed 改成單 binary 修好、source `m67c_make_linux_bundle.py` 已同步。見 memory `[[beta-package-slim]]`
- **M67-F Phase 3：tier-5 實作完成（候選版，2026-07-26；`optimizer_constructive.py` 三處 + 新 gate `m67g_tier5_gate.py` + 併入 `regression_suite.py` 第七項 `tier5`）**——**已上傳的 slim tar 不動**，這是要走完整鏈才談換件的候選。(1) **形態**：`_M67F_CORES_MIN = 32`，`_pool_indices()` 的 M42 條件加 `not tier5` ⇒ 偵測 ≥32 核才把 `_BIG_REDUNDANT_IDX`（22 隻）放回 n>100 帶（13→35）；**只動 M42 層**，tier-3 mid band（ship RED）、M41 swap、M49/M50 REFINE overlay、tier-4 全不碰；`ICCAD_M67F_TIER5=0` 為 kill switch。(2) **🚨 實作關鍵陷阱（新 doctrine）**：**不可沿用 `_effective_cores()`**——它把 unknown 映成 **9999**，那是為 tier-4（`<=8`）設計的安全方向；tier-5 是 **`>=32` 才觸發**，同一個哨兵會變成「**偵測失敗就觸發**」。故新增 **`_effective_cores_hi()`（unknown → 0）**，使兩個 tier 的偵測失敗都退回出貨行為；`ICCAD_ADAPTIVE_CORES` 兩者共用。**任何未來以「高核」為條件的 tier 都必須用 hi 版**。(3) **驗證**：`m67g_tier5_gate.py` **ALL PASS**——V1 auto/4/8/12/16/24/31 核 + kill-switch@48c 對 `git show HEAD` 版 n=1..130 逐一相等；V2 @32/48/96 核 n>100 池 == `ICCAD_M67F_RESTORE=1` 池、n≤100 == 出貨；V3 還原集恰 = `_BIG_REDUNDANT_IDX`(22)、REFINE overlay 不變；V4 monkeypatch 偵測失敗證 hi=0/lo=9999 兩 tier 皆 off。**官方 eval 雙跑**：預設與 **`ICCAD_ADAPTIVE_CORES=48` 強制觸發**皆 total **`1.326473104916827` 逐位**、**cost movers 0、positions movers 0**、100/100 feasible ⇒ **tier-5 是 in-sample NO-OP**（22 隻在樣本內一個 n>100 案都沒贏 = M42 strict gate 的原始內容），**增益 100% 在 OOS**（θ_pool 0.7636）。本機 16 核 avg runtime 1.72s → 強制 48c **1.92s（+11.6%）**（遠低於 M67-F Phase 1 的 1.99×，因該次連 tier-3 一起還原）。(4) **投影**（`m67e_rf48.py project --idx-bands big`，wall 代價已計入：(100,110] +8.70%、(110,inf] +5.68%、其餘 +0.00%）：官方分 **−1.30%(s=1) / −0.55%(1.5) / −0.26%(2~2.5)**，**每個機速同號**。(5) **剩餘風險（唯一）= 48c 真實 wall**——模型說 wall=max-setter 且還原的 22 隻全比 max-setter 便宜，本機 E-core 擁塞前哨 lean-GREEN（slope≈0）佐證，但**真 48c 從未實測**；因 tier-5 只在 ≥32 核觸發，該路徑**只會在評分機上第一次真正執行**（本地已用 `ICCAD_ADAPTIVE_CORES` 強制走過，品質面逐位驗畢，只有 wall 驗不到）。⇒ 換件前置 = regression_suite 全綠 + `make_submission.py all` + GPU 機 WSL `verify_final_tar.sh` + 確認 Drive 可覆蓋重傳
- **🚨 M67-F Phase 3 校準：門檻 32→40，c\* 實測定錨（2026-07-27）**——(1) **GPU 機 WSL 複驗 PASS**（`nproc=16` ⇒ tier-5 **未觸發**，故只證低核路徑不變）：`total 1.326473104916827`、`|d|=0`、feasible 100/100、bundled-first OK、avg **0.89s**、僅 case 84 ULP 4.441e-16；強制 `ICCAD_ADAPTIVE_CORES=48` 補跑 Linux tier-5 路徑亦 `Total 1.3265`、avg 1.09s（+22.5%）⇒ **Linux 上 tier-5 可正常執行且品質不變**。(2) **🚨 一度誤判、已更正**：拿模型 `--cores 16` 比實測（模型 +27~29% vs 實測 **+91~102%**）曾判「模型低估 3.4×」——**錯在把邏輯核當有效核**。本機 16 邏輯核實為 12 實體、其中 8 顆 E-core ⇒ 用實測比值 1.970 反推**有效核 ≈ 10**，模型在 ceff=10 預測 **2.029 與實測吻合** ⇒ **模型結構正確、48c 投影不受動搖**。(3) **c\* 定錨（新的判準量）**：還原池 `c* = Σdt/max dt`（比值 ⇒ 對 REFINE K 不敏感）在 20 個 n>100 案為 **min 15.5 / 中位 19.3 / max 22.5**；**≥24 核起全部 max-bound、成本飽和於 +6.0%**（24/32/40/48/64 逐格相同、0/20 sum-bound），與投影已計入的 +5.7~8.7% 一致。**低於 c\* 則 wall 依 Σ/cores 暴漲**（本機實測還原後 n>100 wall 近乎翻倍 +97%、單案最高 +113%）。(4) **⇒ 門檻 `_M67F_CORES_MIN` 32→40**：偵測核是有效核的**上界**，32 偵測核若碰上本機這種 0.63 有效比 → 有效 ~20 < 22.5 → 反落入 sum-bound 付 +100%；40 對 c\*max 保 ~1.8× 餘裕、仍在 48 核 Beta 觸發、對目標機零代價。(5) gate 同步：`m67g_tier5_gate.py` 的 V1 基準改用 **kill switch**（commit 後 HEAD 亦含 tier-5，比 HEAD 已失效），測點 32c/39c 惰性 + 40/48/96 觸發，ALL PASS。**殘餘賭注一句話 = Beta 有效並行度是否 ≥22.5**（48 核獨佔 ICELAKE 無 E-core，保守估亦遠超）
- **M68 ML-seed 插入點 oracle-ceiling probe：RED（訓練前判死、零 GPU，2026-07-25，`m68_ml_seed_probe.py`(gate0/oracle) + `constructive_m68.cpp/.exe`(default-off `ICCAD_ML_ANCHOR`) + `m68_oracle_stdout.txt`，永不 ship；prohibit 重試「ML pose/anchor → packer 種子」任何變體）**——回應使用者「ML 插入點錯了、該改善 winner 而非從零生成」的提案（計畫 `ml-40-mighty-gray.md` Stage 0）。先定位「先前版本」= 組員 `v7` MLP pose pipeline（非主線、未進 repo）：`codex_train_mlp_anchor.py:92` target=`fp_sol` 正規化 block 中心(+aspect) SmoothL1 = **絕對座標 imitation**（= v2/M52 ill-posed）、訓練 out 只有中心(+aspect) → **根本無 confidence head**（推論側再確認：`codex_make_mlp_optimizer.py:189` anchor 第三槽是**寫死 `1.0`**、模型 out 只有 2 個 sigmoid 座標 ⇒ packer 的 `if mw > 0` gate（`v7_mlp_pose90_wlarge08.py:1013/1040`）**恆真＝死 gate**，confidence 訓練/推論兩側皆不存在）、`v7_mlp_pose90_wlarge08.py:1047` MLP anchor 只是 greedy `_candidate_score_block` 的 soft `mlp_score` 偏置（被 bbox/wire/boundary 蓋過）、`v7_profile_selector_mlp_proxy.py:90` selector proxy `(1+1.5·area+2·hpwl)·exp(6·vrel)` approx **非 shapely**——整套 = 平行且較弱的一疊（6 個 v6 profile vs 41、無 LP）。**Stage 0 = 訓練前最便宜 kill**：`constructive_m68.cpp` 於 `estimate_anchors()` 後加 default-off `ICCAD_ML_ANCHOR=file`（override movable `anchors[]`，鏡射 `ICCAD_ORDER_FILE`；**off 逐位 byte-identical**，gate0 n=21..120 全通）→ 注入 **fp_sol 逐 block 中心 = 完美位置種子**（ML pose head 的理論上界）走真實 greedy、掃 `ICCAD_ANCHOR_W∈{0.1,1,5,20}` 取每案最佳，鏡射 `oracle_perm_probe.py` 的 oracle-min 比較。**結果**：anchor 機制**真有效**——**61/100 案贏過同 profile 無種子、隔離加權增益 +2.180%**（遠勝 ordering 的 +0.005%）、infeasible 0；**但 vs 41-profile portfolio 僅 +0.001%**（3 movers=案 3/9/27 全小 n、wContr≈0、**重案 0 mover**）→ 依預註冊 gate（<0.2%）**RED，不訓練**。**機制**：完美種子買到的 +2.18% 是 portfolio 的 aspect/frame 多樣性**早就 label-free 拿到的** ⇒ ML-seed 與已送 portfolio **是替代非互補**；重案（60% 權重）即使完美種子零移動（呼應 M40 recon RED / M64 flip RED，preplaced 幾何鎖死；proxy 自 M13 = per-case oracle ceiling）。**合併封軸**：ML「pose→packer 種子」插入點死（本條）+ selector（M56 winner case-idiosyncratic + proxy 已 oracle）+ refinement（L3 LP exact + M64/M59/M57/M63）+ 生成（M52）⇒ **ML 四種插入點全 RED**；重開唯一條件仍是 rival 1.29 legit 方法細節 → M27 packer 軸。**誠實範圍**：測 anchor-on-base + AW sweep、非 anchor×全 41 profile（但重案零移動 + proxy=oracle ceiling 使結論穩健）；未升級 Stage 0b LP（vs portfolio 已 <0.2%，遑論更嚴的 1.2978 錨）。計畫 Stage 1/2/3（整合層 / model spec / ship gate）隨之 moot。送件形不動。見 memory `[[m68-ml-seed-red]]`

## 殘留 case（純 optimization 已榨乾）
89 **~1.523**（M37，仍最高；preplaced boundary 撐壞 outline）、85 **1.5240**（M49 REFINE cut 唯一 mover：pre-M49 1.5096、+0.95%；勝出 profile #23 靠 pass-11 改善）、65 ~1.690（M35）、62 1.5227、88 ~1.385（M36）、97 ~1.199（M36 ungate-boundary）、82 ~1.363（M34）、52 ~1.361（M35）、61 **1.3134**（M50 mover +0.81%；pre 1.3030 M37）、79 ~1.247、66 ~1.270、91 1.3481。**M51 movers（2026-07-10）**：99 1.3334→**1.3084**（wide-clamp frame、最重案）、47 1.2242→**1.2179**。**M50 mid movers（REFINE=8 trade @12c）**：49 1.6496（+2.18%）、54 1.4563（+1.81%）、68 1.5045（+1.84%）、70 1.4911（+0.71%）、64 1.3581（+0.16%）；低核 K=4 路徑另有 41/42/44/78/79 小 mover 與 51/77 微改善（見 probe 存檔）。硬 case（89/85/62）= preplaced boundary 幾何強迫；M33-M37 證實成員形狀（cluster/anchored/MIB aspect + tight frame）能鬆動 89/82/88/97/79/66/65/61/52。**M57 補（2026-07-16）**：硬案 violation 軸無肉——89 的 2 個 frozen violators 一絕對死（blk62）一鏈死（blk23，dist 62.3 結構性）；85/62 的 vBd 殘留全 movable/cluster（M53 repair 已死範圍）。**M63 補（2026-07-19）**：89 另有 4 個純 movable cluster g1 violator（blk13/25/63/68）= T2 上界最大單案貢獻 0.559%——pool #22（fc-wide）能**全清**但淨 cost +2.68%（quality 稅 ~+19% 壓過 violation 增益 −14%）；violation 軸「可達但恆付稅」定量見 `M63_REPORT.md` §7。

## 環境 & 指令

- **主程式**：`constructive.cpp` + `optimizer_constructive.py`（舊 SA `optimizer_claude.cpp/.py` 僅 fallback）
- **Conda**：`C:\Users\Nordra\.conda\envs\iccadv\python.exe`；**Compiler**：`C:\msys64\ucrt64\bin\g++.exe`
- 組員參考碼：`C:\Users\Nordra\Downloads\teammate_iccad_study\`
- ⚠️ **eval 實際 ~2.5 分鐘（M51 現值 avg 1.60s@12c；RF 鏈 M50 1.49s、M49 1.54s）**（100 案 serial，大案 ~1.2-3.6s、最重 #78 n=99；M47 ~4 分、M46 ~6 分、M42 ~8 分、M41 前 ~15-17 分）；background 用 harness `run_in_background`（完成通知）；`> file 2>&1` 在 PowerShell 對 native exe 印 cosmetic `NativeCommandError`（無害）

```powershell
# 編譯（Bash 工具寫 .exe 會失敗，務必用 PowerShell）
& "C:\msys64\ucrt64\bin\g++.exe" -O3 -std=c++17 -o constructive.exe constructive.cpp

# 官方 portfolio eval（~2.5 分鐘 M49 後，確定性）
cd "C:\Users\Nordra\Downloads\ICCAD2026_FloorSet\FloorSet\iccad2026contest"
& "C:\Users\Nordra\.conda\envs\iccadv\python.exe" iccad2026_evaluate.py --evaluate ../optimizer_constructive.py 2>&1 | Select-Object -Last 12

# 快速單 profile A/B（~70 秒，乾淨確定性，與官方吻合）
& "C:\Users\Nordra\.conda\envs\iccadv\python.exe" analyze_constructive.py 2>&1 | Select-Object -First 12
```

## Gotchas
- **PowerShell 用 `;` 或 `if ($?){...}` 連接，不能用 `&&`**；Bash 工具寫 .exe 會失敗（sandbox）
- **輸出必須 `%.17g`**（非 %.10f）：否則精確 abutment 被捨入成虛假 shapely cluster fragment（M10，~144 假 fragment/100 案，−6.3% 單一最大 lever）。新增任何被 shapely 評分的幾何都要遵守；查不明 grouping violation 先疑精度
- **proxy 選擇用 shapely vrel**（`_proxy_metrics`），不可用 C++ METRICS vrel
- **compaction 選候選用 true-cost csc** `(area+hw·hpwl)·exp(2·(bv+gf)/nsoft)`，不可用 layout_score（後者 boundary 權 150000 ≫ grouping 6500，會拿 vCl 換 vBd）
- 新增重 profile 前先查它自己的 per-case cpu（每案 wall ≈ 最重 profile 的 max 項，非池總量）
- **判 feasibility 用 `SolutionMetrics.is_feasible`，不可用 cost**：SA fallback 的 cost≈9.999999 是 feasible 品質上限（`M_PENALTY−1e-6`）、被 `%.4f` 進位成「10.0000」，非 infeasible 的 10.0

## env 旋鈕（`constructive.cpp`）
- 預設：`ICCAD_BP_WEIGHT`=30000、`ICCAD_WIRE_MULT`=×1、`ICCAD_ANCHOR_W`=0.10、`ICCAD_LR_ASPECT`/`ICCAD_TB_ASPECT`（boundary aspect，預設 2.50/0.40）
- 關後處理：`NO_COMPACT`/`NO_REFINE`/`NO_PUSH`（關 M14-16+24）/`NO_BND_PUSH`/`NO_SWAP`/`NO_JUMP`；`PUSH_PASSES=N`、`COMPACT_ITERS=N`、`REFINE_ITERS=N`
- pack-order 軸：`WIRE_TIEBREAK`、`WIRE_BFS`、`BFS_PIN`、`ORDER_SWAP=K`、`ORDER_MOVE=K`、`GUIDE_MED`（M26 ship）
- **M41+M42+M45 RF lever（wrapper 旋鈕，`optimizer_constructive.py` `_pool_indices()` 四階 index-based filter，非 constructive.cpp）**：`ICCAD_ADAPTIVE_POOL`（預設 **1=on**）總開關；`=0` 還原 full 41-prof（quality-best 1.3248）。**M41 階**：砍 `ORDER_SWAP`/`ORDER_MOVE` profiles，`ICCAD_ADAPTIVE_N=K` 只砍 `block_count>K`（預設 0=全砍）。**M42 階**：砍 `_BIG_REDUNDANT_IDX`（22 隻，M51 起含 #18）當 `block_count>ICCAD_ADAPTIVE_FREE_N`（預設 **100**）。**M45 tier-3**（universal）：砍 `_M45_BAND_DROP`（9 隻，60<n≤100、35→26），`ICCAD_ADAPTIVE_BAND=0` 關。**M45 tier-4**（低核）：`_effective_cores()≤_M45_CORES_MAX(8)` 時砍 `_M45_LOWCORE_DROP`（(40,60]→14、(100,110]→8、(110,∞]→9；M51 起前兩 band 含 #40）；`ICCAD_ADAPTIVE_CORES=N` 強制 cores 值（≤0/garbage=auto、≥9 等效關閉）；偵測 `sched_getaffinity`→`cpu_count`→9999 fail-open（誤判方向=不觸發=shipped 行為）。⚠️ 改 `_PROFILES` **或重編 constructive.exe**（proxy tie 可能翻轉）後須用 `rf_score_model.py` M42+M45 區塊重算全部 drop 常數，**並重過 `m49_refine_probe.py` gate（`variant 4 big`+`variant 8 mid`+`variant 4 mid`）**（M51 ship 2026-07-10 已全套重跑：re-audit 4200 combos → 常數 regen（+#18、tier-4+#40）→ 三 gate 綠 → eval 1.3265）。
- **M49+M50 REFINE band-cut（wrapper 旋鈕，`_band_env()`）**：`block_count>100` 疊 `ICCAD_REFINE_ITERS=4`（M49）；`60<n≤100` 疊 `=8`（M50 universal，`_M49_REFINE_BAND=((60,100,"8"),(100,∞,"4"))`），`_effective_cores()≤_M45_CORES_MAX(8)` 時 mid 改疊 `=4`（`_M50_REFINE_LOWCORE=((60,100,"4"),)`；偵測 fail-open→universal=安全檔；`ICCAD_ADAPTIVE_CORES` 同 M45 強制/關閉）。`ICCAD_ADAPTIVE_REFINE=0` 關兩 band；`ICCAD_ADAPTIVE_POOL=0` 也一併還原 full REFINE（quality-best 模式保持純淨）。量測依據 + 勿重掃事項見死路 ledger「M49 量測」「M50 mid 補量」條。
- **M53 L1 品質池（wrapper 旋鈕，預設 off=shipped 逐位）**：`ICCAD_L1_POOL=1` 把 `_L1_EXTRA`（2 隻 OS16×free）+ 41 隻 `ICCAD_REFINE_ITERS=24` 複製 tier 掛進 `_PROFILES`（84 隻）；**務必配 `ICCAD_ADAPTIVE_POOL=0`**（否則 swap filter 砍掉 extras、K24 複製卻會漏進 adaptive pool）+ `ICCAD_PROFILE_TIMEOUT=600`（84 路 oversubscribe 下 48s 級 profile 會被預設 120s 靜默丟棄）。`ICCAD_PROFILE_TIMEOUT` 預設 120 = shipped 行為。純 offline 品質錨（1.3176），永不送件
- **M67-F 量測旋鈕（wrapper，`_pool_indices()` 內，預設 off = 逐位不變，永不送件）**：`ICCAD_M67F_RESTORE=1` **只**跳過 `_BIG_REDUNDANT_IDX`（M42）+ `_M45_BAND_DROP`（tier-3）→ 逐帶池全變 35（41 − 6 swap）；**M41 swap 濾除、M49/M50 REFINE band、tier-4 全保留**。用途 = 量 θ（見 ledger M67-F 條）；Phase 2 的多核 wall 實測也用它。`ICCAD_ADAPTIVE_POOL=0` 仍優先 early-return（全池 41 + full REFINE）
- **🆕 M67-F tier-5（wrapper，`_pool_indices()` 內，2026-07-26 候選版；這是唯一 default-ON 的高核 tier）**：偵測 `_effective_cores_hi() >= _M67F_CORES_MIN(32)` 時**只**跳過 `_BIG_REDUNDANT_IDX`（M42）→ n>100 池 13→35；tier-3 mid band（ship RED）/M41 swap/M49-M50 REFINE overlay/tier-4 **全不碰**。`ICCAD_M67F_TIER5=0` 關閉；`ICCAD_ADAPTIVE_CORES=N` 可強制核數（與 tier-4/M50 共用）→ **本機驗證用 `=48` 即可讓它真觸發**。⚠️ **`_effective_cores_hi()` 是 tier-5 專用、不可與 `_effective_cores()` 混用**：後者 unknown→**9999**（為 tier-4「≤8」而設＝偵測失敗不觸發），tier-5 條件是「≥32」，沿用會**偵測失敗就觸發**；hi 版 unknown→**0**，兩 tier 皆 fail-closed。**任何未來以高核為條件的 tier 一律用 hi 版。**驗證用 `m67g_tier5_gate.py`（已併入 `regression_suite.py` 第七項 `tier5`）。in-sample 為 no-op（強制 48c 亦逐位 `1.326473104916827`、0 movers），增益全在 OOS，見 ledger M67-F Phase 3 條
- **M48 編譯/安全網（wrapper）**：`ICCAD_CXX=編譯器` 插到編譯鏈最前；鏈 = msys g++→g++→clang++→c++ × -O3→-O2，每次成功編譯（含 mtime 捷徑）都須過 `_binary_runs()` 1-block smoke 才算數
- **free-aspect 六子軸**（全 gated、off=bit-identical；M29-M37 ship，per-case win 細節見 memory）：
  - `ICCAD_FREE_ASPECT=1`（M29/M30）：single interior movable 在 ±1% area 搜 aspect（`FREE_RATIOS={1.0,1.5,0.6667,2.0,0.5}`），per-candidate；8 free profile 進 portfolio（`free_pin_tight`/`free_gm_pin` 為全池 LOO 前二）
  - `ICCAD_LR_ASPECT`/`ICCAD_TB_ASPECT`（M32）：decoupled **uniform** boundary aspect（高 LR + TB 留 default 0.40）。⚠️ per-block FREE_BOUNDARY 死路 → 須 uniform
  - `ICCAD_CLUSTER_ASPECT=r`（M33）：uniform reshape 純 movable INTERIOR cluster 成員，套在 `apply_safe_mib_dims()` 後、`make_group_item` 前；共振 3.0
  - `ICCAD_FREE_CLUSTER=1` + `ICCAD_FREE_CLUSTER_RATIOS=r1,...`（M34，預設集 `1.0,1.5,0.6667,2.0,0.5`）：per-member 在 `make_group_item` 內搜 aspect、**cluster layout-key 仲裁**（非 greedy-area，避 M32 失敗）；build-time → widen 免抬牆，共振 4.0
  - `ICCAD_FREE_ANCHORED=1` + `ICCAD_FREE_ANCHORED_RATIOS=...` + `ICCAD_FREE_ANCHORED_BND=1`（M35/M36）：mixed cluster movable 成員在 `pack_in_frame` wall-attach 搜 aspect（per-frame、只 commit `out[]`）；gate `boundary==0&&mib==0&&!is_fixed`，BND=1 ungate boundary 成員
  - `ICCAD_MIB_ASPECT=r`（M37）：`apply_safe_mib_dims` 無-master 共享方形 reshape 成同面積共享矩形（保 MIB violation 0、all-interior gated）；共振 wide 5.0
  - `ICCAD_SOFT_ASPECT=r`：全域 interior aspect（粗版，未進 portfolio）
- 死路（code 保留 gated off / 未實作，勿重掃）：`BFS_NORM`、`CLUSTER_ORD=1/2`、`REFRAME`；`ICCAD_FREE_CLUSTER_BND`（M39 已 revert，3 行可復原）
- 離線探測（永不 ship）：`ORDER_FILE=path` + `ORDER_GLOBAL=1`（oracle-perm）
- `ICCAD_CONSTRUCTIVE_SINGLE=1` 退單 base profile（1.7045）

## 工具
- `analyze_constructive.py`（單 profile per-case breakdown）、`profile_vs_portfolio.py KEY=VAL`（新 profile 逐案算 oracle-min 增益，>0.05% 才加）
- `rh_sweep.py`（真值快取 + 掃 _RH/proxy）、`portfolio_ceiling.py`（oracle 天花板）、`profile_audit.py`（M25 池審計：win tally/LOO/**per-profile cpu mean/max**，cache `audit_cache.pkl`）
- **`regression_suite.py`（送件前一鍵回歸閘總管，永不 ship；`[[regression-suite-tool]]`）**：獨立 subprocess 串跑全部 gate——`m48_coldstart_dryrun.py` 四 phase → `rf_score_model.py` asserts → `m49_refine_probe.py` `variant 4 big`/`variant 8 mid`/`variant 4 mid` → `m47b_proxy_equiv.py`——逐項 PASS/FAIL 匯總、任一 FAIL exit 1、子行程 env 先剝除 `ICCAD_*`（防殘留旋鈕汙染）；全程 ~13 分（首跑 753s 全 PASS 2026-07-13）。Beta/Final 送件前跑這一隻即可，個別 gate 條目見下
- **`rf_score_model.py`（M41+M42+M45 RF-aware 投影模型，永不 ship）**：載入 `audit_cache.pkl`，逐案 Q=proxy oracle-min、wall=`max(max_i t, Σt/cores)`，投影 real total 掃 median M。full-pool RF=1.0 重現 1.3269 為 sanity gate。**M42 區塊**：per-big-n winner tally → greedy 精煉（WIN check）→ 推薦 `_BIG_REDUNDANT_IDX`。**M45 區塊**：band-scoped winner（(40,60]/(60,100]/(100,110]/(110,∞) + fine/coarse 對照）→ **strict selection-preserving refine**（逐案 cost 相等 gate，比 WIN 不等式強）→ cores∈{4,6,8,12,16}×M 投影矩陣 + band wall 表 + #19/#21 tie 記錄 + ready-to-paste `_M45_BAND_DROP`/`_M45_LOWCORE_DROP`。內建 assert：`refine(100)==_BIG_REDUNDANT_IDX`（shipped 漂移偵測）。**M46 區塊**：α-靈敏度投影（uniform / FREE-stack-only × cores × M、max-setter tally、tier-3/4 常數 drift assert、α=1 sanity；M46 ship 後基準=新 dt，此區塊=殘餘 headroom 掃描）。改 `_PROFILES` 或重編 exe 後重跑更新全部常數（M46 ship 2026-07-03 已重跑：SANITY 1.3269、全 asserts OK、常數零漂移；M49 ship 2026-07-07 再跑：全綠）。⚠️ **M49 後 `audit_cache.pkl`/本模型 = n>100 的 K=12 counterfactual**（positions 未截斷、dt 偏保守）——band 內的 variant 差異由 `m49_refine_probe.py` 負責，兩者並存不衝突
- **`m47_overhead_probe.py` / `m47b_proxy_equiv.py`（M47 probe/gate，永不 ship）**：前者 monkeypatch wrapper 分解 solve() 計時窗（`total = pre(serialize) + pool_span(C++) + proxy tail`，25 案表 + n>100 aggregate）；後者 fast-proxy 逐位等價 gate（100 案×3 profiles，`m_old != m_new` 即 FAIL）。任何再動 `_proxy_metrics`/selection 的變更都要重過這兩關 + 官方 eval 1.3277 逐位
- **`m48_coldstart_dryrun.py`（M48 dry-run，永不 ship；Beta/Final 送件前重跑）**：四 phase、各自獨立 subprocess（sys.modules 快取會遮 packaging bug）：0 repo 基準 → 1 乾淨目錄冷啟動（無 .exe、只放 `optimizer_constructive.py`+`constructive.cpp`+`optimizer_claude.py` 三檔 → 編譯 + 3 案逐位）→ 2 垃圾 binary 須被 smoke 攔下重編 → 3 `ICCAD_CXX=bogus` 證編譯鏈迭代 → 4 絕對路徑衛生（report-only）。只在 temp 編譯、不碰 repo exe。**M67-B `opwrapper` variant**：scratch 改放送件佈局（`op_wrapper.py` 現產 + 兩 .cpp、無 `optimizer_claude.py`），phase 0 仍用 repo 三檔基準 → 逐位相等即證併檔形 cost-identical
- **`make_submission.py`（M67-B 打包器 + 官方佈局 gate，永不 ship）**：`stage|verify|all`——stage 產 `build_submission/cadc1075/`（**M67-G slim 後 6 檔**含 `bin/constructive_linux` 0o755；移除未用的 `optimizer_claude.cpp`+`bin/sa_linux`）+ tar，`build_op_wrapper_text()` 為併檔單一實作（m48 opwrapper variant 共用）；verify = 解 tar → 疊 evaluator/8 loader/dataset junction → 官方指令 `python iccad2026_evaluate.py --evaluate op_wrapper.py` → 100 案逐位對 `results_shipped_m51.json`。⚠️ 子行程須 `encoding="utf-8"`（tqdm 撞 cp1252）
- **`m67c_make_linux_bundle.py`（M67-C Linux 驗證包 builder，永不 ship）**：產 `Downloads/m67c-linux-verify.tar.gz`（161MB;內嵌 setup_env/run_all/verify_final_tar 三 .sh 強制 LF + `m67c_smoke.py`/`m67c_tier3.py` + 現產 staged tar + 8 loader + `LiteTensorDataTest` + anchor）。WSL2 端 `bash setup_env.sh` → `bash run_all.sh` 跑 T1 build+smoke / T2 m48 opwrapper / T3 官方 100 案 bundled-first 逐位（ULP warn 帶 2e-9）/ T4 破壞 binary 落編譯鏈;`bash verify_final_tar.sh <tar>` 供最終送件 tar 端到端複驗（免重建大包）。結果見 `M67_PLAN.md` §M67-C
- **`m46_stage_probe.py` + `constructive_m46.cpp/.exe`（M46 probe，永不 ship）**：計時儀裝副本（`ICCAD_STAGE_TIMING=1` → stderr TIMING 行；**M49 再加 `ICCAD_REFINE_TRACE=1` → stderr RTRACE 行**：逐 refine pass sc/imp/FNV64 hash、逐 frame last_imp、case 勝出 frame）+ 四模式 driver（`verify` 12 組 byte-compare / `verify100` 全 100 案×6 profile + per-band α / `diff` 差分旋鈕 / `timing` stage 預算）。opt-A/B/C 已於 M46（2026-07-03）ship 併入 `constructive.cpp`；副本保留儀裝供 stage 分析，verify/verify100 現在=比對 merged vs probe（M46 ship 時 600/600 PASS、M49 儀裝後 verify 12/12 PASS + trace 模式 260/260 position-vs-cache MATCH）
- **`m49_refine_probe.py`（M49/M50 probe/gate，永不 ship）**：`trace [big|mid]` = 儀裝 exe 收 last_imp 分佈（全 frame + 勝出 frame）/勝出 frame idx（max_trials 證據）/cycle 偵測 + 全 pair position-vs-cache 驗證；`variant K1[,K2..] [big|mid]`（M50 起 band 參數化，預設 big）= shipped exe 疊 `ICCAD_REFINE_ITERS=K` 跑 band×kept（含 K=12 control，須全 pairs 逐位=cache 才算 session 有效；big 260、mid 1000）→ exact 判定 + band wall 表 + Path B 真 cost 表（proxy 選擇 + evaluate_solution）+ real 投影（M×cores、鏡射 rf_score_model pools 含 tier-4；mid 模式下大案兩側同用 K=12 counterfactual dt，delta 嚴格歸因 mid）。**任何 `_PROFILES`/exe 變更後重跑 `variant 4 big`+`variant 8 mid`+`variant 4 mid` 重新 gate M49/M50**；⚠️ trace 的 K-coverage 表標籤差一（表中 K=x 指 last_imp≤x，即 REFINE_ITERS=x+1）；pm cache `m49_pm_cache.pkl`
- `dbg_area.py`、`dbg_boundary.py` / `dbg_vio_stats.py`（violation 分類）、`dbg_compact*.py`、`dbg_hpwl_push.py`
- **`m53_diff_results.py`（M53 diff 工具，永不 ship）**：比對兩份官方 eval results json——總分/加權 delta/逐案 movers（wContr 排序）/feasible/avg runtime；A/B 錨 `results_L1_final.json`（live）+ `results_L3_port_top32_area.json`（offline 錨逐案值）已 commit（A6 2026-07-17）；`results_shipped_m51.json` 留 repo 根未 commit（docker 冷啟動 bit-exact + m58 sanity 基準）；其餘 L1/L3 中間 results（base/refine24/refine48/pushcompact64/oracle/top-k 掃描）A6 已清（結論全在 ledger，可由 env 旋鈕重生）
- **`m53_l2_probe.py` + `constructive_l2.cpp/.exe`（M53 L2 probe，永不 ship）**：噪聲副本（`ICCAD_NOISE`=per-candidate score、`ICCAD_NOISE_W`=per-run 權重 jitter、`ICCAD_SEED`；σ=0 逐位=shipped）+ 五模式 driver（`gate0` σ=0 byte-gate / `phase0` 錨重現+winner / `phase1` σ 掃描 / `phase2 N SB SA` best-of-N 曲線 / `union` 跨 σ 最終判定）；cache `m53_l2_cache.pkl`（sig=pool+exe md5，斷點續跑）。L2 已判 RED（2026-07-12，見死路 ledger）——保留供重現；**其 cache 的 σ>0 samples 現兼任 L3 l2stack 種子庫**
- **`m53_l3_probe.py`（M53 L3 probe，永不 ship；需 `pip install scipy` 於 iccadv）**：constraint-graph LP 後處理，七模式——`gate0`（anchor json 逐位重現 gate）/ `probe15` / `full [--area]`（winner-only LP，dump results json）/ `port15` / `portfull --topk K`（LP×portfolio 重選：84-pool 補跑（L2 cache 優先採用）+ top-K 各自 LP + post-LP proxy 重選；cache `m53_l3_cache.pkl` sig=pool+exe md5 斷點續跑）/ `repair --src J`（violating boundary 強制等式，已證 0 可修）/ `l2stack --src J`（L2 noisy samples 當 LP 種子）。`--iters N`（拓撲重建迭代，2-4 即 fixpoint）、`--area`（聯合 area 一階目標，弱支配建議常開）。產物 `results_L3_*.json`（m53_diff_results.py 可直接 A/B）。量測結論見 §0 L3；**任何動 `_PROFILES`/exe 後 cache 自動失效重跑**
- **`m54_lp_rf_model.py`（M54 LP-aware RF 模型，永不 ship）**：兩模式——`dep`（deployable-shaped LP 重跑全 100 案：proxy-guard 決策 + 硬檢查、逐 pass 計時 t_build/t_solve/t_guard、離線官方評分、等價對照，dump `results_M54_dep_lp.json` + `m54_cache.pkl`）/ `model`（RF 投影：t@12c=實測 eval runtime、他 cores 用 audit-cache wall 比值縮放、β=solver 減速係數作用於 t_solve、gate=band×cap(abort/skip)×iters 掃描 + strict weak-win 搜尋 + per-case 表；β=0 kill test 用 `m.BETAS=(0.0,)` monkeypatch）。判定 RED 見死路 ledger M54 條（M62 補判改寫其適用範圍，見同條補註）
- **`m62_break_even.py`（M62 break-even 模型，永不 ship；cache-only 純分析）**：import `m54_lp_rf_model` 復用 load_walls/cache/常數——`tp = α·(t_build+t_guard) + β·t_solve + ε`，α∈{1..0}×β∈{1..0} × gate（m54 combos + 擴充 cap 0.25/0.1）全掃 + WIN 判準（全 20 cells max≤1e-9 ∧ min≤−0.01%）+ 四 sanity（anchor 逐位／M54 原 caps 無-win 重現／β=0 kill +0.480%／α=β=0 +0.8907%）+ detail（(1,1) win 清單與 abort-only 檢查、gate 組成、機速 s 掃描、±30% jitter Monte Carlo 40 draws、per-case dt 預算表 kfl=0.3046、safe-set 參考、ε 2/5ms 敏感度）。`--win-min`/`--eps` 可調。判定 GREEN（α\*=1.0）見 ledger M54 條補註 + `M62_REPORT.md`
- **`m63_vio_bound.py`（M63 violation 上界稽核，永不 ship；純分析）**：主模式 = shipped M51 逐案逐 bit violation 分解（vBd 分 frozen/cluster-member/single、vCl 分 mixed/pure、vMb；語意逐位鏡射官方 evaluator；gate0 = cost/vrel/n_soft/總分逐位重現）+ 三層上界（`cost·exp(2ΔV/ns)`）+ M60 五案交叉驗證，dump `results_M63_vio_bound.json`（含逐 bit 記錄）；`pool` 模式 = audit_cache 42 profiles × 100 案逐 combo movable-vBd oracle（min-mv per case + bit-clearing 候選的官方 cost）。判定 ABOVE-BAR/RED-leaning 見未來方向 §0d + `M63_REPORT.md`。⚠️ pool 模式 caveat：cache rows 無 `_band_env` overlay（n>60 = K=12 counterfactual）
- **`m64_flip_probe.py`（M64 相鄰拓撲 flip probe，永不 ship；`m64_cache.pkl` sig=anchor md5 斷點續跑）**：import `m53_l3_probe` 取資料/eval/polish、複製 `build_and_solve`/`lp_pass` 加 `force_rel`（unit-pair 級 disjunct 替換）/`skip_bnd_ties`（boundary 鎖診斷）/`bbox_relax`（variant B area cap）三參數，原檔未動。六模式：`selfcheck`（錨逐位 + force-當前方向逐位=無 force 佈線 gate）/ `pilot`（85,88,91；91 最慢排最後）/ `heavy`（85..99）/ `full` / `l2base`（自 `m53_l2_cache.pkl` 重生 l2stack 逐案 best = min(port32,l2b) 誠實基準；`mode_l2stack` 不可 import 呼叫故重寫）/ `report`（直方圖+稅分解+diag+gate 判定）。sound 前濾 = extent(A)+extent(B) vs bbox row（證明不可行才跳）。判定 RED 見 ledger M64 條 + `M64_REPORT.md`
- **`m65_l1_cell.py`（M65 L1 K24-extra probe，永不 ship；`m65_cache.pkl` sig=profiles+exe md5 斷點續跑）**：import `m53_l3_probe` 取資料/`cost_eval`/`lp_pass`；兩 cell = `oc._L1_EXTRA` + env 疊加 `ICCAD_REFINE_ITERS=24`、subprocess 直呼 shipped exe（env 剝 ICCAD_*）。五模式：`gate0`（池=84 + 缺 cell assert + anchor 3 案逐位）/ `pilot`（2 cells × 15 重案，mover gate 1e-6）/ `full`（oracle-min bar 0.05% + 86-pool proxy 重選，需 `m53_l3_cache.pkl` sig 合）/ `l3seed`（LP 種子 vs L3 錨，弱基準保守、bar 0.10%）/ `report`（dump `results_M65_l1cell.json`）。判定 RED 見 ledger M65 條 + `M65_REPORT.md`
- **`m66_equiv_cv.py`（M66 等價類 CV probe，永不 ship；cost 寄生 `m56_cache.pkl` cost dict 斷點續跑）**：先設 `sys.argv=[..,"cv"]` 再 import `m56_percase_oracle`（頂層 script，import 即重跑原 CV = gate0 錨、副作用重 dump `results_M56_percase.json` 逐位同值）；`equiv`/`keep_set_e`/`model_pool_e`/`loo_J_e` 鏡射原函式、eq 參數化。三模式：`selfcheck`（gate0 + singleton-E 奇偶閘 200×2 recs 逐位）/ `cv`（E build ~2350 官方 eval + E 化 LOO/OOS + 預註冊判定；⚠️ >10 分，harness 背景 timeout 會殺 process tree → 用 `Start-Process` detached + Monitor）/ `diag`（逐 break 分解 LABEL-ABSENT vs HMIN-FLIP）。判定 RED 見 ledger M56 條補註 + `M66_REPORT.md`
- **`m67_oos_probe.py`（M67-D OOS 泛化預檢，永不 ship；`m67_oos_cache.pkl` sig=exe md5+`_PROFILES`，抽樣旗標不入 sig 故加大 K 可沿用）**：import 前剝 `ICCAD_*` → `import optimizer_constructive`，`MyOptimizer` 建一次逐案 solve（`contextlib.redirect_stderr` 收 M48 fallback 行、per-case wall 計時）；訓練 case 本地展開（不經 `tree_decode_probe.load_layout` 的 tree assert）、`_baseline_official()` 逐位鏡射 `_extract_baseline` 含 stored-metrics 分支、`_cost()` = 官方 strict + RF=1.0；抽樣鏡射 validation 的「每 n 一案」結構（seed 67、優先取不同檔、prefix-stable in K）；估計量 per-n 先平均再 `compute_total_score`。六模式：`gate0`（env/池 + in-set 3 案逐位 + 訓練 fp_sol verbatim）/ `run`（240 案 424s，每 10 案 atomic save）/ `report`（headline+band+逐 n 配對+最壞案+品質分解+runtime+結構特徵，dump `results_M67D_oos.json`）/ `ref`（`ICCAD_CONSTRUCTIVE_SINGLE=1` 兩語料單 profile 參照，~6 分）/ `pool0`（`ICCAD_ADAPTIVE_POOL=0` 在 n>100 兩語料重跑 = adaptive 切法 OOS 稅，~20 分，`--pool0-lo` 可調帶）/ **`restore --arm {pool,refine}`（M67-F，2026-07-22 新增）**：中間點量測——`pool` = `ICCAD_M67F_RESTORE=1`（只還原 M42/M45 池砍）、`refine` = `ICCAD_ADAPTIVE_REFINE=0`（只還原 REFINE band），沿用 `pool0` 的 80 案與估計量、shipped/full 兩端點重用 cache（桶 `m67f`，斷點續跑），輸出 `θ=(S−R)/(S−F)` + pilot 檢查點 + 預註冊判定 + 跨臂相加一致性 + **逐帶分解表**（wContr/θ/movers）+ dump `results_M67F_theta_{arm}[_lo_hi].json`；內建 **Gate A**（旋鈕自檢，先跑不解案；含 `restore(80) − shipped(80)` 恰等於 `_M45_BAND_DROP`）+ **Gate B**（in-set 對 `results_shipped_m51.json` 比 cost；**兩段判準**：restore **變差** = 硬 FAIL 停跑〔proxy 樣本內是 oracle-min，超集池只可能弱贏 ⇒ 變差＝knob 壞〕、restore **變好** = DRIFT 印出續跑〔= 出貨常數漂移的發現，見 ledger M67-F mid 條 (4)〕）。**`--pool0-hi`（2026-07-22 加）= 上界**，配 `--pool0-lo 60 --pool0-hi 100` 可單獨計分 mid 帶；窗口只影響選案與報表、不影響 cache（逐案 key）。各約 5-13 分。判定與數字見 ledger M67-D / M67-F 兩條 + `M67D_REPORT.md` / `M67F_REPORT.md`
- **`m67e_rf48.py`（M67-E 48c RF 投影，永不 ship；`m67e_cache.pkl` sig=`_PROFILES`+exe md5）**：鏡射 `rf_score_model.py`/`m49_refine_probe.py` 的 dataset prep 與 `_RH=1.4` selector（不 import 主模型，避免每 mode 吃一次 shapely pass），cache 存 PM + **逐 (case,profile) proxy 秒數**（48c wall 模型的第三項）+ lazy cost。五模式：`gate0`（rf_score_model 子行程 exit0+六 marker／audit sig／alpha JSON 逐位對齊／**48c tier fail-open**，含 @4c 活性對照／48c wall max-bound）／`calib`（κ 比例模型 + 常數 M 模型 + M10 池映射估機速 bracket）／`fit`（γ 逐帶 + REFINE-free 40 案 OLS 取 per-profile overhead）／`project`（四變體 shipped/restore/restoreK12/full × κ × s × floor 餘裕 × free-restore 預算 × break-even θ）／`report`（dump json）。旋鈕 `--alpha/--cores/--speeds/--slack/--tax-all/--skip-rfmodel`＋**`--theta`（M67-F 加，預設 = 實測 0.7636）**＋**`--theta-mid`（預設 0.5913）/`--idx-bands`（預設 `mid,big`）（2026-07-22 加）**。⚠️ **`restore` 變體 = dt-filtered 池（`dt ≤ max-setter` 才撿）⇒ wall-free 是它的定義、不是發現**；θ 是拿 **index-based** 池量的 → **投影一律看新變體 `restoreIdx`**（`pool_restore_idx()`，對 100/100 案與 `ICCAD_M67F_RESTORE=1` 池身分完全相同、已 assert）：官方分 **−1.30%(s=1) / −0.55%(1.5) / −0.26%(2~2.5)**，且 `project` 尾巴會印逐帶 `dW@48c`（index restore 真的要付 wall）。結論見 ledger M67-E 條 + M67-F 兩條 + `M67E_REPORT.md` / `M67F_REPORT.md` §7
- **`m67g_tier5_gate.py`（M67-F tier-5 池身分閘，永不 ship；已併入 `regression_suite.py` 第七項 `tier5`）**：對 `git show HEAD:optimizer_constructive.py` 產的副本比 `_pool_indices`/`_band_env`（n=1..130）——**V1** auto/4/8/12/16/24/31 核 + `ICCAD_M67F_TIER5=0`@48c 全逐一相等（tier-5 惰性）／**V2** @32/48/96 核：n>100 池 == `ICCAD_M67F_RESTORE=1` 池、n≤100 == 出貨（blast radius 只在重帶）／**V3** 還原集恰 = `_BIG_REDUNDANT_IDX`(22)、REFINE overlay 不變／**V4** monkeypatch `os.cpu_count`/`sched_getaffinity` 使其 raise，證 `_effective_cores_hi()`→0（tier-5 off）且 `_effective_cores()`→9999（tier-4 off）＝**兩 tier 皆 fail-closed**。**任何動 `_PROFILES`/`_BIG_REDUNDANT_IDX`/cores helper/tier 常數後必須重跑**。PASS 判準 = exit 0 + `M67G-TIER5 GATE: ALL PASS`
- **`m68_ml_seed_probe.py` + `constructive_m68.cpp/.exe`（M68 ML-seed oracle-ceiling probe，永不 ship，RED 存檔勿重試）**：`constructive_m68.cpp` 於 `estimate_anchors()`（:1342）後加 default-off `ICCAD_ML_ANCHOR=file`（`id x y` 逐 movable block override `anchors[]`，鏡射 `ICCAD_ORDER_FILE`；unset 逐位 byte-identical）。兩模式：`gate0`（constructive_m68 off vs shipped `constructive.exe` 逐位 byte-gate、n=21..120）／`oracle [K]`（注入 `fp_sol` 逐 block 中心 = 完美位置種子、掃 `ICCAD_ANCHOR_W∈{0.1,1,5,20}` 取每案最佳、鏡射 `oracle_perm_probe.py` 報 vs base（隔離 anchor 機制）與 vs portfolio（真 bar）兩增益 + 預註冊判定，dump `m68_oracle_stdout.txt`）。判定 RED（anchor +2.18% vs base / +0.001% vs portfolio）見 ledger M68 條 + memory `[[m68-ml-seed-red]]`
- **M57-M61 probe 工具（2026-07-16/17，全 RED 存檔，永不 ship）**：`m57_reanchor_probe.py`（reanchor/diag/diag2 三模式，`force_bnd` 支援 `(block, mask)`；`results_M57_reanchor.json`）、`constructive_m58.cpp/.exe`+`m58_nsoft_probe.py`（`ICCAD_NSOFT_OFFICIAL=1`；`m58_cache.pkl`+`results_M58_nsoft.json`）、`constructive_m59.cpp/.exe`+`m59_refine_seed_probe.py`（`ICCAD_REFINE_DUMP=file` 逐 frame/pass dump c1/c2；`m59_cache.pkl`+`results_M59_refine_seed.json`+`m59_rows.json`）、`constructive_m60.cpp/.exe`+`m60_anchored_deficit.py`（`ICCAD_ANCHOR_TRACE=file`；`m60_cache.pkl`+`m60_forks.json`）、`constructive_m61.cpp/.exe`+`m61_event_frame_probe.py`（`ICCAD_FRAME_EVENT_TRACE=file`+`ICCAD_FORCE_FRAME=WxH`；`m61_cache.pkl`+`m61_events.json`）。副本 trace-off 皆過 byte-gate 逐位=shipped；復現指令見各 `M5X_REPORT.md`
- **`m52_phase0_probe.py`（M52 Phase 0 probe，永不 ship）**：`pipeline|slack|legalize|vocab|all [quick]`——tree_sol→`render_bstar`→`compact_down`/`legalize_v0`→官方 `evaluate_solution` 用 **`_cost_strict`**（⚠️ 勿用 `tree_decode_probe._cost_of`：`target_positions=None` 不查 preplaced/fixed 硬約束）；合法性保持 tree 擾動（MOVE_SUB/MOVE_LEAF/SWAP/FLIP + `near_miss_one`）、Y transposition、面積精確 dims 擾動（MIB 群組一致）；`legalize_v0` = obstacle-aware ordered gravity（未擾動恆等 = 逐位重現 fp_sol）；cache `m52_phase0_cache.pkl`（sig 驗證）。訓練集限定（驗證集無 tree_sol）；報表 R = cost/fp_floor per-case、wRq = 理想 soft 修復上界
- **離線天花板探測（永不 ship，保留為記錄）**：`oracle_perm_probe.py`（M26 ordering）、`dbg_seqpair.py`（M27 global-packer）、`reconstruct_probe.py`（M28：oracle 1.1079、headroom 100% quality；快取 `reconstruct_probe_cache.json`）、`tree_decode_probe.py`（M29 decoder：tree_sol=B\*-tree、X 規則 100% 精確、Y 序是品質 lever；讀訓練 `floorset_lite/*.th`）、`tree_build_probe.py`（M29 greedy builder 死路 8.22）、`recon_slice_probe.py`（M40 遞迴二分 slicing：雙閘死、reconstruction RED-confirmed）

## 檔案結構（要點）
- `constructive.cpp` 🏆 — placer，含 M9-M37 全部行為 + M46 hot-path exact 加速（`M46KeyHash`/`M46Grid`/opt-A，bit-identical）（見 env 旋鈕）+ METRICS stderr
- `optimizer_constructive.py` 🏆 — 41-prof portfolio（M51 #40 `fa22_fc_pin_tight_wire` = wide-clamp frame、案 99 winner）+ shapely proxy(_RH=1.4)；pruned profile 標 `[Mxx-pruned]` 註解可復原；**M41+M42+M45 `_pool_indices()` default-on 四階砍 profiles：swap + `_BIG_REDUNDANT_IDX`（n>100）+ `_M45_BAND_DROP`（universal 中案 band）+ `_M45_LOWCORE_DROP`（`_effective_cores()≤8` 才啟用）**；**M47 proxy 路徑：`as_completed` 就地算 proxy（一次一個，⚠️ 並發=GIL thrash 4× 慢）+ `_hpwl_b2b_fast`/`_hpwl_p2b_fast` tolist 版（逐位=harness 版，動它必重過 `m47b_proxy_equiv.py`）**；**M48 hardening：`_ensure_compiled` 編譯鏈+`_binary_runs()` smoke、`solve()`（安全 wrapper）→`_solve_impl`（原 body）三層安全網（SA→`_row_fallback`）+ selection 守門回 cands[0]**；**M49+M50 `_band_env()`：n>100 疊 `ICCAD_REFINE_ITERS=4`、60<n≤100 疊 `=8`（低核 cores≤8 改 `=4`，`_M50_REFINE_LOWCORE`；`ICCAD_ADAPTIVE_REFINE=0` 關）**；旋鈕見 env 節
- `optimizer_claude.cpp/.py/.exe` — 舊 SA，僅 fallback
- `floorplan_gnn.pth` — v1 GNN（unsupervised，僅舊路線）；`gnn_training.md` — ML 文件
- `iccad2026contest/iccad2026_evaluate.py` — 評估腳本

## ML（M52 Phase 0 量測 RED 2026-07-11——證據見「未來發展方向 §0b」與死路 ledger；歷史見 `gnn_training.md`）
- **歷史（v1-v3，勿重走）**：v1 unsupervised GCN 退役；v2 supervised MSE on `fp_sol` 失敗（ill-posed 一對多）；v3 pairwise ranking 訊號弱 + M26 oracle-perm 證實「訊號餵我們的 placer」cap 在 +0.005% → 該類路線永久封卷
- **M52**：生成式 imitation（tree-空間與 direct-layout 皆然）被「容錯帶=0 × 輸入零訊號」二重殺死（見 §0b）。重開唯一條件：rival 1.29 方法細節證明 legit、非 imitation、非 leak → 歸 M27 packer 軸（cost-driven sampling 搜尋），非 ML 軸
- ⚠️ 本環境**禁止跑訓練、可推論**：訓練 script 交使用者上 CUDA；需要訓練時**停下告知使用者**

## 舊 SA 路線（fallback only，`optimizer_claude`）
峰值 8-profile 3.0625。constructive 已全面超越，僅作 fallback。`analyze_violations.py`/`check_viols.py` 不可跑（用 `viol_breakdown.py`）。
