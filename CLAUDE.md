# ICCAD 2026 FloorSet — Session Context

## Claude 對話框規範
- 聊天室語句**盡量精簡**、用**繁體中文**。

## 🚨 範式轉移（最重要，先讀）

**這題是 reconstruction（還原 baseline 原圖），不是 floorplan optimization。**

- Cost = `(1 + 0.5·(HPWL_gap + Area_gap)) · exp(2·V_rel)`：gap=0 ∧ V_rel=0 → **Cost=1.0**（理論最小）。「找最佳解」永遠 HPWL_gap>0 → Cost>1；還原原圖才能 gap≈0。
- 真天花板 ~1.1（`fp_sol` verbatim = **1.1079**，headroom 100% 在 quality、violation 已贏）。組員 **1.0322 是 oracle**（讀本地 validation label，hidden test 退 fallback → 不適用）；legit 上限 ~1.62。
- 訓練 `fp_sol` = ground truth (w,h,x,y)，但無監督 loss 沒用它 → per-block local ML 在這題很弱（組員 v10-v12 全 <1%）。
- **現況：M41（2026-06-25）開全新 RuntimeFactor 軸（與 quality 正交，被本地 eval 強制 RF=1.0 藏住）；M42（2026-06-26）第二槍；M45（2026-07-02）第三槍 per-band tiers + cores-adaptive；M46（2026-07-03）第四槍 C++ hot-path exact 加速；M47（2026-07-04）第五槍 wrapper overhead（proxy overlap + tolist internals）；M48（2026-07-04）送件 hardening（編譯鏈+binary smoke、solve() 三層安全網、冷啟動 dry-run；零分數變更）；**M49（2026-07-07）第六槍量測型 REFINE band-cut**（n>100 全 profile `REFINE_ITERS 12→4`，第一個有數據的 quality-trade）；**M50（2026-07-09）第七槍 mid-band REFINE 兩層 tier**（universal 60<n≤100 `=8` + 低核 cores≤8 深化 `=4`）**。本地分 1.3269→1.3277→1.3281→**1.3285**（RF=1.0 fiction；M41 +0.06%、M49 +0.027%、M50 +0.028%）：M41 砍 6 隻 swap profiles（OS16×3/OS8×2/OM8，大案 18-20s 純 runtime 死重）→ avg 9.89→5.90s；**M42 再砍 21 隻「不贏任何 n>100 案」的 build profiles（per-big-n 冗餘，非全域 LOO）→ avg 5.90→4.73s（−20%）、big-case wall n=120 砍半 15.6→8.0s、quality BIT-IDENTICAL（local 仍 1.3277、20 案全 median-independent WIN）**、投影 real 疊加 M41 共 **~−22%**（@ M=11、robust 跨 M∈[6,20]）、100/100 feasible；**M46 再 exact 加速 pack 熱路徑（opt-A/B/C，全池 4100/4100 positions bit-identical）→ avg 4.73→2.70s、@12c M=11 投影 1.1473→0.9757（再 −15%）、零下行（純加速無 +0.06% local 代價）**；**M47 堵 wrapper overhead（計時窗內串行 shapely proxy = n>100 wall 的 29.2%、rf_score_model 從未建模）：proxy 改 as_completed 與 pool 重疊 + proxy 內部 tolist 化（300/300 逐位 gate）→ avg 2.70→1.85s、n>100 total −34.8%、tail 29.2%→0.4%、eval 1.3277 逐位——把投影 0.9757 的兌現誤差從 +8~11% 堵到 ~2%**；**M49 量測 REFINE 曲線後砍 n>100 的 refine passes 12→4（+0.027% local、唯一 mover case 85 +0.95%、20/20 medIND WIN）→ band wall −50%（eval 61.0→30.4s）、avg 1.54s、投影 real @12c M=6/8/11 = −12.6/−7.9/−2.4%、最壞 +0.03%**；**M50 對 mid band 同法兩層 ship（trace mid 證同樣無 exact 截斷；ungated K=4 高核淨損 +0.069% → universal K=8：6 movers、+0.028% local、mid wall @12c −20.2%、投影 @12c M=6/8/11 = −1.14/−0.85/−0.13%、最壞 cell +0.03%；低核 cores≤8 改 K=4：@4c 全 M −2.0~−3.5%、fail-open 回 K=8）→ eval avg 1.49s、逐案 diff 恰 6 mover 全合 probe 預測**；**M51（2026-07-10）首個 M37 後 quality add：wide-CLAMP frame profile**（`FRAME_ASPECTS` 4th slot ≥2.0 在 preplaced-heavy 案 clamp 成預設集永不生成的固定寬 outline；`fa22_fc_pin_tight_wire` #40 破最重案 99 n=120 1.3334→**1.3084**、bonus case 47 −0.0063 → local 1.3285→**1.3265**（−0.15%）、eval diff 恰 2 案零回歸、100/100 feasible、avg 1.60s；同 session 證 BP_WEIGHT-down 全死）。**quality 軸 converged（M51 收掉最後 in-space 殘渣）**（packer/order M26-27 + free-aspect 六子軸 M29-M37 + reconstruction M40-RED + M51 frame-aspect 殘渣全探畢）；RF 軸唯一前提是「官方真套 RF」（否則只損 ~+0.09% local，極不對稱賭注）。**2026-07-07 評估：原掛牌的 frame-B&B exact 早棄經 code 驗證改判 RED**（guide 鏈 + nudge 非單調，見死路 ledger）。**2026-07-10 ML 軌重開（M52）→ 2026-07-11 Phase 0 量測 RED**（`m52_phase0_probe.py`，零訓練即判死）：管線 GREEN（260 案 100% 逐位重現 fp_sol）但**容錯帶 ≈ 0**——單一 near-miss token 錯 = wR 1.232（理想 soft 修復後仍 +7.2%）、dims 任何擾動 wR≈2.9 且 ε→0 已滿高；疊 index-probe 零訊號 → **imitation 家族（含 direct-layout 模型 B）全滅**，見未來方向 §0 + 死路 ledger + `[[m52-phase0-red]]`。逐版細節 git log + memory（`[[m50-midband-refine-tiers]]`…`[[m29-tree-decoder]]`、`[[ml-track-reopened]]`）。

## 評分公式（2026-05-23 確認）

- **Cost**（per case）= `(1 + 0.5·(HPWL_gap + Area_gap)) · exp(2·V_rel) · max(0.7, R^0.3)`
  - 不可行 = 10.0；feasible 上限 9.999999；gap 從下方 clamp 到 0（贏過 baseline 無額外獎勵）
  - `V_rel = (V_boundary + V_grouping + V_mib) / N_soft`，`N_soft = boundary blocks + Σ(MIB-1) + Σ(Cluster-1)`
- **Total Score** = `Σ Cost[i]·exp(n_i/12) / Σ weight`
  - 權重 e^(n/12)：n=120→8.0%、n≥110 累計 ~53%（中小型 case 比舊版重很多）；總權重 ≈ 275418
- **RuntimeFactor** = `max(0.7, R^0.3)`（逐案，`evaluate.py:552`），R 分母 = cross-submission median（未知，組員 ~11s 唯一參考）。本地 eval **強制 =1.0**（`:924-940`）→ **M1-M37 每次 A/B 對此項全盲，1.3269 是 RF=1.0 fiction**。**懲罰比 = (t1/t2)^0.3，與 median 無關**（→ 逐案可本地判定）。**M41 反轉：RF 不只是約束、是 lever**——`cost∝t^0.3`，砍大案 wall 是 median-independent 的 real-score 增益（同案 `Q_cap/Q_full < (t_full/t_cap)^0.3` 即贏；大案品質結構卡死 → 幾乎恆贏）。見 `[[m41-runtime-factor]]`、`rf_score_model.py`。

## 目前狀態

### 🏆 最佳：local **1.3265**（M51 2026-07-10；quality add −0.15%）/ RF 鏈 M41-M50 全保持（投影 real @12c M=11 ~0.975 量級、M49 後大案 wall −50%、avg **1.60s**、100/100 feasible）＋ M48 送件 hardening

M51 = `fa22_fc_pin_tight_wire`（#40）：`ICCAD_FRAME_ASPECTS=1.0,0.75,1.35,2.2` 疊 fc_pin_tight——**wide-CLAMP 機制**（aspect ≥2.0 的 frame 在 preplaced-heavy 案被 `frame_candidates()` 的 `pre_*+MARGIN`/`max_i*+MARGIN` 下限 clamp 成同一個固定寬 outline，預設集最寬 1.8 從未生成它；2.0/2.2/2.5 逐位同值、REFINE K=4/12 逐位同值）。破最重案 99（n=120、權重 8%）1.3334→1.3084 + case 47 1.2242→1.2179；官方 eval **1.3265**、diff 恰 2 案全改善。驗證鏈全綠：re-audit（4200 combos、proxy 確選 #40）→ rf_score_model regen（`_BIG_REDUNDANT_IDX` +#18=22 隻、tier-4 兩 band +#40、#19/#21 tie 保持、全 asserts OK）→ m49 三 gate（control 100% EXACT、big 唯一 mover 仍 case 85、mid 同 6 movers）。

M41+M42+M45 default-on（`ICCAD_ADAPTIVE_POOL=1`）**四階砍 profiles**（皆在 `_pool_indices()`）：M41 砍 swap（全案）、M42 砍 22 隻 build 冗餘（`block_count>100`；M51 起含 #18）、**M45 tier-3 universal** 砍 9 隻中案 band 冗餘（`60<n≤100`，35→26，`ICCAD_ADAPTIVE_BAND=0` 關）、**M45 tier-4 低核**（偵測 cores≤8 才啟用：(40,60]→14、(100,110]→8、(110,∞]→9；M51 起 (40,60]/(100,110] drop 集含 #40，`ICCAD_ADAPTIVE_CORES` 強制/關閉）。M45 用 **strict selection-preserving gate**（逐案 cost 相等才砍）→ local 逐位不變、任何 median/cores 下弱贏。`ICCAD_ADAPTIVE_POOL=0` 還原 full 41-prof = quality-best **1.3248**（M51；audit est wall 4.69s；**同時還原 M49 full REFINE**）。**M49+M50 wrapper lever（`_band_env()`，每 profile env 疊 `ICCAD_REFINE_ITERS`）**：`block_count>100` 疊 `=4`（M49：19/20 案逐位不變、唯一 case 85 +0.95%、+0.027%）；`60<n≤100` 疊 `=8`（M50 universal：34/40 案逐位不變、6 movers、+0.028%），偵測 cores≤`_M45_CORES_MAX(8)` 時 mid 改疊 `=4`（`_M50_REFINE_LOWCORE`，fail-open→universal 安全檔）；`ICCAD_ADAPTIVE_REFINE=0` 關全部。⚠️ 官方 local eval 顯示 **1.3285**（RF=1.0 fiction）——真增益在 real RF 項，本地永遠看不到（見 RuntimeFactor 節）。**M46（2026-07-03）與 pool-prune 正交**：`constructive.cpp` pack 熱路徑三個 exact 優化（opt-A skip 未讀 xs/ys sets、opt-B hash dedup、opt-C 32×32 grid overlap；儀裝與 opt-D 未帶入）→ re-audit 4100/4100 positions 完全相同、官方 eval 1.3277 逐位不變、avg 4.73→**2.70s**。

`constructive.cpp`（C++ 建構式定框 placer，B 路線重寫組員架構）+ `optimizer_constructive.py`（portfolio wrapper）。**確定性**（無 randomness/限時 → run-to-run 一致，可精確 A/B）、8.78s/case 單 profile。**proxy 自 M13 起 = oracle ceiling**（完美選擇，加 profile 全額 realize → selection 不再是瓶頸）。

### 單 profile 架構（5 階段，~0.16s/case）
1. **boundary-aspect dims**：LEFT/RIGHT-only aspect **2.50**、TOP/BOTTOM-only **0.40**（拉高 edge capacity 降 vBd，最高 ROI insight）
2. **MIB 形狀統一**（`apply_safe_mib_dims`）：master 相容→用 master；否則 movable ≤1% area→`sqrt(avg)` 方形。保 1% 硬約束 → vMb 145→0
3. **cluster 建構**：純 movable→複合 item（3 ordering×5 layout，key=`(fragments, boundary_bad, area, aspect)` 字典序）；mixed(preplaced+movable)→anchored（first-pass 貼 preplaced「牆」）
4. **定框 greedy packing**：試 4-5 個 outline frame（面積小優先），每 item boundary-aware 候選評分（`bbox_area + 0.10·anchor + ww·WIRE·wire + BP_W·boundary_miss`），ww base **×2000**；layout_score 挑最佳 frame
5. **後處理**：compaction（M10）→ wire refinement（M9）→ HPWL push/slide/swap/jump（M14-16/24）

### Portfolio 層
平行跑 41 deterministic profile（env 旋鈕變體；M51 加 #40 `fa22_fc_pin_tight_wire`），用 **baseline-free proxy** 選最佳：
- proxy = `(area/Â + _RH·hpwl/hmin)·exp(2·vrel)`，Â=1.035·ΣblockArea，hmin=該 case 各 profile 最小 hpwl，**_RH=1.4**（補償 hmin/hbase≈1.3-1.4 對 hpwl 項的低估）
- ⚠️ **vrel 必須用 shapely 算**（wrapper `_proxy_metrics`），不可用 C++ union-find（1e-3 tol，34/100 案不一致）
- 下檔保護：無用 profile 不被選、不傷分（只花 runtime）

### 演進里程碑（deterministic A/B；M4 起累計 −38.5%）
M1 singles 3.62 → M2 cluster 2.35 → M4 +MIB/layout-key/wire×2000 1.82 → M5 anchored 1.7045 → M6-8 portfolio 1.5659 → M9 wire refine 1.5375 → **M10 %.17g + compaction 1.4528** → M12 40-prof 1.4371 → M13 narrow frame + _RH=1.4 1.4349 → M14-16 HPWL push 1.4231 → M17-23 pack-order 1.3983 → **M24 HPWL jump 1.3862** → M25 審計剪枝(38-prof) → **M26 GUIDE_MED 1.3843** → M27 global-packer 死路 → M28 reconstruction ceiling(GREEN) → **M29-M37 free-aspect 六子軸 1.3843→1.3269**（每子軸見下方 env 旋鈕 + memory）→ M38/M39 殘渣收束 → **M40 reconstruction RED-confirmed**（quality 軸到此 converged）→ **M41 RuntimeFactor lever（正交新軸）：砍 6 隻 swap profiles，local 1.3269→1.3277(+0.06%)、avg 9.89→5.90s(−40%)、投影 real ~−12%** → **M42 RF 二階：砍 21 隻 build 冗餘(n>100, per-big-n)，local 1.3277 bit-identical、avg 5.90→4.73s(−20%)、big-case wall 砍半、投影 real 疊加共 ~−22%** → **M45 RF 三槍 per-band tiers（2026-07-02）：tier-3 universal 砍 9 隻中案 band 冗餘（60<n≤100、strict cost-equal gate）+ tier-4 低核偵測深砍，local 1.3277 逐位不變、投影 real 再 −1.2~1.5%（universal ∀cores）/ 低核機 −3.7~−9.3%** → **M46 RF 四槍 C++ hot-path（2026-07-03）：opt-A/B/C exact 併入 constructive.cpp，4100/4100 positions bit-identical、eval 1.3277/100 feasible、avg 4.73→2.70s、@12c M=11 投影 1.1473→0.9757（−15%、∀median ∀cores 無條件弱贏）** → **M47 RF 五槍 wrapper overhead（2026-07-04）：proxy as_completed 重疊 pool（argmin by-construction 不變）+ proxy 內部 tolist 化（hpwl per-edge tensor 索引 ~315ms/call → ×11.5，300/300 逐位 gate），eval 1.3277 逐位/Avg Cost 不變/100 feasible、avg 2.70→1.85s、n>100 total 86.9→56.7s（−35%）、串行 tail 29.2%→0.4%** → **M48 送件 hardening（2026-07-04）：編譯鏈+smoke、solve() 三層安全網、冷啟動 dry-run，零分數變更** → **2026-07-07 評估：frame-B&B exact 改判 RED、殘餘 backlog 定案（見未來方向 §1）** → **M49 RF 六槍量測型 REFINE band-cut（2026-07-07）：RTRACE 儀裝量出 score-vs-pass 曲線（無 exact 截斷點：17/260 勝出改善落在最後 pass；cycle 2/1040 RED；max_trials=3 非 exact 15/260 緩辦），variant 實測 K=4 弱支配 6/8（19/20 案逐位不變、case 85 +0.95%、local 1.3277→1.3281 +0.027%、20/20 medIND WIN）→ ship `_band_env()` n>100 疊 `ICCAD_REFINE_ITERS=4`，eval 逐案 diff 唯一 case 85、avg 2.00→1.54s、n>100 −50.1%、投影 real @12c M=6/8/11 = −12.6/−7.9/−2.4%、最壞 +0.03%** → **M50 RF 七槍 mid-band REFINE 兩層 tier（2026-07-09）：trace mid 1000 pairs 證無 exact 截斷（40 勝出改善於 pass 11；cycle 538 events 僅記錄）；variant 量測 ungated K=4 高核淨損（11 movers、+0.069% local、@≥12c M≥11 +0.06~0.07%）→ ship universal K=8（6 movers 49/54/61/64/68/70、+0.028% local→1.3285、mid wall @12c 91.6→73.1s −20.2%、投影 @12c M=6/8/11 = −1.14/−0.85/−0.13%、最壞 cell +0.03%）+ 低核 cores≤8 tier K=4（@4c 全 M −2.0~−3.5%、@8c M≤14 全贏；`_M50_REFINE_LOWCORE`、fail-open）；eval 逐案 diff 恰 6 mover 全 MATCH probe 預測、100/100 feasible、avg 1.71→1.49s（mid −24.6%）** → **M51 quality shot wide-CLAMP frame（2026-07-10）：standalone FRAME_ASPECTS/BP_W-down 全 ~0（11 掃描），但 wide ≥2.0 疊 fc_pin_tight 觸發 clamp-frame → 案 99 +1.9%（2.0/2.2/2.5 逐位同值 = clamp-invariant、K=4 逐位有效）；ship #40、regen 全常數（+#18 入 big-drop）、三 gate 綠、官方 eval 1.3285→1.3265、diff 恰 {99,47} 零回歸**。

## 🔑 戰略結論：quality 三面天花板皆探畢；**RF 軸七槍已 ship（M41+M42+M45+M46+M47+M49+M50）**

1. **ordering / ML 永久封卷**（M26 oracle-perm，`oracle_perm_probe.py`）：注入完美 fp_sol 排序，placer 只多拿 +0.002%（類內）/ +0.005%（全域）→ 瓶頸是 **placer**（greedy+compact+push）非 pack order。⇒ refinement pair-relocation / order-LNS / 監督式 ML ranking 全不值得。
2. **更好的 packer 封死**（M27 global-packer，`dbg_seqpair.py`）：greedy 已在 (area,HPWL) frontier；agap 與 hgap **結構耦合**（wire-driven 花 area 換低 HPWL）+ cluster/preplaced 強迫 void → B*-tree/SP/skyline 重寫不值得。
3. **reconstruction RED-confirmed**（M28 GREEN→M29 YELLOW→**M40 RED**）：headroom +0.219（oracle 1.1079，100% 在 quality，top-15 大案佔 68.9%）但 X 結構不可從 connectivity 還原（M40 Spearman 0.009）+ Y 序需 label（M40 deterministic +159% vs oracle-Y）→ 重寫 slicing placer 不可行。詳見死路 ledger + `[[m40-reconstruction-red]]`。
4. **RuntimeFactor 軸（M41+M42+M45+M46，四槍已 ship）**：以上三點全是 **quality**；計分式還有 `max(0.7,R^0.3)` 一項，本地 eval 強制 =1.0 而被全程忽略。`cost∝t^0.3` + 大案 quality 卡死 → 砍 wall = median-independent real-score 增益。**M41** 砍 swap（投影 ~−12%）；**M42** 砍 21 隻 n>100 build 冗餘（per-big-n；wall 砍半、疊加 −22% @M=11）；**M45**（2026-07-02）把 redundancy 從累積（n>T）推廣到 **band-scoped**（不贏任何 lo<n≤hi 案）：中案 band 冗餘的大案 winner（#2/#13/#17/#18）在中案是純 sum 死重 → tier-3 universal 砍 9 隻（中案 12c 也 sum-bound → −1.2~1.5% ∀cores）+ tier-4 低核（cores≤8）深砍（@4c −9.3%、slowness uncapped 的下行保險）。M45 gate 升級為 **strict selection-preserving**（逐案 cost 相等，比 WIN 不等式強——後者在 RF floor 附近高估）→ 任何 median、任何 cores 弱贏。官方 cores/median 皆未知（PDF 無 specs）→ cores 偵測 fail-open 回 shipped 行為。**M46**（2026-07-03）在 pool-prune 觸底（(100,∞] candidates=0、max-setter 結構設底）後轉攻 wall-setter 本身：C++ exact 加速 pack 熱路徑（α≈0.5）→ 每案 t'≤t → ∀median ∀cores 無條件弱贏、零 local 代價。**M49**（2026-07-07）攻最後一塊大頭 REFINE（12/13 packs、從未量測）：先量曲線證無 exact 截斷點，再以 M41 式 trade ship `REFINE_ITERS 12→4 @n>100`（+0.027% local 全來自 case 85 一案）——floor 飽和使 M≥11 的 K=4/6 等值，K=4 的優勢在 M=6-8（−8~13%）；@4c sum-bound 使任何 median 都保增益。**M50**（2026-07-09）把同一 lever 推進 mid band（60<n≤100、40 案×kept 25）：mid @12c 高 M 大多 RF-floored → quality 損直接穿透，ungated 深砍（K=4）在高核淨損 → **分層**：universal K=8（+0.028% local 換 @12c M≤11 全贏、最壞 +0.03% = M49 同天花板）+ 低核 cores≤8 深化 K=4（M45 tier-4 doctrine：sum-bound regime 才付得起，@4c 全 M −2.0~−3.5%）。mid 案的 cross-team median 大概率比 big 低（人人都快）→ K=8 的 M=6-8 增益帶比表面更可及。見 `[[m50-midband-refine-tiers]]`、`[[m49-refine-band-cut]]`、`[[m46-wall-setter-speedup]]`、`[[m45-band-tiers]]`、`[[m42-runtime-2nd-order]]`、`[[m41-runtime-factor]]`。

## 未來發展方向（M51 後；2026-07-10 更新）

> quality 軸 converged（M51 收掉最後 in-space 殘渣）；**RF 軸已 ship 七槍**（M41-M50）；**送件 hardening 已 ship（M43+M48）**；**frame-B&B exact RED（2026-07-07）**；**M52 ML 軌 Phase 0 量測 RED（2026-07-11，§0）**。**主線回到維護態：Beta/Final 送件檢查（§3）**；追分只剩未知結構洞察。依 ROI：

0. **M52 ML 軌 Phase 0：RED（2026-07-11 量測，`m52_phase0_probe.py`；零訓練即判死、一張 GPU 都沒燒）**
   原計劃 = tree-空間自迴歸取樣 → M29 decoder → legalize → portfolio proxy 選（完整計劃見 git 歷史與 `[[ml-track-reopened]]`）。Phase 0 三量測結果：
   - **(i) 管線 GREEN**：true tree + oracle-Y + true dims → `render_bstar`+`compact_down`/`legalize_v0` → 官方 `evaluate_solution`（strict `target_positions`）**260 案 100% 逐位重現 fp_sol**、cost 相等、fp 全 strict-feasible（訓練 floor vrel 0.10-0.14）；decode+legalize 僅 2-3ms/案。⚠️ gotcha：`tree_decode_probe._cost_of` 傳 `target_positions=None` = **不查 preplaced/fixed 硬約束**——後續任何實驗必用 probe 的 `_cost_strict`。
   - **(ii) slack：容錯帶 ≈ 0（懸崖非斜坡）**：單一最溫和 **near-miss** token 錯（最近鄰 anchor 重接一個 movable leaf）= **wR 1.232**、理想 soft 修復上界 **wRq 1.072**——一個 token 就吃掉整條 1.10 容錯帶；random-slot 單錯 1.319/1.088；2% token 錯 wR 2.71；Y-order 2% 1.73；**dims 任何擾動（±2% jitter 或 8/16/32 檔量化）wR ≈ 2.9-3.1、曲線在 ε→0 已滿高**。機制：**27 boundary blocks/案** 的 1e-6 bbox-edge 接觸 + cluster abutment 被任何幾何誤差打碎 → `exp(2·vrel)` ×2-3，疊 X-rule 傳播的 area/HPWL gap。
   - **(iii) legalizer v0**（obstacle-aware ordered gravity + preplaced snap + reservation bump）：硬可行修復 **100%（構造保證）**、self-inflation 恰 0、歸因 **model(+213%) ≫ interact(+42%)** → 懸崖是模型誤差本身，quality 不可修。
   - **判死鏈（imitation 家族全滅，含原備援模型 B）**：(a) 容錯帶=0（本次）× (b) 輸入零訊號（`recon_index_probe`：netlist/index/area 對 layout Spearman ~0.01 → 任何 imitation 模型 token 準度必然低）→ 模型落在曲線深處（s≥0.4 端 wR 7-8）。**direct-layout 模型 B 同死**：懸崖機制與表徵無關（dims_jit 即純幾何擾動證據），且它連 tree decode 的結構性 X-abutment 都沒有；v2 MSE 前科 + M26 訊號注入 cap 封掉「模型出座標→placer 修」全部變體。**勿再嘗試任何以 fp_sol 為監督的生成/回歸。**
   - **rival 1.29 重解讀**：零容錯 + 零訊號下，他隊若 legit 必非「還原/imitation」而是 **cost-driven 大量取樣搜尋 + 約束強制 legalizer**（「用時非常久」吻合）= 實質重開 M27「更好的 packer」（我們 greedy 已在 frontier）——或 label leak（組內 1.0322 前科）。**重開 ML 唯一條件：取得他隊方法細節、證明 legit 且非 imitation**（屆時歸 M27 packer 軸評估，非 ML 軸）。
   - **正面資產**：vocab 掃描（20160 案 / 1.24M movable）：W/H/X/Y **100% 整數**、`area==W·H` 精確、**整數 divisor-pair p50=2 / p95=6** → 未來任何軌的 dims 表徵 = divisor pointer（近乎免費）；boundary 27.3/案、preplaced 2.9、fixed 7.3、clusters 3.5、MIB 群 1.0（僅 0.5% 群 dims 一致 = label 自帶 vMb floor）。decode+legalize 管線、擾動算子、`m52_phase0_cache.pkl`（9160 entries）留供重現。

1. **M49 已 ship（2026-07-07 本 session；見 `[[m49-refine-band-cut]]`，工具 `m49_refine_probe.py` + RTRACE 儀裝）**：原 backlog (a)(b) 兩項一次結案。**(a) REFINE band-cut**：trace 260 pairs 證**無 exact 截斷點**（17/260 勝出 frame 的最後改善落在 pass 11——refine 改善不飽和，Path A 死）→ 走量測 trade：K=4/6/8 三者 local 損**完全相同**（19/20 案 cost 逐位不變、唯一 case 85 +0.95%、其勝出 profile #23 在 pass 11 改善、無中間 K 可救）→ **K=4 弱支配** ship（n>100 疊 `ICCAD_REFINE_ITERS=4`）。eval **1.3281**（+0.0269%、與 probe 預測逐位合）、逐案 diff 唯一 case 85、100/100 feasible、avg **1.54s**、n>100 total 61.0→30.4s（−50.1%）；投影 real @12c M=6/8/11/14/20 = −12.6/−7.9/−2.4/−0.02/**+0.03%（最壞）**、@4c 任何 M 都獲利（M=20 仍 −0.9%）。**(b) refine ≥2-cycle：RED**（2/1040 鏈、期望 ~0.09% band wall，勿實作）。**max_trials=3 緩辦**：非 exact（第 4 frame 勝 15/260=6%）、M≥11 floor 飽和使邊際 ≈0、要動 shipped C++ + 全重驗鏈 → 數據已存檔（`m49_refine_probe.py trace`），僅當證據顯示官方 M≤8 才回頭。⚠️ 後續規則：**任何 `_PROFILES` 修改或 exe 重編後，除 rf_score_model 常數外還要重過 `m49_refine_probe.py` gate（M50 起 = `variant 4 big` + `variant 8 mid` + `variant 4 mid`）**（band 內 selection/quality 可能移動）；勿在 M≥11 疊更多 wall cut（floor 飽和、只付 quality 不收 RF）。**M50（2026-07-09）已把 mid band 補齊**（`variant`/`project` band 參數化後量測；ungated 深砍高核淨損 → 兩層 tier ship，詳見里程碑與 `[[m50-midband-refine-tiers]]`）；小案 band（n≤60）deep floor + wall 極小 → 不值得掃。
2. **M51 已 ship（2026-07-10 本 session）——原 §2 兩 probe 一次結案**：**(a) FRAME_ASPECTS**：standalone 6 集合（wide 2.2/2.6、wide-only、mid-fill、no-square、mild-tall）全 ~0.000%——但 **stacked** 觸發 **wide-CLAMP 機制**（見「目前狀態」）→ ship `fa22_fc_pin_tight_wire`。掃描細節與勿重掃事項見死路 ledger「FRAME_ASPECTS M51」條。**(b) BP_WEIGHT 向下：RED**——10000/3000/1000/300 standalone 全 0 wins；3000 疊 M37 recipe / fc_pin_tight 兩 stack 亦全 0（M37 stack 甚至倒退：control 的 K-fiction wins 都被消滅）→ 雙向全死（原 ledger 只有向上）。quality in-space 殘渣至此**真正清空**——再追純 quality 只剩 §0 ML 軌與全新結構洞察。
3. **Beta/Final 送件前**：重跑 `m48_coldstart_dryrun.py` 四 phase + `rf_score_model.py` asserts + `m49_refine_probe.py variant 4 big` / `variant 8 mid` / `variant 4 mid`（control 逐位 gate）。
4. **M47 已 ship（2026-07-04；見 `[[m47-wrapper-overhead]]`，工具 `m47_overhead_probe.py`/`m47b_proxy_equiv.py`）**：`solve()` 計時窗內的 wrapper 開銷 rf_score_model 從未建模（模型只算 C++ wall）——實測串行 shapely proxy tail = n>100 wall 的 **29.2%**（case 99 40.8%、2.9s）。兩槍：**(a)** proxy 改主線程 `as_completed` 就地算、與尚在跑的慢 profile 重疊、一次只跑一個（⚠️ 13 個 proxy 丟 worker threads 並發 = GIL thrash **4× 更慢**，勿重試）；**(b)** proxy 內部 tolist 化（`calculate_hpwl_b2b/p2b` per-edge tensor 索引 ~315ms/call → **×11.5**、constraints 標量迴圈同治；fp32→fp64 exact widening 同值、同序累加 → 逐位相同，300/300 gate）。結果：eval **1.3277 逐位**、Avg Cost 逐位不變、avg **2.70→1.85s**、n>100 total −34.8%、tail →0.4%——投影 0.9757 的兌現誤差（實測 t 比模型高 ~40% → RF +8~11%）堵到 ~2%。殘餘 wrapper 開銷只剩 `_serialize_input` pre ~2.8%（動 SA fallback 共用碼、收益 ~0.08s/大案 → 不值得）。
5. **M46 已 ship（2026-07-03；見 `[[m46-wall-setter-speedup]]`）**：pack≈98% of wall（cand_gen 66% / overlap 29%）→ 三個 exact 優化併入 `constructive.cpp`：**opt-A** 跳過 interior item 永不讀的 xs/ys set 插入、**opt-B** dedup 換 hash（保 first-occurrence 順序）、**opt-C** 32×32 uniform grid overlap（布林等價）；計時儀裝與 **opt-D（RED：refine 12 pass 內從不逐位收斂，勿重試）**未帶入。**驗證三層全過**：probe verify+verify100 **600/600 byte-identical**（merged vs probe，傳遞性=與舊 baseline 全同）→ re-audit（555s vs 舊 ~1.5h，α 即時實現）**4100/4100 positions 完全相同** → 官方 eval **1.3277**、100/100 feasible、avg **2.70s**。`rf_score_model.py` asserts 全過（M42 `refine(100)`==shipped、M45 tier-3/4 常數零漂移、#19/#21 tie 保持 2.766374981）→ **drop 常數全部不變**。投影 real @12c M=11 **1.1473→0.9757（−15.0%）**、band wall (110,∞] @12c 3.92s、M45 tier 增益結構保持（uni ≤−1.6%、@4c 低核 −9.4%）。**殘餘 RF headroom 有限**：cand_gen 剩 sort 本身（TOL 比較器 + std::sort 不穩定 → **候選生成順序與 sort 絕不可動**）、REFINE/frames 砍次數屬 quality-affecting tier；PGO 不採用（wrapper 現場編譯 + `[[codex-m43-lto-inert]]`）。**M47 α 表定量（rf_score_model M46 區塊，2026-07-04）**：@12c M=11 uniform α=0.9→−1.84%、α=0.7→−4.04%（floor 飽和，α=0.5 亦 −4.05%）；M≥14 幾乎歸零；小 median（M=6-8）才有 −8~−10% → 當時掛牌唯一剩餘 exact 構想 = **frame 級 B&B 早棄**＝conditional GO——**2026-07-07 code 驗證改判 RED**（LB 前提結構性不成立，見死路 ledger 與本節 §1）。⚠️ 高核 pool-prune 死路不變（`[[teammate-prune-v1-zero-gain]]`）；任何新 pool-prune 仍須過 `rf_score_model.py` M45 strict gate。
6. **submission hardening（M43 2026-06-27 已驗，三項全綠 ✅，零 code 變更；見 `[[m43-submission-hardening]]`）**：
   - **proxy generalization** ✅（`rh_sweep.py` + M43 size 分層）：oracle-min 1.3268、`_RH∈[1.1,1.6]` 全 1.3268–1.3271（basin 比預想 [1.3,1.6] 更寬平），shipped 1.4=1.3269（+0.0001 over oracle）。分層 small/mid/big 的 argmin 漂到 1.10–1.25（flat-basin 4th-decimal 噪音、cosmetic flag），但 `_RH=1.4` 對**各層自身最佳** gap ≤0.007%（small +0.00010 / mid +0.00009 / big +0.00000）→ 無論 hidden size 分佈如何偏都近 oracle，**非 overfit**（operative 指標是 gap 非 argmin）。
   - **feasibility 保證** ✅（`dbg_fallback_feasible.py` 新建）：`python_sa_solve` fallback **is_feasible 100/100**（⚠️ cost≈9.999999 是 feasible 品質上限 `M_PENALTY−1e-6`、被 `%.4f` 進位成「10.0000」，非 infeasible 的 10.0；判 feasibility 用 `SolutionMetrics.is_feasible` 不可用 cost）。pool 非空不變式：M41 砍 swap→34、M42 大案再砍 21→**13**，`cands==0` 才退 fallback → kept≥13≥1 恆成立。
   - **RF/median 不確定性** ✅（`rf_score_model.py`，重用 Jun-25 `audit_cache.pkl`、簽章仍符）：SANITY full-pool RF=1.0=**1.3269**；模型**逐位元重現** shipped `_BIG_REDUNDANT_IDX`（21 idx、FREE_N=100）→ 確認 M42 lever 即模型最佳；投影 real 在 **M∈[6,25] 每列恆更佳**（full→M41→M42 @M=11 = 1.4742→1.2904→1.1473、combined −22.2%）、n>100 **20/20 median-independent WIN**；最壞（官方忽略 RF）只損 +0.06% local。
7. **M48 hardening 已 ship（2026-07-04；見 `[[m48-submission-hardening]]`，工具 `m48_coldstart_dryrun.py`）**：(a) `_ensure_compiled` 編譯鏈（-O3→-O2 × msys g++→g++→clang++→c++、`ICCAD_CXX` 可強制）+ **`_binary_runs()` 1-block smoke**——關鍵：mtime 捷徑會接受「存在但不可執行」的 binary（如送件包夾 Windows PE 上 Linux 官方機）→ 無 smoke 則整條編譯鏈是死碼、每案靜默退 SA（總分 ×2+）；(b) `solve()` 三層安全網（body 原封搬 `_solve_impl`）：例外 → SA fallback → SA 也炸 → `_row_fallback`（preplaced 精確 + 其餘不重疊排列 = hard-feasible 拿 9.999999 cap 非例外的 10.0）＋ selection 段獨立守門（proxy/argmin 炸 → 回 cands[0] 保 C++ 好解：故障注入實測 1.37 vs SA 9.66）；(c) 冷啟動 dry-run 四 phase ALL PASS（編譯在 `_load_optimizer` 內、**計時窗外**，`evaluate.py:845` vs `:884`）。驗證：官方 eval **1.3277、逐案 100/100 cost 與 7/1 參考逐位相同**、100/100 feasible、repo exe hash 不變、故障注入 T1-T5 全過、smoke n=1 前測 OK。零 local 行為變更。
8. **若要再純-quality 追分**：in-space 殘渣已由 M51 清空（§2）、**§0 ML 軌已量測 RED（2026-07-11）** → 只剩未知的 constraint 結構洞察，或（唯一條件式重開）取得 rival 1.29 方法細節後走 M27 packer 軸評估。

### 精度 / 數值（持續遵守）
任何新加的、會被 shapely 評分的幾何輸出都要保持精確 abutment + `%.17g`（見 Gotchas）。

## 死路 ledger（勿重試）

- **boundary aspect port 到舊 SA**（2.50/0.40）：3.3258→3.4255 退步（skyline ≠ 組員 shelf，tall block 成 cliff）
- **preplaced-aligned frame**（攻 case 89）：greedy 不下 tighter width，case 89 結構性無解
- **cluster-rigid pack/slide**：cluster 無 slack（100 案僅 1 能動）+ 剛體平移破壞 abutment → shapely 假 fragment（**M10 精度牆，任何移動 cluster 成員的後處理都撞**）
- **violating boundary 修復**：202 violating 真值 0 可修（`dbg_vio_stats.py`：123 cluster+45 preplaced+34 single 全 BLOCKED）→ residual vBd 只能 packing 階段擺對
- **per-frame compaction + csc 重估 frame**：csc 固定 hw 跨 outline 失準（拿 vCl 換 vBd）
- **reframe**（`ICCAD_REFRAME`）：與 portfolio aspect 多樣性結構性冗餘（code 保留 gated off）
- **env knob 軸**：WIRE_MULT 4/6、ANCHOR 0.30、ultra-narrow frame、WT/BFS/NORM/PIN 組合、CLUSTER_ORD、OM×tight 全 ≤0.063%（⚠️ M32 例外：pure decoupled LR=4.5 +0.186% 已 ship）
- **per-block FREE_BOUNDARY（M32）**：0.000%——greedy 局部 area 項偏好窄塊（與 edge-capacity 要的「統一變扁」反向）⇒ boundary-aspect win 須 **UNIFORM**（profile 級），per-block 死，code 已 revert。uniform 版 = M32 ship
- **cluster aspect uniform 飽和（M33，勿重掃）**：standalone 弱（2.0=+0.14%），win 須 stack tight+FREE+PIN，寬 ratio 共振峰 **3.0**（+1.105%）；已加 ca{2.0,0.6,1.25,0.4,3.0}
- **cluster per-member 飽和（M34，勿重掃）**：共振 **4.0**（PIN +0.466%）、5.0 退步、narrow(max2.0) 僅 +0.143%；已加 fc_pin_tight + fc_gm_pin
- **MIB-member 飽和（M37 ship + M38 收束，勿重掃）**：共振 ungated-wide **5.0**（89/61 ship）；tall 0.25→79 +0.027% below bar；M38 gated complement sweep 全 below bar（唯一 mover 79 gated-6.0 +0.033%）。per-case 選 ratio 不需 arbiter（portfolio 即選擇器）
- **FREE_CLUSTER boundary-ungate（`ICCAD_FREE_CLUSTER_BND`，M39，勿重掃）**：鏡像 M36 anchored-BND（layout-key 仲裁、build-time wall-free）。**LIVE 但 below bar**：92/100 案 / 391 members、layout-key 確實挑更好 boundary aspect（非 M32 純死），但贏案全低權重小/中案、硬案 89/97/85/82/88 任何 ratio 零移動 → 三 stack best +0.034% < bar → **revert**（3 行可復原：static 宣告 + gate `(boundary==0||FREE_CLUSTER_BND>0)` + env parse）
- **reconstruction 遞迴二分 slicing builder（`recon_slice_probe.py`，M40，勿重試）**：reconstruction **RED-confirmed**。M29 只證偽 greedy 插入 builder（8.22）；M40 補做正統 spectral 遞迴二分 slicing（Fiedler-median cut、region-aspect 切向）配 M29 `render_bstar` exact-X + oracle-Y。**雙閘各自獨立證死**（self-check `trueTree_oracleY` quality=1.0000 重現 fp_sol）：**Gate A（P1 X 還原）FAIL**——slicing-X+oracle-Y quality **2.05**（ceiling 1.0、我們 1.274）、movable X-order **Spearman 0.009**（pin-free 零相關）；**Gate B（P2 deterministic Y 序）FAIL**——即便給 **true tree X**，5 個 deterministic Y 序最佳 +**159%** vs oracle-Y（Y 需 label）。任一死即 RED。⚠️ 全集 100% 有 preplaced（mean 2.7、無 pure-movable）→ builder 須 obstacle-aware；但 Spearman 是 movable-only/pin-free，preplaced 救不了此 kill
- **RF 三階 @高核（組員 codex prune V1 #19/26/27、我方候選 #8/12/26/27，2026-07-02 驗證）**：CORES=12 下 10 案 n>110 全部 `tCap==tM42`（被砍隻無一 wall-setter）→ **增益恰 0.00%**；quality 僅靠 #19/#21 case-90 proxy 完全並列（2.766374981）才零損。⚠️ **此 RED 是 cores-conditional**：低核（≤8）wall 翻 sum-bound → 同類砍法 −3.7~−9.3% → **M45 tier-4 已 ship cover**（strict gate、#19 as winner 恆保留、cores 偵測 fail-open）。高核 regime 勿再嘗試 pool-prune 攻大案 wall（max-setter 結構設底、(100,∞] candidates=0）。任何新 pool-prune 須過 `rf_score_model.py` M45 **strict selection-preserving gate**（逐案 cost 相等；WIN 不等式在 RF floor 附近會高估）——winner tally 單獨不足（tie/hmin 耦合會誤判）
- **frame 級 B&B 早棄（2026-07-07 code 驗證，勿投資 LB 證明）**：M46/M47 曾掛 conditional GO（「需先證 layout_score 對 partial layout 有單調下界」）——靜態驗證三事實即死：(1) `constructive.cpp:1568` `guide.swap(c2)` **無條件**執行，12/13 的 refine packs 是 guide 鏈環節（sc 沒改善也 load-bearing），跳過任一即非 bit-identical；(2) 可 exact 早棄的只剩非首 frame 的 initial pack = 3/52 packs ≈ **5.8% 上限**（OS/OM 候選 pack 本可 exact 棄，但 M41 已砍出池）；(3) `run_frame` 在 `layout_score` 前跑三個 nudge（`:1490-1492`），`final_boundary_nudge` 把 miss 的 boundary block 搬到 frame 外——若該 block 原撐 xmax 則 **bbox 反縮**、HPWL 雙向 → partial layout 對最終分數**無單調下界**。殘餘 exact 只剩 refine ≥2-cycle 偵測（opt-D 只證無 fixpoint）→ 已由 M49 證 RED（見下條）。見 `[[frame-bnb-early-abandon-red]]`
- **REFINE exact 截斷 / refine cycle 快轉 / max_trials=3（M49 量測 2026-07-07，勿重試 exact 版）**：RTRACE 全量曲線（20 案×13 profiles×4 frames）證三件事：(1) **無 exact REFINE 截斷點**——17/260 勝出 frame 的最後改善落在 pass 11（refine 改善不飽和、fat tail），K<12 必有案變；(2) **≥2-cycle 僅 2/1040 鏈**（case 94 #17 period 6、case 96 #12 period 2、0 fixpoint）→ 偵測快轉期望 ~0.09% band wall，不值得；(3) **max_trials=3 非 exact**（第 4 frame 勝 15/260）且 M≥11 floor 飽和邊際 ≈0 → 緩辦。quality-trade 版（非 exact）已由 M49 ship（K=4）；勿在 M≥11 再疊 wall cut（floor 飽和只付不收）。數據重現：`m49_refine_probe.py trace`。**M50 mid 補量（2026-07-09，勿重掃）**：`trace mid` 1000 pairs 同樣**無 exact 截斷**（40 勝出 frame 改善於 pass 11、K-coverage 到 REFINE_ITERS=12 才全 exact）；**cycle 在 mid 極多（538 events vs big 的 2）但快轉仍 RED**（要動 shipped C++ + band 大多 RF-floored）；**ungated K=4 mid 勿 ship**（11 movers、+0.069% local、@≥12c M≥11 淨損 +0.06~0.07%——floor 飽和 quality 損直接穿透）；**K=6 不必量**（movers ⊇ K=8 且含 41/42 +3.9~4.3% 大 mover、低核 upside 已被 tier 覆蓋）；max_trials 於 mid 同樣非 exact（992/1000 用滿 4 frames）。ship 版 = universal K=8 + cores≤8 K=4（M50）
- **OS K>16 / OM K 組合**：K=16 飽和；**OS16×free（M36）** +0.630% 但 48s/n=120（4× OS16，增益與 4× runtime 結構耦合，OS16+PIN+tight 無 free=+0.001%）；runtime 候補 om16/os24/os32 懲罰比不划算
- **compaction 方向偏好 / pack 向 connectivity 重心**：compact_layout 已對稱試 4 單向+8 兩步由 csc 仲裁；wire 已是動態重心
- **BP_WEIGHT 30000→1M 無變化**（無可行 bp=0 位置）；**M51 補：向下亦全死**（10000/3000/1000/300 standalone 0 wins；3000 疊 M37 recipe / fc_pin_tight 兩 stack 全 0，M37 stack 甚至倒退）→ **BP_W 軸雙向封卷**；**試更多 frame** all-frames→2.42（4-5 frame 最佳）；**wire×50000**→1.93；**wire_order**→vBd 390
- **FRAME_ASPECTS（M51 量測 2026-07-10，勿重掃）**：tall `0.67-0.33`（pre-M25）與 narrow `0.55-0.28`（M13）**早掃過**（僅 `tall_asp7_anc` 存活）；M51 補 wide/mid——**standalone 全 ~0.000%**（wide 2.2/2.6、wide-only、mid-fill 1.15/0.85、no-square、mild-tall 0.85/0.7 六集合；僅 case 9 n=30 零權重）；win 只在 **stacked**：wide ≥2.0 疊 fc_pin_tight 觸發 **clamp-frame**（`frame_candidates()` `pre_*+MARGIN`/`max_i*+MARGIN` 下限把 2.0/2.2/2.5 clamp 成同一 outline → 逐位同值 = 值不敏感、勿掃更多 wide 值）破案 99；M37 anchored host 較差（1.3129 vs 1.3084）；保留 1.8+2.2 變體亦同值（非「移除 1.8」效應）。**結構洞已封**：n≥60 只試前 4 frames（`max_trials`、等面積 tie-break 近方形優先）→ 預設集第 5 值 0.55 從未被試，no-square 集讓它被試 = 0 增益
- **FREE_RATIOS 加寬（M31）**：整池 oracle 僅 −0.044% + 抬牆 21→29.6s（free 搜尋 ∝ n²，結構耦合）→ 全軸死
- **proxy/_RH（M31 再確認）**：oracle-min==proxy(_RH=1.4)==1.3269 零漏分 → 要降分須降 oracle 本身
- **ML shape / perm ranking**：oracle 上限實驗 = BL packer 是天花板（perm+SA 3.27、shape only 3.42）
- **M52 tree-space 生成軌 / imitation 家族（Phase 0 量測 2026-07-11，勿重試）**：`m52_phase0_probe.py` 三量測：(i) 管線 GREEN（260 案 100% 逐位重現、decode+legalize 2-3ms/案）；(ii) **容錯帶 ≈ 0**——單一 near-miss token 錯 wR **1.232**（理想 soft 修復後仍 **1.072**）、random-slot 單錯 1.319、2% token 2.71、Y-order 2% 1.73、**dims 任何擾動（jitter/量化皆然）≈2.9-3.1 且 ε→0 已滿高**；懸崖機制 = boundary-touch（27/案、1e-6 bbox-edge）+ cluster abutment 對任何幾何誤差歸零 → `exp(2·vrel)`×2-3 疊 X-rule 傳播 gap；(iii) legalizer v0 硬可行修復 100%（構造保證、self-inflation=0）但 quality 不可修（model +213% ≫ interact +42%）。**疊 `recon_index_probe` 零訊號 → imitation 全滅，含 direct-layout 模型 B**（懸崖與表徵無關；v2 MSE + M26 注入 cap 封「模型出座標」變體）。倖存 ML 形態只剩 cost-driven sampling 搜尋 = 歸 M27 packer 條目管轄（勿以 M52 名義重開）。⚠️ 附帶 gotcha：`tree_decode_probe._cost_of` 不查 preplaced/fixed 硬約束（`target_positions=None`），任何 layout 級評分實驗必用 `m52_phase0_probe._cost_strict`

## 殘留 case（純 optimization 已榨乾）
89 **~1.523**（M37，仍最高；preplaced boundary 撐壞 outline）、85 **1.5240**（M49 REFINE cut 唯一 mover：pre-M49 1.5096、+0.95%；勝出 profile #23 靠 pass-11 改善）、65 ~1.690（M35）、62 1.5227、88 ~1.385（M36）、97 ~1.199（M36 ungate-boundary）、82 ~1.363（M34）、52 ~1.361（M35）、61 **1.3134**（M50 mover +0.81%；pre 1.3030 M37）、79 ~1.247、66 ~1.270、91 1.3481。**M51 movers（2026-07-10）**：99 1.3334→**1.3084**（wide-clamp frame、最重案）、47 1.2242→**1.2179**。**M50 mid movers（REFINE=8 trade @12c）**：49 1.6496（+2.18%）、54 1.4563（+1.81%）、68 1.5045（+1.84%）、70 1.4911（+0.71%）、64 1.3581（+0.16%）；低核 K=4 路徑另有 41/42/44/78/79 小 mover 與 51/77 微改善（見 probe 存檔）。硬 case（89/85/62）= preplaced boundary 幾何強迫；M33-M37 證實成員形狀（cluster/anchored/MIB aspect + tight frame）能鬆動 89/82/88/97/79/66/65/61/52。

## 環境 & 指令

- **主程式**：`constructive.cpp` + `optimizer_constructive.py`（舊 SA `optimizer_claude.cpp/.py` 僅 fallback）
- **Conda**：`C:\Users\Nordra\.conda\envs\iccadv\python.exe`；**Compiler**：`C:\msys64\ucrt64\bin\g++.exe`
- 組員參考碼：`C:\Users\Nordra\Downloads\teammate_iccad_study\`
- ⚠️ **eval 實際 ~2.5 分鐘（M50 後 avg 1.49s@12c；M49 1.54s）**（100 案 serial，大案 n>110 ~1.3-2.9s；M47 ~4 分、M46 ~6 分、M42 ~8 分、M41 前 ~15-17 分）；background 用 harness `run_in_background`（完成通知）；`> file 2>&1` 在 PowerShell 對 native exe 印 cosmetic `NativeCommandError`（無害）

```powershell
# 編譯（Bash 工具寫 .exe 會失敗，務必用 PowerShell）
& "C:\msys64\ucrt64\bin\g++.exe" -O3 -std=c++17 -o constructive.exe constructive.cpp

# 官方 portfolio eval（~2.5 分鐘 M49 後，確定性）
cd "C:\Users\Nordra\Downloads\ICCAD2026_FloorSet\FloorSet\iccad2026contest"
& "C:\Users\Nordra\.conda\envs\iccadv\python.exe" iccad2026_evaluate.py --evaluate ../optimizer_constructive.py 2>&1 | Select-Object -Last 12

# 快速單 profile A/B（~70 秒，乾淨確定性，與官方吻合）
& "C:\Users\Nordra\.conda\envs\iccadv\python.exe" analyze_constructive.py 2>&1 | Select-Object -First 12
```

## Gotchas
- **PowerShell 用 `;` 或 `if ($?){...}` 連接，不能用 `&&`**；Bash 工具寫 .exe 會失敗（sandbox）
- **輸出必須 `%.17g`**（非 %.10f）：否則精確 abutment 被捨入成虛假 shapely cluster fragment（M10，~144 假 fragment/100 案，−6.3% 單一最大 lever）。新增任何被 shapely 評分的幾何都要遵守；查不明 grouping violation 先疑精度
- **proxy 選擇用 shapely vrel**（`_proxy_metrics`），不可用 C++ METRICS vrel
- **compaction 選候選用 true-cost csc** `(area+hw·hpwl)·exp(2·(bv+gf)/nsoft)`，不可用 layout_score（後者 boundary 權 150000 ≫ grouping 6500，會拿 vCl 換 vBd）
- 新增重 profile 前先查它自己的 per-case cpu（每案 wall ≈ 最重 profile 的 max 項，非池總量）

## env 旋鈕（`constructive.cpp`）
- 預設：`ICCAD_BP_WEIGHT`=30000、`ICCAD_WIRE_MULT`=×1、`ICCAD_ANCHOR_W`=0.10、`ICCAD_LR_ASPECT`/`ICCAD_TB_ASPECT`（boundary aspect，預設 2.50/0.40）
- 關後處理：`NO_COMPACT`/`NO_REFINE`/`NO_PUSH`（關 M14-16+24）/`NO_BND_PUSH`/`NO_SWAP`/`NO_JUMP`；`PUSH_PASSES=N`、`COMPACT_ITERS=N`、`REFINE_ITERS=N`
- pack-order 軸：`WIRE_TIEBREAK`、`WIRE_BFS`、`BFS_PIN`、`ORDER_SWAP=K`、`ORDER_MOVE=K`、`GUIDE_MED`（M26 ship）
- **M41+M42+M45 RF lever（wrapper 旋鈕，`optimizer_constructive.py` `_pool_indices()` 四階 index-based filter，非 constructive.cpp）**：`ICCAD_ADAPTIVE_POOL`（預設 **1=on**）總開關；`=0` 還原 full 41-prof（quality-best 1.3248）。**M41 階**：砍 `ORDER_SWAP`/`ORDER_MOVE` profiles，`ICCAD_ADAPTIVE_N=K` 只砍 `block_count>K`（預設 0=全砍）。**M42 階**：砍 `_BIG_REDUNDANT_IDX`（22 隻，M51 起含 #18）當 `block_count>ICCAD_ADAPTIVE_FREE_N`（預設 **100**）。**M45 tier-3**（universal）：砍 `_M45_BAND_DROP`（9 隻，60<n≤100、35→26），`ICCAD_ADAPTIVE_BAND=0` 關。**M45 tier-4**（低核）：`_effective_cores()≤_M45_CORES_MAX(8)` 時砍 `_M45_LOWCORE_DROP`（(40,60]→14、(100,110]→8、(110,∞]→9；M51 起前兩 band 含 #40）；`ICCAD_ADAPTIVE_CORES=N` 強制 cores 值（≤0/garbage=auto、≥9 等效關閉）；偵測 `sched_getaffinity`→`cpu_count`→9999 fail-open（誤判方向=不觸發=shipped 行為）。⚠️ 改 `_PROFILES` **或重編 constructive.exe**（proxy tie 可能翻轉）後須用 `rf_score_model.py` M42+M45 區塊重算全部 drop 常數，**並重過 `m49_refine_probe.py` gate（`variant 4 big`+`variant 8 mid`+`variant 4 mid`）**（M51 ship 2026-07-10 已全套重跑：re-audit 4200 combos → 常數 regen（+#18、tier-4+#40）→ 三 gate 綠 → eval 1.3265）。
- **M49+M50 REFINE band-cut（wrapper 旋鈕，`_band_env()`）**：`block_count>100` 疊 `ICCAD_REFINE_ITERS=4`（M49）；`60<n≤100` 疊 `=8`（M50 universal，`_M49_REFINE_BAND=((60,100,"8"),(100,∞,"4"))`），`_effective_cores()≤_M45_CORES_MAX(8)` 時 mid 改疊 `=4`（`_M50_REFINE_LOWCORE=((60,100,"4"),)`；偵測 fail-open→universal=安全檔；`ICCAD_ADAPTIVE_CORES` 同 M45 強制/關閉）。`ICCAD_ADAPTIVE_REFINE=0` 關兩 band；`ICCAD_ADAPTIVE_POOL=0` 也一併還原 full REFINE（quality-best 模式保持純淨）。量測依據 + 勿重掃事項見死路 ledger「M49 量測」「M50 mid 補量」條。
- **M48 編譯/安全網（wrapper）**：`ICCAD_CXX=編譯器` 插到編譯鏈最前；鏈 = msys g++→g++→clang++→c++ × -O3→-O2，每次成功編譯（含 mtime 捷徑）都須過 `_binary_runs()` 1-block smoke 才算數
- **free-aspect 六子軸**（全 gated、off=bit-identical；M29-M37 ship，per-case win 細節見 memory）：
  - `ICCAD_FREE_ASPECT=1`（M29/M30）：single interior movable 在 ±1% area 搜 aspect（`FREE_RATIOS={1.0,1.5,0.6667,2.0,0.5}`），per-candidate；8 free profile 進 portfolio（`free_pin_tight`/`free_gm_pin` 為全池 LOO 前二）
  - `ICCAD_LR_ASPECT`/`ICCAD_TB_ASPECT`（M32）：decoupled **uniform** boundary aspect（高 LR + TB 留 default 0.40）。⚠️ per-block FREE_BOUNDARY 死路 → 須 uniform
  - `ICCAD_CLUSTER_ASPECT=r`（M33）：uniform reshape 純 movable INTERIOR cluster 成員，套在 `apply_safe_mib_dims()` 後、`make_group_item` 前；共振 3.0
  - `ICCAD_FREE_CLUSTER=1` + `ICCAD_FREE_CLUSTER_RATIOS=r1,...`（M34，預設集 `1.0,1.5,0.6667,2.0,0.5`）：per-member 在 `make_group_item` 內搜 aspect、**cluster layout-key 仲裁**（非 greedy-area，避 M32 失敗）；build-time → widen 免抬牆，共振 4.0
  - `ICCAD_FREE_ANCHORED=1` + `ICCAD_FREE_ANCHORED_RATIOS=...` + `ICCAD_FREE_ANCHORED_BND=1`（M35/M36）：mixed cluster movable 成員在 `pack_in_frame` wall-attach 搜 aspect（per-frame、只 commit `out[]`）；gate `boundary==0&&mib==0&&!is_fixed`，BND=1 ungate boundary 成員
  - `ICCAD_MIB_ASPECT=r`（M37）：`apply_safe_mib_dims` 無-master 共享方形 reshape 成同面積共享矩形（保 MIB violation 0、all-interior gated）；共振 wide 5.0
  - `ICCAD_SOFT_ASPECT=r`：全域 interior aspect（粗版，未進 portfolio）
- 死路（code 保留 gated off / 未實作，勿重掃）：`BFS_NORM`、`CLUSTER_ORD=1/2`、`REFRAME`；`ICCAD_FREE_CLUSTER_BND`（M39 已 revert，3 行可復原）
- 離線探測（永不 ship）：`ORDER_FILE=path` + `ORDER_GLOBAL=1`（oracle-perm）
- `ICCAD_CONSTRUCTIVE_SINGLE=1` 退單 base profile（1.7045）

## 工具
- `analyze_constructive.py`（單 profile per-case breakdown）、`profile_vs_portfolio.py KEY=VAL`（新 profile 逐案算 oracle-min 增益，>0.05% 才加）
- `rh_sweep.py`（真值快取 + 掃 _RH/proxy）、`portfolio_ceiling.py`（oracle 天花板）、`profile_audit.py`（M25 池審計：win tally/LOO/**per-profile cpu mean/max**，cache `audit_cache.pkl`）
- **`rf_score_model.py`（M41+M42+M45 RF-aware 投影模型，永不 ship）**：載入 `audit_cache.pkl`，逐案 Q=proxy oracle-min、wall=`max(max_i t, Σt/cores)`，投影 real total 掃 median M。full-pool RF=1.0 重現 1.3269 為 sanity gate。**M42 區塊**：per-big-n winner tally → greedy 精煉（WIN check）→ 推薦 `_BIG_REDUNDANT_IDX`。**M45 區塊**：band-scoped winner（(40,60]/(60,100]/(100,110]/(110,∞) + fine/coarse 對照）→ **strict selection-preserving refine**（逐案 cost 相等 gate，比 WIN 不等式強）→ cores∈{4,6,8,12,16}×M 投影矩陣 + band wall 表 + #19/#21 tie 記錄 + ready-to-paste `_M45_BAND_DROP`/`_M45_LOWCORE_DROP`。內建 assert：`refine(100)==_BIG_REDUNDANT_IDX`（shipped 漂移偵測）。**M46 區塊**：α-靈敏度投影（uniform / FREE-stack-only × cores × M、max-setter tally、tier-3/4 常數 drift assert、α=1 sanity；M46 ship 後基準=新 dt，此區塊=殘餘 headroom 掃描）。改 `_PROFILES` 或重編 exe 後重跑更新全部常數（M46 ship 2026-07-03 已重跑：SANITY 1.3269、全 asserts OK、常數零漂移；M49 ship 2026-07-07 再跑：全綠）。⚠️ **M49 後 `audit_cache.pkl`/本模型 = n>100 的 K=12 counterfactual**（positions 未截斷、dt 偏保守）——band 內的 variant 差異由 `m49_refine_probe.py` 負責，兩者並存不衝突
- **`m47_overhead_probe.py` / `m47b_proxy_equiv.py`（M47 probe/gate，永不 ship）**：前者 monkeypatch wrapper 分解 solve() 計時窗（`total = pre(serialize) + pool_span(C++) + proxy tail`，25 案表 + n>100 aggregate）；後者 fast-proxy 逐位等價 gate（100 案×3 profiles，`m_old != m_new` 即 FAIL）。任何再動 `_proxy_metrics`/selection 的變更都要重過這兩關 + 官方 eval 1.3277 逐位
- **`m48_coldstart_dryrun.py`（M48 dry-run，永不 ship；Beta/Final 送件前重跑）**：四 phase、各自獨立 subprocess（sys.modules 快取會遮 packaging bug）：0 repo 基準 → 1 乾淨目錄冷啟動（無 .exe、只放 `optimizer_constructive.py`+`constructive.cpp`+`optimizer_claude.py` 三檔 → 編譯 + 3 案逐位）→ 2 垃圾 binary 須被 smoke 攔下重編 → 3 `ICCAD_CXX=bogus` 證編譯鏈迭代 → 4 絕對路徑衛生（report-only）。只在 temp 編譯、不碰 repo exe
- **`m46_stage_probe.py` + `constructive_m46.cpp/.exe`（M46 probe，永不 ship）**：計時儀裝副本（`ICCAD_STAGE_TIMING=1` → stderr TIMING 行；**M49 再加 `ICCAD_REFINE_TRACE=1` → stderr RTRACE 行**：逐 refine pass sc/imp/FNV64 hash、逐 frame last_imp、case 勝出 frame）+ 四模式 driver（`verify` 12 組 byte-compare / `verify100` 全 100 案×6 profile + per-band α / `diff` 差分旋鈕 / `timing` stage 預算）。opt-A/B/C 已於 M46（2026-07-03）ship 併入 `constructive.cpp`；副本保留儀裝供 stage 分析，verify/verify100 現在=比對 merged vs probe（M46 ship 時 600/600 PASS、M49 儀裝後 verify 12/12 PASS + trace 模式 260/260 position-vs-cache MATCH）
- **`m49_refine_probe.py`（M49/M50 probe/gate，永不 ship）**：`trace [big|mid]` = 儀裝 exe 收 last_imp 分佈（全 frame + 勝出 frame）/勝出 frame idx（max_trials 證據）/cycle 偵測 + 全 pair position-vs-cache 驗證；`variant K1[,K2..] [big|mid]`（M50 起 band 參數化，預設 big）= shipped exe 疊 `ICCAD_REFINE_ITERS=K` 跑 band×kept（含 K=12 control，須全 pairs 逐位=cache 才算 session 有效；big 260、mid 1000）→ exact 判定 + band wall 表 + Path B 真 cost 表（proxy 選擇 + evaluate_solution）+ real 投影（M×cores、鏡射 rf_score_model pools 含 tier-4；mid 模式下大案兩側同用 K=12 counterfactual dt，delta 嚴格歸因 mid）。**任何 `_PROFILES`/exe 變更後重跑 `variant 4 big`+`variant 8 mid`+`variant 4 mid` 重新 gate M49/M50**；⚠️ trace 的 K-coverage 表標籤差一（表中 K=x 指 last_imp≤x，即 REFINE_ITERS=x+1）；pm cache `m49_pm_cache.pkl`
- `dbg_area.py`、`dbg_boundary.py` / `dbg_vio_stats.py`（violation 分類）、`dbg_compact*.py`、`dbg_hpwl_push.py`
- **`m52_phase0_probe.py`（M52 Phase 0 probe，永不 ship）**：`pipeline|slack|legalize|vocab|all [quick]`——tree_sol→`render_bstar`→`compact_down`/`legalize_v0`→官方 `evaluate_solution` 用 **`_cost_strict`**（⚠️ 勿用 `tree_decode_probe._cost_of`：`target_positions=None` 不查 preplaced/fixed 硬約束）；合法性保持 tree 擾動（MOVE_SUB/MOVE_LEAF/SWAP/FLIP + `near_miss_one`）、Y transposition、面積精確 dims 擾動（MIB 群組一致）；`legalize_v0` = obstacle-aware ordered gravity（未擾動恆等 = 逐位重現 fp_sol）；cache `m52_phase0_cache.pkl`（sig 驗證）。訓練集限定（驗證集無 tree_sol）；報表 R = cost/fp_floor per-case、wRq = 理想 soft 修復上界
- **離線天花板探測（永不 ship，保留為記錄）**：`oracle_perm_probe.py`（M26 ordering）、`dbg_seqpair.py`（M27 global-packer）、`reconstruct_probe.py`（M28：oracle 1.1079、headroom 100% quality；快取 `reconstruct_probe_cache.json`）、`tree_decode_probe.py`（M29 decoder：tree_sol=B\*-tree、X 規則 100% 精確、Y 序是品質 lever；讀訓練 `floorset_lite/*.th`）、`tree_build_probe.py`（M29 greedy builder 死路 8.22）、`recon_slice_probe.py`（M40 遞迴二分 slicing：雙閘死、reconstruction RED-confirmed）

## 檔案結構（要點）
- `constructive.cpp` 🏆 — placer，含 M9-M37 全部行為 + M46 hot-path exact 加速（`M46KeyHash`/`M46Grid`/opt-A，bit-identical）（見 env 旋鈕）+ METRICS stderr
- `optimizer_constructive.py` 🏆 — 41-prof portfolio（M51 #40 `fa22_fc_pin_tight_wire` = wide-clamp frame、案 99 winner）+ shapely proxy(_RH=1.4)；pruned profile 標 `[Mxx-pruned]` 註解可復原；**M41+M42+M45 `_pool_indices()` default-on 四階砍 profiles：swap + `_BIG_REDUNDANT_IDX`（n>100）+ `_M45_BAND_DROP`（universal 中案 band）+ `_M45_LOWCORE_DROP`（`_effective_cores()≤8` 才啟用）**；**M47 proxy 路徑：`as_completed` 就地算 proxy（一次一個，⚠️ 並發=GIL thrash 4× 慢）+ `_hpwl_b2b_fast`/`_hpwl_p2b_fast` tolist 版（逐位=harness 版，動它必重過 `m47b_proxy_equiv.py`）**；**M48 hardening：`_ensure_compiled` 編譯鏈+`_binary_runs()` smoke、`solve()`（安全 wrapper）→`_solve_impl`（原 body）三層安全網（SA→`_row_fallback`）+ selection 守門回 cands[0]**；**M49+M50 `_band_env()`：n>100 疊 `ICCAD_REFINE_ITERS=4`、60<n≤100 疊 `=8`（低核 cores≤8 改 `=4`，`_M50_REFINE_LOWCORE`；`ICCAD_ADAPTIVE_REFINE=0` 關）**；旋鈕見 env 節
- `optimizer_claude.cpp/.py/.exe` — 舊 SA，僅 fallback
- `floorplan_gnn.pth` — v1 GNN（unsupervised，僅舊路線）；`gnn_training.md` — ML 文件
- `iccad2026contest/iccad2026_evaluate.py` — 評估腳本

## ML（M52 Phase 0 量測 RED 2026-07-11——證據見「未來發展方向 §0」與死路 ledger；歷史見 `gnn_training.md`）
- **歷史（v1-v3，勿重走）**：v1 unsupervised GCN 退役；v2 supervised MSE on `fp_sol` 失敗（ill-posed 一對多）；v3 pairwise ranking 訊號弱 + M26 oracle-perm 證實「訊號餵我們的 placer」cap 在 +0.005% → 該類路線永久封卷
- **M52（2026-07-10 重開 → 2026-07-11 訓練前判死）**：生成式 imitation（tree-空間與 direct-layout 皆然）被「**容錯帶=0 懸崖 × 輸入零訊號**」二重殺死（`m52_phase0_probe.py`，數字見 §0/ledger）——零訓練成本出局。資產：dims = integer divisor pointer 的 vocab 結論、decode+legalize_v0 管線、`_cost_strict`
- **重開唯一條件**：他隊 1.29 方法細節證明 legit、非 imitation、非 leak → 屆時歸 M27 packer 軸評估（cost-driven sampling 搜尋），非 ML 軸
- ⚠️ 本環境**禁止跑訓練、可推論**：訓練 script 交使用者上 CUDA；需要訓練時**停下告知使用者**

## 舊 SA 路線（fallback only，`optimizer_claude`）
峰值 8-profile 3.0625。constructive 已全面超越，僅作 fallback。`analyze_violations.py`/`check_viols.py` 不可跑（用 `viol_breakdown.py`）。
